# 开发日志｜企微篮球晨报稳定性修复

日期：2026-09-15
分支：`codex/education-2.0`（工作区已有其他任务改动，本次未切换分支、未提交、未推送）

## 生产故障证据

- 2026-09-07 至 2026-09-15 共 9 个业务日，仅 09-07、09-09 成功创建待发任务。
- 生成失败主要为 `openai_response_missing_output` 与 `openai_response_invalid_json`。
- 内容校验失败记录普遍只有 1 条占位新闻，且 `search_source_urls` 为空；失败稿把中国篮协首页当作来源并明确写有“待核实”，安全校验阻止其进入客户群是正确行为。
- 生产对照请求确认：DeepSeek 即使显式强制 `web_search`，响应仍可能只有 `reasoning` 与 `message`，没有可审计的 `web_search_call` 或引用注解。
- 09-10 至 09-13 还存在机构提醒模板被 `observation_not_for_training_organization` 误拦截的问题。

## 本次修复

- 增加白名单来源独立发现：新华网体育、中国新闻网体育、中国篮协、FIBA、NBA。
- 候选文章必须逐页可访问、域名仍在白名单、发布时间位于回看窗口，跳转目标也必须继续满足白名单。
- 模型优先只使用已核验候选，URL、发布时间、国内外范围和类别必须原样返回；模型自造或修改的 URL 不计入来源证据。
- 新增腾讯新闻专用连接器：调用体育与 NBA 频道公开 PC 文章流，构造并回读 `news.qq.com/rain/a/...` 正式页，核对标题、作者、发布时间和正文。
- 腾讯来源按精确名单放行；“体育领域创作者”、个人篮球号、AI 账号和没有正文摘要的条目默认拒绝。禁止为启用腾讯源而放宽到整个 `qq.com`。
- DeepSeek 空正文、非法 JSON、响应不完整和瞬时错误增加单轮内部重试，并设置结构化输出上限。
- 修正机构运营日历模板的语义误判，不降低隐私、营销、夸张表述和事实来源门槛。

## 验证

- `node tools/test-wecom-group-briefing.js`：通过。
- `npm run check --prefix cloudfunctions/sxWecomGroupBriefing`：通过。
- `git diff --check -- cloudfunctions/sxWecomGroupBriefing tools/test-wecom-group-briefing.js ops/lightserver`：通过。
- 真实白名单来源只读验证：最近 72 小时得到 4 条国内职业/国家队候选、5 条国外候选，均带文章 URL 与页面发布时间。
- 腾讯真实链路只读验证：文章流可返回真实文章 ID；诊断放行的样本成功完成正式页、作者和发布时间回读。按生产可信名单运行时，当天 41 条篮球相关稿件全部属于个人创作者，因此腾讯候选为 0，安全边界符合预期。

## 生产部署

- 用户明确授权后，于 2026-09-15 13:12（Asia/Shanghai）部署到 `124.221.239.63`。
- 部署前应用备份：`/opt/sxf-platform/backups/wecom-group-briefing-20260915-131050`。
- 部署前配置备份：`/opt/sxf-platform/backups/wecom-group-briefing.env.20260915-131050.bak`，权限仍为 `0600`。
- `SOURCE_DOMAIN_ALLOWLIST` 精确新增 `news.qq.com`，没有加入宽泛的 `qq.com`；生产白名单共 12 个域名规则。
- 腾讯文章流读取页数为 2，可信来源精确名单 11 项；DeepSeek 单轮尝试 2 次，结构化输出上限 6000。
- 容器 `sxf-wecom-group-briefing` 已重建并达到 `healthy`，健康接口返回 `ok=true`、`service_enabled=true`、`scheduler_enabled=true`。
- 容器内文件 SHA-256：
  - `core.js`：`aa397515f781b0cfa2a5a1487de052884621acc94699472a7a3e99fd94c135dd`
  - `index.js`：`21daaeb291f73afef7bea229c91b2c7cc4ac1044884b16b0ef6e59c4be900be5`
  - `README.md`：`ccb8f9a999f6476e1a514f2fd0b4a7e0f2edfbd037ae507e78c71a24f20219a0`
- 生产配置不落库真实生成验证：状态 `ready`，4 条新闻，国内 2 条、国外 2 条，4 条来源证据，校验错误 0；来源为中国新闻网体育和 FIBA。
- 部署暂存目录和候选镜像已清理，应用与配置回滚备份已回读确认保留。

## 边界与下一步

- 未提交、未推送 GitHub 或 Gitee。
- 本次真实生成验证未写入状态、未调用企微接口、未创建或补发 2026-09-15 群发任务。
- 生产定时器和自动创建开关保持部署前既有启用状态，下一次每日调度将使用新来源链路。
