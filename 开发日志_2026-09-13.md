# 开发日志（2026-09-13）

## 云端音效库扩充

- 用户提供 13 个 WAV 源文件：进攻音乐 5 个、防守音乐 4 个、自定义目录 4 个；源文件保持不变。
- 将源文件转为适合小程序播放的 MP3，总大小由约 163MB 降至约 6.58MB。
- 云存储新增：进攻音乐 5 个、防守音乐 4 个、自定义音效 4 个。
- 用户纠正分类后，将休息音乐1、休息音乐2、暂停复制到 `mc-mp3/自定义音效/`；原云文件保留为安全备份，但对应的 3 条错误暂停分类记录已停用。
- 云端音效库写入并回读 26 条有效记录：暖场 13、进攻 5、防守 4、自定义 4；26 条均成功生成短期播放地址。
- 自定义音效包含互动、休息音乐1、休息音乐2、暂停，全部使用 `ambience` 频道并进入 MC 自定义音效可选来源。

## 首次使用无声修复

- 根因：暖场、进攻和防守属于背景音乐，但原预加载队列只处理得分、自定义、暂停和关键比赛音效；新手机没有本地缓存时，首次点击需要临时下载，且私有云文件直读失败时预加载没有短期地址回退。
- 计分板首次进入或音效新增后，优先预加载一首暖场音乐、全部进攻/防守短音乐及关键比赛音效。
- 预加载期间显示“加载比赛音乐”；全部完成后弹出“音效加载完成”，失败时明确提示检查网络并重新进入。
- 云端音效保持私有，不扩大长期公开读取范围；音效库读取函数每次签发短期播放地址，客户端直读失败时自动使用短期地址下载并缓存。
- 音乐切换采用双播放器交叉切换，交叉淡化由约 420ms 缩短至约 180ms；暖场播放期间后台提前缓存下一首，连续点击仍以最后一次操作为准。

## 验证与边界

- 13 个新增云存储文件上传成功；分类纠正后，自定义音效 4 个目标路径均回读存在。
- `sxSaveAudioLibrary`、`sxGetAudioLibrary` 已部署；音效库回读 26 条，短期播放地址 26 条。
- 暖场、进攻、防守、暂停、互动五类抽样短期地址均返回 HTTP 200 和 `audio/mpeg`。
- 首次预加载行为测试通过：开始时显示加载提示，成功后仅弹出一次完成结果；再次进入且缓存有效时不重复弹窗。
- 音乐快速切换测试通过：双播放器交叉切换保持，切换时长约 180ms，暖场下一首后台缓存。
- 真机反馈出现“音效未加载完整”后，将首次可用门槛调整为暖场、进攻、防守各成功缓存一首；达到最低可用条件即提示完成，其余音乐在后台继续加载。
- 失败提示新增“重新加载”，短期签名地址下载失败时会重新向云函数获取新地址再重试；随机播放优先选择已经缓存的歌曲。
- 首次预加载补丁提交：`e99a99c2`（`fix(mc): make first audio preload usable`），已推送 GitHub 与 Gitee 的 `codex/education-2.0` 分支。
- 微信开发者工具已覆盖上传同一2.1.6开发代码，上传说明为“修复首次音效预加载误报，增加重试和后台补载”；总包3,230,491字节、主包458,512字节、计分板分包275,261字节。
- 本次未修改版本号；微信开发者工具已成功上传2.1.6开发代码，上传说明为“扩充云端音效库，修复首次无声并优化音乐切换”，总包3,229,237字节、主包458,512字节、MC设置分包47,534字节、计分板分包274,007字节。
- 当前仅完成开发代码上传，未提交微信审核或正式发布。
- Git分支：`codex/education-2.0`；音效功能提交：`d9ddebf4`（`feat(mc): expand cloud audio library and preload music`）。
- GitHub `origin` 与 Gitee `gitee` 已同步并回读到同一提交 `d9ddebf4997049502d10f7839a2170987a4190c8`，两个远端 ahead/behind 均为 `0/0`。
