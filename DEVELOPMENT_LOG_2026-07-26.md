# 2026-07-26 开发记录

## 官网 AEO / GEO 每日优化

- 分支：`codex/education-2.0`。
- 发文前刷新公开搜索、正式域名收录辅助信号和外链信号；由于尚未接入站长平台 URL 级数据，既有页面继续标记为“待复查”。
- 新增 3 篇不同搜索意图文章：
  1. 核心长文：青少年篮球赛事参赛名单怎么管理？
  2. 单一问题：篮球比赛点错比分怎么办？
  3. 场景文章：篮球试听课怎么组织临时对抗赛？
- 更新内容中心、Sitemap、`llms.txt`、收录台账和外链台账。
- 中文质量自评：名单管理 45/50；比分纠错 45/50；试听课对抗赛 44/50。均检查事实边界、产品开放能力和首页关键词链接。
- 未修改小程序、云函数或 PC 后台业务。
- 本轮不提交、不推送 Git。

## 验证与部署

- `npm run site:validate` 通过：9 篇文章、16 个 Sitemap URL。
- `npm run site:deploy` 通过；部署目标为篮球环境 `sxf-basketball-d9gp6yt0rd1f7be4d`。
- CloudBase 系统域名与正式域名的首页、内容中心、Sitemap、robots、`llms.txt` 和全部 9 篇文章均回读成功。
- 今日 3 篇新文章已标记为“已上线、待复查”，未把上线状态写成搜索收录。
- 未解决数据边界：尚未接入百度、Google 或必应站长平台，无法取得 URL 级收录、曝光、查询词、点击和排名数据。
- 下一步：2026-07-28 起按 3—7 天观察窗口复查既有页面；取得站长平台权限后补充 URL 级证据。

## 搜索优化技能同日复核（12:23）

- 检测到今日 3 篇文章及配套站点更新已经完成，因此执行幂等复核；未重复生成第二批文章，未重复部署相同内容。
- 再次运行 `npm run site:validate`，结果通过：9 篇文章、16 个 Sitemap URL。
- 正式域名首页、内容中心、Sitemap、robots、`llms.txt` 和今日 3 篇文章均返回 HTTP 200。
- 今日 3 篇文章的页面标题、正式 URL 与自指 canonical 一致。
- 重新检查品牌词、域名和今日核心文章标题；公开搜索仍未提供可核验的 URL 级收录证据，也未发现新的可核验站外链接。
- 收录状态保持“已上线、待复查”；本次未重新部署，未提交或推送 Git。

## CSDN 首篇外链稿

- 用户确认知乎首篇文章已于 2026-07-25 手动发布，今日不重复发布知乎内容。
- 新增 CSDN 技术稿《微信小程序篮球计分板实战：计时、暂停和撤销怎么设计》。
- 文章依据当前计分板的正计时/倒计时、前后台时间恢复、暂停联动、操作快照和撤销逻辑整理；示例代码明确为简化版本。
- 正文只保留一个指向正式域名现场计分指南的链接，未添加联系方式或多重导流。
- 稿件通过内容质量检查和严格草稿检查，均为 0 个硬性问题、0 个警告；中文质量自评 45/50。
- 保存路径：`docs/outreach/csdn/2026-07-26-wechat-basketball-scoreboard-timer-pause-undo.md`。
- 用户已确认完成 CSDN 手动发布。
- 用户提供的编辑页文章编号为 `163215052`；该地址属于创作后台，不作为公开外链 URL。
- 发布后精确标题、文章编号和目标官网链接的公开检索暂未返回结果；由于当前未取得公开文章 URL，暂记为“已发布待核验”，尚不计入有效外链。
- 已取得并核验 CSDN 公开文章：`https://blog.csdn.net/weixin_50328081/article/details/163215052`。
- CSDN 来源页返回 200，标题与文章一致，canonical 已去除 `spm` 跟踪参数。
- 正文锚文本“篮球比赛现场计分指南”直接指向 `https://www.sxfbasketball.cn/guides/basketball-scoring.html`；目标页返回 200。
- CSDN 为该链接添加 `rel="nofollow"`。该链接计入有效品牌与引荐外链，但不宣称传递搜索排名权重。

## 公众号行业主文固定小程序入口

- 按用户确认，将赛小蜂篮球小程序卡片写入蜂狂篮球行业主文的长期交付规则。
- 固定 AppID：`wx06d735da15276acd`；固定页面：`pages/quick-match/index`；卡片标题：`一键开赛，现场计分`。
- 固定卡片位置为正文结束后、资料来源前；每篇行业主文只插入一次，避免与首页卡片重复。
- 《蜂家日记》继续保持纯故事内容，不加入产品宣传或小程序卡片。
- 微信预览链接中的 `tempkey` 属于临时凭证，不作为长期链接保存；长期规则以 AppID 和页面路径为准。
- 更新 `tools/wechat-official/CONTENT_STRATEGY.md`、`tools/wechat-official/article-prompt.txt` 及多平台发文技能账号配置；账号配置校验通过。
- 本次未提交、未推送 Git，未自动发布任何内容。

## 多平台发文控制台双击无响应修复

- 原因：控制台实际已在后台运行并监听 `127.0.0.1:4318`，但无窗口EXE二次启动时唤起浏览器不稳定，用户侧看起来像“没有反应”。
- 调整 `tools/publishing-console/server.js`：优先通过 Windows Explorer 打开控制台地址，失败时回退到系统默认URL启动方式；端口已被现有实例占用时，等待浏览器启动后再退出第二个进程。
- `npm run check` 通过，重新构建 D 盘EXE并完成接口回读。
- 当前仅有一个后台实例运行，`/api/dashboard` 返回 HTTP 200；重复启动测试后仍保持单实例。
- 旧版EXE备份：`D:\多平台发文控制台\多平台发文控制台-旧版-20260726.exe`。
- 本次未提交、未推送 Git。

## 多平台账号绑定流程

- 按用户要求，为头条号、顶端号、百家号、企鹅号和知乎增加“绑定账号”入口。
- 绑定流程只打开平台官方登录页；二维码、短信、身份验证和最终发布均由用户操作。
- 用户登录后回到控制台填写一次官方页面显示的账号名称并确认；控制台保存账号名称、平台定位、内容格式和人工交付规则。
- 控制台不保存密码、验证码、Cookie、登录二维码或身份资料，也不读取浏览器登录态。
- 企业文章平台默认使用篮球教培/青少年篮球文章定位；知乎使用结论先行、证据边界清楚的问答结构。
- 绑定后的账号进入“建立操作箱”列表，交付方式为人工发布包；`auto_publish=false`，首次真实发布前 `route_tested=false`。
- 隔离配置模拟绑定成功：账号模板校验通过，绑定接口和控制台回读均返回 200，真实账号配置未被测试数据污染。
- 重新构建并替换 `D:\多平台发文控制台\多平台发文控制台.exe`；当前单实例运行，控制台接口返回 200。
- 绑定功能前EXE备份：`D:\多平台发文控制台\多平台发文控制台-绑定功能前-20260726.exe`。
- 本次未提交、未推送 Git。

## 平台登录页无法打开修复

- 复现结果：`/api/open-platform` 返回 200，但旧版通过隐藏 PowerShell 调用系统浏览器时未实际显示登录页。
- 改为双通道打开：用户点击后由当前浏览器直接新开官方创作页；若弹窗被拦截，再由 Windows Explorer / 系统默认URL处理器打开。
- 头条号、顶端号、百家号、企鹅号和知乎统一使用该入口，不涉及自动登录、表单填充、Cookie读取或验证码处理。
- `npm run check`、EXE构建和控制台接口回读通过；头条号官方入口测试返回 200。
- 已替换 `D:\多平台发文控制台\多平台发文控制台.exe`；修复前版本备份为 `D:\多平台发文控制台\多平台发文控制台-登录修复前-20260726.exe`。
- 本次未提交、未推送 Git。

## 多平台发文控制台桌面内置浏览器

- 将原有网页启动器升级为 Electron 桌面程序；控制台与平台登录页均在程序窗口内使用，不再依赖系统默认浏览器能否正常唤起。
- 头条号、顶端号、百家号、企鹅号和知乎分别使用独立、可持久化的浏览器分区，登录状态保存在当前 Windows 用户的 `%LOCALAPPDATA%\SxfPublishingConsole\BrowserProfiles` 中，平台之间不共用登录状态。
- 控制台不提供密码、验证码、Cookie 或身份资料的读取、导出和自动填写接口；用户仍需手动登录、处理平台验证、核对内容并点击最终发布。
- 账号绑定继续采用“打开官方创作页 -> 用户手动登录 -> 回到控制台填写页面显示的账号名称 -> 保存绑定”的合规流程。
- Electron 固定为 40.0.0，以适配当前 Node.js 20 环境；`npm run check` 与 `npm audit --omit=dev` 通过，生产依赖审计为 0 个漏洞。
- 源码模式使用独立端口 4320 验证通过；便携版 EXE 使用独立端口 4321 验证通过，并确认能够创建平台独立浏览器分区及正常退出。
- 已生成并安装 `D:\多平台发文控制台\多平台发文控制台.exe`，文件大小 85,473,658 字节；旧 Node 网页版备份为 `D:\多平台发文控制台\多平台发文控制台-Node网页版-20260726.exe`。
- 正式启动后 `http://127.0.0.1:4318/api/dashboard` 返回 HTTP 200；重复点击 EXE 后监听进程保持不变，单实例检查通过。
- 构建阶段生成的便携版 EXE 已完成实际运行验证；后续 NSIS 临时压缩包清理曾受 Windows 文件占用影响，不影响已生成和安装的 EXE。
- 本次未提交、未推送 Git。

## 内置平台窗口空白修复

- 用户实测打开头条号时，平台窗口能够创建，但顶部说明栏和官方登录页均显示为空白。
- 原因定位为 Electron 40 下 `WebContentsView` 未稳定挂载到平台窗口；网络和安装包资源均正常，头条号官方入口返回 HTTP 200。
- 将平台页面容器改为兼容性更稳定的 `BrowserView`，按窗口内容区重新计算边界，并保留各平台独立持久化登录分区。
- 增加页面加载失败提示和重试入口，真实网络错误不再只显示空白窗口。
- 源码隔离测试已实际渲染并截图验证：平台顶部说明栏正常，头条号手机号登录、扫码登录及第三方登录区域均完整显示。
- 临时验证窗口在测试结束时自动关闭；该窗口未使用用户的正式登录分区，也未保存用户刚输入的验证码。
- 版本更新为 0.2.1；`npm run check` 通过，便携版 EXE 构建成功。
- 已将修复版安装到 `D:\多平台发文控制台\多平台发文控制台.exe`，文件大小 85,475,328 字节；修复前版本备份为 `D:\多平台发文控制台\多平台发文控制台-内置浏览器修复前-20260726.exe`。
- 正式程序已重新启动，`http://127.0.0.1:4318/api/dashboard` 返回 HTTP 200；临时测试实例均已关闭。
- 本次未提交、未推送 Git。

## 多平台发文控制台 0.3.0 左右工作台

- 按用户要求将独立平台弹窗改为左右一体式工作台：左侧为账号列表、文章与图片导入、投递账号和最近任务；右侧为当前平台的官方创作后台。
- 点击左侧微信公众号、头条号、顶端号、百家号、企鹅号或知乎后，右侧切换到对应平台，并继续使用平台独立、可持久化的登录分区。
- 登录、短信、扫码、身份验证、平台声明、预览和最终发布仍由用户手动完成；程序不自动填写平台表单，不点击发布，不读取或导出密码、验证码和 Cookie。
- 增加登录后账号名称自动识别：只读取官方后台页面上可见的账号名称，连续两次高置信度一致后写入账号配置；识别失败时保留“手动修正”入口。
- 使用用户当前真实头条登录态验证成功：从 `https://mp.toutiao.com/profile_v4/index` 识别到“河南业余足球联盟”，候选评分 108，绑定方式更新为 `official-page-detected`；`credentials_stored=false`、`cookies_read=false`。
- 头条账号 `河南业余足球联盟` 已写入多平台账号配置，账号配置校验通过；当前已配置微信公众号“蜂狂篮球”和头条号“河南业余足球联盟”。
- 增加“选择文章”和“选择图片”：文章支持 Markdown/TXT；单个发布包最多导入 30 张 JPG/JPEG/PNG/WebP/GIF 图片，每张不超过 20MB。
- 图片导入时复制到操作箱 `assets/images/` 并写入 `manifest.json`；版权、署名、平台比例和 AI 声明仍要求用户发布前人工核对。
- 隔离测试创建 1 个操作箱并导入 1 张图片，清单资产数、复制文件和风险提醒均验证正确；未写入正式操作箱。
- 桌面源码视觉验证显示左侧 6 个账号入口和右侧头条登录页正常；0.3.0 便携版打包验证显示 6 个平台卡片、2 个已配置账号选项、右侧浏览器渲染和本地接口均正常。
- 已安装 `D:\多平台发文控制台\多平台发文控制台.exe`，文件大小 85,480,327 字节；改版前备份为 `D:\多平台发文控制台\多平台发文控制台-左右工作台改版前-20260726.exe`。
- 正式程序已启动，`http://127.0.0.1:4318/api/dashboard` 返回 HTTP 200；当前监听进程为新版桌面程序。
- 本次未提交、未推送 Git。

## 顶端号登录入口修复（0.3.1）

- 用户提供并确认顶端号官方入口为 `https://mp.topnews.cn/#/index`。
- 旧版只配置了 `https://mp.topnews.cn/`，未携带顶端创作平台单页应用要求的哈希路由，右侧浏览器切换成功但页面显示为空白。
- 同步更新本地服务平台目录与 Electron 官方入口白名单，统一使用完整地址 `https://mp.topnews.cn/#/index`。
- 官方入口 HTTP 访问返回 200；Electron 隔离测试进入 `https://mp.topnews.cn/#/login`，页面标题为“顶端创作平台”，说明前端应用已启动并正确转入登录路由。
- `npm run check` 通过，0.3.1 便携版 EXE 构建成功。
- 已安装 `D:\多平台发文控制台\多平台发文控制台.exe`，文件大小 85,480,552 字节；修复前版本备份为 `D:\多平台发文控制台\多平台发文控制台-顶端号修复前-20260726.exe`。
- 正式程序已重新启动，控制台接口返回 HTTP 200，并回读确认顶端号入口为完整 `#/index` 地址。
- 本次未提交、未推送 Git。

## 多平台发文控制台账号记忆与页面切换修复（0.3.2）

- 根因一：新版多平台内容配置已经升级为“写作适配模板”，旧控制台却仍把登录账号名称写入同一个配置文件，导致内容模板与账号绑定相互覆盖，并出现 `missing folder_name`、`missing adaptation` 等校验信息。
- 根因二：平台切换时旧的 `BrowserView` 没有被明确移除，点击头条号后可能仍显示顶端号页面。
- 将账号登录绑定独立保存到当前 Windows 用户的 `%LOCALAPPDATA%\SxfPublishingConsole\account-bindings.json`；内容适配模板继续由发文技能管理，两类数据不再混用。
- 已迁移并回读当前已知账号：微信公众号“蜂狂篮球”、头条号“河南业余足球联盟”、顶端号“旭升说体育”。百家号、企鹅号和知乎待用户首次登录后识别或确认。
- 每个平台继续使用独立的 Electron 持久化会话分区；关闭或切换页面前刷新会话存储。程序不读取、导出或保存明文密码、验证码和 Cookie。
- 修复 Electron 40 中 `flushStorageData()` 不返回 Promise 却调用 `.catch()` 导致的主进程 JavaScript 弹窗。
- 平台切换时先明确移除旧视图，再挂载目标视图。真实会话分区下完成“头条号 → 顶端号 → 头条号”测试，三次均只有一个右侧视图，地址依次为 `mp.toutiao.com`、`mp.topnews.cn`、`mp.toutiao.com`。
- `npm run check` 通过；0.3.2 便携版 EXE 构建成功。
- 已安装 `D:\多平台发文控制台\多平台发文控制台.exe`，文件大小 85,481,149 字节；替换前版本备份为 `D:\多平台发文控制台\多平台发文控制台-账号记忆修复前-20260726.exe`。
- 正式程序已启动，`http://127.0.0.1:4318/api/dashboard` 返回 HTTP 200；账号名称、顶端号完整入口、操作箱路径和“不保存密码”状态均回读正确。
- 本次未提交、未推送 Git。

## 知乎登录兼容与百家号会话状态修复（0.3.3）

- 用户实测知乎内置登录页出现“10001：请求参数异常，请升级客户端后重试”。公开安全验证页也会把非标准访问转入安全校验，说明知乎当前登录链路不适合嵌入 Electron。
- 按合规边界将知乎改为系统浏览器模式：点击知乎后移除旧的内置平台视图，调用 Windows 默认浏览器打开 `https://www.zhihu.com/creator`，控制台右侧显示清晰的安全验证、登录、核对与手动发布说明；不伪装浏览器、不绕过平台验证。
- 百家号实际已经进入创作后台，但顶部默认只显示头像，旧逻辑因未取得账号名称而持续显示“登录”。
- 将“官方页面已确认登录”和“账号名称已绑定”拆分为两个状态。检测到百家号创作后台的登录元素后，左侧立即显示“已登录，账号名待确认”，按钮改为“打开”；用户展开右上角头像菜单后可继续自动识别，或点击“修正”手动填写真实账号名。
- 补充“头像”“Logo”“切换”等拒绝词，防止把界面控件误存为账号名称。测试中曾产生的“头像”错误绑定已立即删除，正式账号绑定文件未保留该测试值。
- 使用现有百家号独立会话验证：页面为 `https://baijiahao.baidu.com/`，登录信号 `.mp-header-user` 存在，左侧卡片显示“已登录 · 写作规则可用”和“打开”。
- 完成“百家号 → 知乎 → 百家号”切换测试：内置视图数量依次为 1、0、1，返回百家号后地址与会话均正确。
- `npm run check` 通过；0.3.3 便携版 EXE 构建成功。
- 已安装 `D:\多平台发文控制台\多平台发文控制台.exe`，文件大小 85,482,114 字节；替换前版本备份为 `D:\多平台发文控制台\多平台发文控制台-知乎百家号修复前-20260726.exe`。
- 正式程序已启动，`http://127.0.0.1:4318/api/dashboard` 返回 HTTP 200；百家号测试误绑定为空、企鹅号“绿茵豫报”及既有账号绑定保持不变，程序仍不保存密码。
- 本次未提交、未推送 Git。

## 知乎右侧 WebView2 登录容器（0.4.0）

- 按用户确认，将知乎从“只打开系统浏览器”升级为“右侧 WebView2 优先、系统浏览器兜底”。
- 本机已安装 Microsoft Edge WebView2 Evergreen Runtime；使用微软官方 `Microsoft.Web.WebView2 1.0.4078.44` SDK和系统自带 .NET Framework 编译器生成 x64 WinForms WebView2 宿主，无需额外安装完整 .NET SDK。
- 新增 `tools/publishing-console/webview2-host/`：宿主窗口作为 Electron 主窗口的子窗口嵌入右侧区域，跟随主窗口缩放；知乎登录、验证码、内容核对和最终发布仍由用户手动完成。
- WebView2 使用独立持久目录 `%LOCALAPPDATA%\SxfPublishingConsole\WebView2Profiles\zhihu`。控制台不读取、导出或迁移其中的密码、Cookie、验证码和身份资料。
- 右上角增加“浏览器兜底”按钮；WebView2 组件缺失、启动失败或平台临时拒绝加载时，仍可打开系统默认浏览器，右侧登录状态不会被控制台读取。
- 完成“知乎 → 百家号 → 知乎”测试：知乎 WebView2 启动成功并进入 `https://www.zhihu.com/signin?next=%2Fcreator`，切换百家号时隐藏，返回知乎时复用同一个运行实例和登录目录。
- 完成窗口截图验证：知乎登录页完整位于控制台右侧，左侧账号区、顶部状态栏、“浏览器兜底”和“刷新页面”按钮均正常。
- 修复桌面内置服务退出流程，并为 WebView2 宿主增加父进程存活监测；通过 `/api/shutdown` 退出后，Electron 主进程、WebView2 宿主和测试端口均不残留。
- `npm run check`、WebView2 宿主编译、源码运行、便携版运行和辅助进程退出检查均通过。
- 已安装 `D:\多平台发文控制台\多平台发文控制台.exe`，版本 0.4.0，文件大小 85,631,613 字节；替换前版本备份为 `D:\多平台发文控制台\多平台发文控制台-WebView2改版前-20260726.exe`。
- 正式程序已启动，`http://127.0.0.1:4318/api/dashboard` 返回 HTTP 200；等待用户在正式窗口点击知乎并完成首次 WebView2 手动登录。
- 本次未提交、未推送 Git。

## 多平台发文控制台双入口与待改写队列（0.5.0）

- 将旧版“母稿文件路径”升级为两个明确入口：公开文章链接、写作技能输出文件夹。
- 公开链接入口只接受发布后可长期访问的 HTTPS 地址，拒绝 `tempkey`、预览页和草稿链接；创建任务时记录母稿标题、链接和目标平台。
- 技能文件夹入口只复制 `.md`、`.txt`、`.html`、`.docx` 与常见图片，不执行源文件夹里的脚本，不导入技能提示、密码、Cookie、浏览器资料或其他凭据。
- 技能文件夹中的文章和图片被复制到独立操作箱，原始文件保持不变；图片逐张标记为版权、署名、AI声明和跨平台复用权待人工确认。
- 控制台不伪装为内置 AI。创建任务后状态为“待改写”，界面明确提示用户在 Codex 对话中说“处理最新操作箱”；平台改写、Word嵌图和视觉检查完成后才显示“可人工发布”。
- 修复控制台仍调用旧版 `--source` 参数的问题，改为匹配发文技能的 `--source-url`、`--title`、`--accounts`、`--out` 接口；同步修复新版清单使用 `images` 而非 `assets` 的图片记录字段。
- 最近任务与任务详情兼容发文技能 v2 清单：支持 `article_file`、`docx_file`、`queued/adapted/docx_created/validated` 状态，并避免把待改写占位稿误当成可发布正文。
- 增加“打开操作箱文件夹”，便于用户手动取用最终 Word 和图片；右侧官方平台页面仍只负责人工核对、上传和点击发布。
- 为知乎新增独立问答改写模板：结论先行、问题可直接回答、保留证据边界、回应主要异议，禁止虚构亲历和空泛反问。
- 发文技能现在支持稳定公开 URL 或不可变的上游写作技能输出包；五个平台内容模板校验通过。
- `npm run check` 通过；公开链接与技能文件夹脚本分别创建 5 账号测试任务成功，控制台 API 双入口测试成功。
- Electron 隔离视觉测试通过：左侧 6 个平台注册入口、5 个下游改写账号和双入口表单显示正常；测试未写入正式操作箱，未读取或改变正式平台登录态。
- 0.5.0 便携版构建成功并安装到 `D:\多平台发文控制台\多平台发文控制台.exe`，文件大小 85,633,592 字节；替换前 0.4.0 备份为 `D:\多平台发文控制台\多平台发文控制台-0.4.0-双入口改版前-20260726.exe`。
- 安装文件与构建文件 SHA-256 一致；正式程序重启后接口回读版本为 `0.5.0`、平台数为 6、下游改写模板数为 5、人工发布锁定为 `true`，正式操作箱路径保持不变。
- 既有账号绑定配置在程序替换后保持：微信公众号“蜂狂篮球”、头条号“河南业余足球联盟”、顶端号“旭升说体育”、企鹅号“绿茵豫报”；百家号和知乎的浏览器登录会话仍由各自持久目录保管，不因 EXE 替换而清除。
- 本次未提交、未推送 Git。

## 微信公众号已发布文章五平台发布包（2026-07-26）

- 以稳定公开链接 `https://mp.weixin.qq.com/s/uyBXUpQ9-9dNAELCCyuBDw` 为不可变母稿，建立正式操作箱 `20260726-203433-9岁1米85,U9篮球赛还公平吗`。
- 分别为今日头条、百家号、企鹅号、顶端号和知乎完成定向改写；保留事实与来源边界，未把微信公众号小程序卡片、关注引导和尾图带入其他平台。
- 五个平台各生成 1 份包含标题、摘要、分类、标签、正文、真实新闻图片、图片署名和六项资料来源的 Word 发布包。
- Word 发布包共渲染 24 页并逐页检查；标题、正文、图片比例、图片说明、项目符号、清单编号、超链接及分页均正常。
- 图片审计确认每份 Word 均嵌入 3 张行内图片；操作箱最终校验通过，5 个账号状态均为 `validated`，并生成 `COMPLETE` 标记。
- 最终状态仅为“可人工审核发布”；未向任何平台粘贴正文、创建平台草稿或点击发布。
- 本次未提交、未推送 Git。

## “查看发布包”按钮修复（0.5.1）

- 用户点击带标点任务名的“查看发布包”没有反应。根因是服务端任务编号白名单不接受中文或英文逗号，接口返回“任务编号无效”，前端又没有处理点击请求异常。
- 任务编号校验改为允许安全的中文和标点目录名，同时继续拒绝路径分隔符、控制字符、`.`、`..`、多级目录及越界路径。
- 最近任务按钮增加可见错误弹窗；“打开操作箱文件夹”增加成功和失败反馈，避免请求失败时界面无提示。
- 源码语法检查通过；独立端口测试确认目标任务可返回5个平台、5份已验证 Word 发布包，路径穿越请求仍被拒绝。
- 使用新版桌面程序完成真实界面点击测试：“查看发布包”弹窗正常打开，显示5个平台卡片、5个复制标题按钮、5个复制正文按钮和1个操作箱文件夹入口。
- 0.5.1 便携版已安装到 `D:\多平台发文控制台\多平台发文控制台.exe`；安装文件与构建文件 SHA-256 一致。旧版备份为 `D:\多平台发文控制台\多平台发文控制台-0.5.0-查看发布包修复前-20260726.exe`。
- 正式程序已启动并回读版本为 `0.5.1`；目标任务5个平台均处于可人工发布状态。
- 本次未提交、未推送 Git。

## 发布包图片预览与一键准备发布（0.5.2）

- 发布包弹窗原先只展示 Markdown 正文和 Word 路径，虽然图片已嵌入 Word，但用户无法在控制台直观看到图片。
- 发布包新增安全的本地图片预览接口；图片只允许从当前操作箱清单读取，校验任务目录、文件路径和图片格式后返回，不暴露其他本地文件。
- 弹窗新增“文章图片”区域，当前任务5张真实图片均可预览，并明确说明每个平台的 Word 发布包嵌入其中适用的3张图片。
- 每个平台新增“打开带图Word”和“一键准备发布”：前者直接打开对应 Word；后者同时切换到该平台官方创作页并打开带图 Word，供用户手动复制、粘贴、核对图片和点击发布。
- 合规边界保持不变：程序不自动向平台编辑器填表、不代替用户上传图片、不读取 Cookie、不处理验证码、不点击发布。
- 源码语法检查通过；本地与正式程序界面测试均确认弹窗正常打开、5张图片全部加载、5个平台均出现两个发布准备按钮。
- `open-account-docx` 实际调用验证成功，正确打开今日头条带图 Word 发布包。
- 0.5.2 便携版已安装到 `D:\多平台发文控制台\多平台发文控制台.exe`，安装文件与构建文件 SHA-256 一致；旧版备份为 `D:\多平台发文控制台\多平台发文控制台-0.5.1-图片预览改版前-20260726.exe`。
- 正式程序已启动并回读版本为 `0.5.2`，目标任务包含5张可预览图片、5个平台和5份已验证 Word。
- 本次未提交、未推送 Git。

## Word 启动链路修复（0.5.3）

- 用户实测“打开带图Word”和“一键准备发布”仍无反应。诊断确认本地接口返回成功，但后台通过 `explorer.exe` 打开 DOCX 后没有启动 Word，也没有捕获启动失败。
- 桌面程序新增受限文件打开接口，改用 Electron `shell.openPath` 并等待真实系统返回；只允许打开正式操作箱目录内存在的 `.docx` 文件。
- “打开操作箱文件夹”同步改用桌面原生接口，并限制为操作箱根目录的直接任务子目录且必须包含 `manifest.json`。
- “打开带图Word”失败时按钮直接显示“打开失败”并保留系统错误；“一键准备发布”先确认 Word 启动成功，再关闭发布包弹窗并切换平台，失败时弹窗保持并显示“准备失败”。
- Electron 桌面实测确认 Word 进程及可见主窗口真实启动；点击“一键准备发布”后发布包弹窗关闭、对应平台卡片切换为活动状态。
- `npm run check` 通过；0.5.3 便携版已安装到 `D:\多平台发文控制台\多平台发文控制台.exe`，安装文件与构建文件 SHA-256 一致。
- 旧版备份为 `D:\多平台发文控制台\多平台发文控制台-0.5.2-Word启动修复前-20260726.exe`。
- 正式程序已启动并回读版本为 `0.5.3`，最新任务仍为可人工发布且已完成状态。
- 本次未提交、未推送 Git。

## 多平台发文技能 Word 标题门槛补充

- 根据用户对实际 Word 发布包的检查，确认标题段落出现了继承项目符号和手动换行的问题。
- 更新 `publish-multi-account-content` 技能：标题必须使用独立非列表标题段落，禁止编号、项目符号、制表符、前导符号、继承列表属性和手动换行。
- 标题只允许由 Word 根据页面宽度自然换行，目标为一至两行，交付前必须在首张渲染页上按可读尺寸专项检查。
- 同步更新 Word 编辑门槛；技能快速校验通过。
- 本次未提交、未推送 Git。
