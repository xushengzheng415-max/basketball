# 云开发跨账号迁移记录（2026-07-25）

## 迁移范围

- 小程序 AppID：`wx06d735da15276acd`
- 原环境（公司 B）：`cloudbase-d4g93f0re5f3274c1`
- 新环境（公司 A）：`sxf-basketball-d9gp6yt0rd1f7be4d`
- 新环境套餐：体验版，上海地域
- 本地项目：`E:\Documents\sxf-basketball`
- 离线备份：`E:\Documents\SxfCloudBackups\basketball-cloudbase-d4g93f0re5f3274c1-20260725-124840`

离线备份目录位于 Git 仓库外。云函数环境变量、数据库导出和用户数据没有写入 Git。

## 已迁移资源

| 资源 | 迁移结果 |
|---|---|
| 云存储 | 原环境 461 个文件已复制到新环境，路径和文件大小已核对 |
| 云函数 | 19 个函数已部署，运行时、入口、超时、内存和敏感环境变量已按哈希核对 |
| HTTP 云函数 | `sx-beta-registration-bot` 已按 HTTP 类型部署 |
| HTTP 访问服务 | `/sx-beta-registration-bot`、`/sxWxPayNotify` 已创建，鉴权与安全域名配置和原环境一致 |
| 定时触发器 | `sxGeneratePeriodReport` 的 `periodReportQueueWorker` 已恢复 |
| 静态托管 | 原环境 54 个对象均已迁移；新环境另有系统生成的认证图标 |
| 数据库 | 截至 2026-07-25 15:47，11 个集合、282 条文档已迁移；源端与目标端逐条内容比对全部通过 |
| 数据库权限与索引 | 11 个集合均已恢复并回读验证，权限和索引全部匹配 |
| 云函数安全规则 | 已迁移并通过规则哈希比对 |
| 云存储权限 | 已恢复为 `PRIVATE` |

## 必要的目标环境改写

- 小程序和云函数中的环境 ID 已切换为新环境。
- 云存储 `cloud://` 前缀已切换为新桶。
- 静态托管的 `cloud-admin/index.html` 已改为初始化新环境。
- 微信支付回调地址已切换到新环境的 `/sxWxPayNotify`，回调入口已实测可达。
- 数据库文档中的旧云存储前缀在写入新环境时同步改写。

## 验证记录

- `npm run build:weapp`：通过。
- 原生小程序、云函数和迁移工具的 JavaScript 语法检查：通过。
- 131 个 JSON 文件解析检查：通过。
- WXML 复杂表达式专项检查：无命中。
- 静态托管首页和云后台页面：HTTP 200；页面中不再引用旧环境。
- 微信支付回调路径：HTTP 200。
- 数据库：11 个集合，源端 282 条、目标端 282 条，逐条内容核验全部通过。
- 数据库权限与索引：11 个集合回读核验全部通过。
- 目标环境云函数：19 个正式函数；临时数据导入函数和临时云存储快照已删除。
- `sxGetAudioLibrary` 目标环境只读冒烟调用：执行成功，无函数错误。
- `npm run typecheck`：未通过，现有 `config/index.ts` 的 `import.meta` 与 TypeScript `module` 配置不兼容；该问题与本次环境迁移无关，正式构建不受影响。

## 切换边界

旧环境 `cloudbase-d4g93f0re5f3274c1` 已于 2026-07-25 完成释放，回读确认公司 B 账号下已无可用云开发环境。后续切换步骤为：

1. 解绑小程序与公司 B 腾讯云账号。
2. 使用管理员微信扫码，将小程序绑定到公司 A 腾讯云账号。
3. 关联新环境，上传体验版并完成登录、读取、写入、支付回调和静态页面冒烟测试。

旧环境已不可恢复；仓库外离线备份仍保留。
