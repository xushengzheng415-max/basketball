# 开发日志｜企微外部群篮球机构晨报

> 日期：2026-08-25  
> GitHub Issue：[#8](https://github.com/xushengzheng415-max/basketball/issues/8)  
> 当前工作区分支：`codex/education-2.0`

## 一、完成内容

- 确认企微外部客户群的官方边界：自建应用只能创建客户群群发任务，群主仍需在企微客户端确认发送。
- 新增独立服务 `cloudfunctions/sxWecomGroupBriefing/`，监听 `127.0.0.1:3400`，不修改小程序、PC 后台或既有企微服务。
- 接入 OpenAI Responses API、网页搜索和严格 JSON Schema，生成 2—3 条篮球赛讯、1 条培训机构观察和 1 个当天行动。
- 新增来源白名单、搜索来源回溯、日期窗口、双来源、内容字节数、隐私、广告、夸大和提示注入校验。
- 新增企微客户群列表、群详情、创建群发任务、提醒、任务状态和实际发送结果查询。
- 强制显式配置群主和客户群 ID，固定 `allow_select=false`，禁止默认面向全部客户群。
- 新增日期与投递目标双重幂等、创建结果未知保护、明确拒绝后的人工重置、提醒间隔和 41063 退避。
- 新增仅本机访问的中文管理页和 HTTP Basic 鉴权。
- 新增 Docker Compose、0600 环境文件模板、部署脚本及服务器操作说明。
- 安全配置助手支持选择 DeepSeek 或 OpenAI；DeepSeek 使用官方 Responses API 的 `deepseek-v4-flash`、网页搜索和结构化输出。
- 新增实施方案、内容规则和独立契约测试。

## 二、新增文件

- `cloudfunctions/sxWecomGroupBriefing/`
- `tools/test-wecom-group-briefing.js`
- `docs/企微外部群篮球机构晨报实施方案.md`
- `docs/企微外部群篮球机构晨报内容规则.md`
- `ops/lightserver/docker-compose.briefing.yml`
- `ops/lightserver/wecom-group-briefing.env.example`
- `ops/lightserver/deploy-wecom-group-briefing.sh`
- `ops/lightserver/configure-wecom-group-briefing.sh`
- `ops/lightserver/README-wecom-group-briefing.md`
- `开发日志_2026-08-25-企微外部群篮球机构晨报.md`

## 三、验证结果

- `node tools/test-wecom-group-briefing.js`：通过。
- `npm run check --prefix cloudfunctions/sxWecomGroupBriefing`：通过。
- 中文文件 UTF-8 无 BOM 检查：通过。
- 本地进程烟测：`/health` 返回 200；未认证管理接口返回 401；正确 Basic 认证返回 200。
- 服务器 Compose 配置检查、镜像构建和健康检查：通过。
- 本地与服务器应用文件 SHA-256：逐项一致。

## 四、服务器部署

- 服务器：`api.saixiaofeng.com`（固定公网 IP `124.221.239.63`）。
- 应用目录：`/opt/sxf-platform/apps/wecom-group-briefing`。
- 配置文件：`/opt/sxf-platform/config/wecom-group-briefing.env`，权限 `0600`。
- 状态目录：`/opt/sxf-platform/data/wecom-group-briefing`，权限 `0750`。
- 容器：`sxf-wecom-group-briefing`，当前状态为 healthy。
- 本机端口：`127.0.0.1:3400`。
- 当前安全状态：`SERVICE_ENABLED=false`、`SCHEDULER_ENABLED=false`、`AUTO_CREATE_ENABLED=false`、`AUTO_REMIND_ENABLED=false`。
- 既有 3100、3200、3300 服务保持运行，未修改现有 Nginx。
- 企微域名校验文件 `WW_verify_h02jneGOQJzpr9Pk.txt` 已在 `https://api.saixiaofeng.com/` 根路径公开；公网返回 200，16 字节内容与用户下载文件逐字节一致。服务器原有精确 Nginx 路由和文件已正确配置，本次未覆盖。
- 用户确认晨报时间为每天北京时间 09:00；待确认提醒时间顺延为 10:00。
- DeepSeek 与企微鉴权均已验证成功；`ZhengXuSheng` 名下读取到 7 个客户群，唯一测试目标设为“赛小蜂｜篮球机构经营与教学交流”（111人）。
- 首次 DeepSeek 自动预览因来源时效、HTTP 链接和正文超长被安全门禁拦截，未创建任务；随后使用两项当日 HTTPS 权威来源创建了短版测试晨报待确认任务，状态回读为 `pending`、目标群数 1、无部分失败。
- 用户确认群内文案不带链接、减少编辑说明并增加适量表情符号。第一版测试任务已通过企微官方停止群发接口撤销；第二版曾使用“🔥今日快讯、👀机构观察、✅今天可做、📌来源名称”结构，后续按用户反馈继续调整并撤销。
- 用户进一步确认：固定3条新闻、删除“今天可做”、删除信息来源、机构观察不说教。第二版任务已撤销；第三版任务只包含“🔥今日快讯”3条和“👀机构观察”1段，目标群仍为唯一测试群，已创建并等待群主确认。
- 2026-08-26 复查发现正式调度、自动建任务和提醒开关仍为关闭，导致09:00没有生成任务。三项开关已正式开启。补跑第1次因 DeepSeek JSON 外层格式失败，第2次解析成功但被 HTTP 来源和超时来源门禁拦截；已增加 DeepSeek 代码块兼容、旧 HTTP 来源升级为 HTTPS、观察固定双来源及说教/经营因果拦截。随后以当日3条合格新闻创建补发任务，并成功触发1次群主提醒。
- 2026-08-27 09:00 调度准时运行并生成3条新闻，但一条人民网搜索来源为 HTTP、正文引用被规范为 HTTPS，导致来源追溯误判而未建任务。已统一搜索来源与正文 URL 的 HTTP→HTTPS 规范化，新增安全重验流程；09:14 使用原始自动生成内容重验通过并创建当天待发送任务，唯一目标群1个。

## 五、已知边界

- 系统确认引用 URL 来自本次网页搜索，并校验模型给出的日期窗口，但未独立抓取每个原文页面证明全文和发布日期。
- 初次联调必须保持 `AUTO_CREATE_ENABLED=false`，由运营打开原文核对；群主确认仍是最终事实关口。
- JSON 状态和进程锁只支持单实例，不得把该 Compose 服务横向扩容。
- 当前工作区已有大量其他任务的未提交改动，且停留在 `codex/education-2.0`。为避免搬动或混入这些改动，本次没有切换分支、提交或推送；所有本次文件保持独立新增。

## 六、待完成配置

1. 在企微管理后台创建“赛小蜂篮球晨报”自建应用。
2. 将该应用加入“客户联系 → 可调用接口的应用”，并把目标群主加入应用可见范围。
3. 将固定公网 IP `124.221.239.63` 加入企业可信 IP。
4. 由服务器管理员直接填写 `wecom-group-briefing.env` 中的企微 Secret、OpenAI Key、群主 UserID、目标客户群 chat_id 和管理令牌；不得粘贴到聊天或提交仓库。
5. 先用一个测试客户群手动生成、创建任务、确认发送并回读结果。
6. 测试通过后依次开启调度、自动创建和可选提醒。

## 七、下一步

- 完成企微后台授权和服务器密钥配置。
- 执行首个测试群联调并记录 `msgid`、群主确认状态和实际发送结果。
- 连续人工核验 3—5 天内容后，再决定是否开启 `AUTO_CREATE_ENABLED=true`。
