# 2026-08-14 开发日志

## PC 赛事中心：现场人员与比赛任务逻辑收敛

- 现场人员名单改为赛事级手动维护：主办方通过“新增现场人员”建立计分员、记录员或 MC，支持内部教练与外请人员来源。
- 比赛任务不再手写或预置人员；保存后的竞赛日程自动生成每场任务行，任务只记录日期、阶段、轮次、组别、场地和岗位绑定状态。
- 每场任务可分别绑定计分员、记录员和 MC；同一人员可跨场次动态复用，外请人员仍只获得被分配场次的权限。
- 移除首场默认人员数据，避免出现“第一场已完成、其余场次待分配”的混乱假象；没有现场人员时，绑定入口会提示先建立名单。
- 裁判名单继续独立维护，裁判在名单完成后按比赛绑定 1 至 3 人并直接发送任务；普通业务赛事不会自动创建虚拟裁判。

## 验证

- `node --check admin/tournament-center.js` 通过。
- `node --check tools/test-tournament-onsite.js` 通过。
- `node tools/test-tournament-onsite.js` 通过：赛程任务载入、日期/组别筛选、真实场次人员绑定、绑定保存、裁判名单及 1–3 人裁判组流程均通过。

## 裁判/现场人员岗位能力

- 新增人员弹窗改为岗位能力多选：计分员、记录员/数据员、MC。
- 同一人员可同时拥有多个岗位能力；保存后同步进入现场人员名单，并出现在对应场次的绑定下拉中。
- 裁判名单表格和详情改为展示可承担岗位，不再依赖单一自由文本“裁判资质”。
- 新增多岗位人员的交互回归断言已通过。

## 现场人员维护与任务查看

- 人员列表新增编辑、删除操作，编辑支持重新选择多岗位能力。
- 点击人员后展示该人员按岗位能力可承接的未分配工作任务，可直接进入对应场次安排。
- 删除人员会同步清理人员名单；已绑定的现场任务恢复为待分配，裁判组任务标记为待重新发送。
- 现场执行回归新增人员任务查看、编辑同步、删除同步三项断言，全部通过。

## 现场任务页文字可读性

- 放大当天任务表表头、时间、场次/阶段、场地、人员状态、来源标签和操作文字。
- 同步提升右侧人员分配卡片、筛选控件和赛程载入提示的字号，保持任务列表高度和滚动行为稳定。

## 现场执行入口收敛

- 移除“技术台人员绑定”作为现场执行独立子页面，避免与“人员与任务”重复维护同一套岗位绑定逻辑。
- 工作台原“管理绑定”入口改为直接进入“人员与任务”；旧 `#onsite/bindings` 链接自动规范到人员任务页。
- “球员数据任务”和“控制台状态”仍作为独立的运营/设备查看页面保留。
- 回归验证新增旧入口合并断言，全部通过。

## 组别筛选与现场操作人数

- 人员与任务页的组别筛选统一按赛事组展示：`U8 启蒙组 · A组`、`U8 启蒙组 · B组` 会合并为 `U8 启蒙组`，表格明细仍保留子组信息。
- 每场任务新增 1/2/3 人操作配置并持久化到 `onsiteAssignments[matchId].operatorCount`：1 人仅显示计分员，2 人显示计分员 + MC，3 人显示计分员 + 记录员/数据员 + MC。
- 操作人数会参与准备状态计算；仅球队模式下记录员/数据员不作为必需席位，避免无球员数据赛事被错误判定为人员不足。
- 控制台状态的“查看”按钮改为打开设备连接详情，展示所属场次、设备、绑定人员、连接延迟、权限范围和 PC 只读边界。

## 本次验证

- `node --check admin/tournament-center.js` 通过。
- `node tools/test-tournament-onsite.js` 通过：组别合并筛选、1 人/3 人操作模式、任务绑定保存、控制台详情查看、仅球队模式隐藏球员数据任务和旧入口跳转均通过。
- 更换岗位弹窗新增岗位下拉，可在计分员、记录员/数据员和 MC 之间切换；切换后会同步刷新可选人员和权限说明，确认时会清理原岗位绑定，避免同一席位残留。
- 现场执行回归新增“更换岗位提供岗位下拉菜单”断言并通过。

## 控制台状态改为真实赛程模拟

- 控制台状态不再读取旧的 G1001/G1003 占位数据，改为读取当前赛事已保存赛程（如 M001）和每场操作人数配置。
- 每场按 1/2/3 人模式生成主控计分台、球员数据台和 MC 控制台席位；已绑定人员显示“在线（模拟）”，未绑定显示“待绑定”。
- 仅球队模式自动隐藏球员数据台；无赛程时显示引导状态，而不是虚构设备。
- 控制台详情弹窗读取当前席位的真实场次、设备模拟编号、绑定人员、权限和模拟延迟；刷新按钮会重新计算当前席位数量。
- 回归验证新增真实赛程控制台、仅球队模式隐藏数据台断言，全部通过。

## 赛果与数据闭环

- 新增“赛果与复核”真实赛事页：读取已保存竞赛日程，按场次展示组别、阶段、轮次、场地、队徽、比分、来源与复核状态。
- 后台可逐场录入或修正主客队比分，保存时可标记比赛结束；每次修改追加操作人、时间、前后比分与原因，不覆盖现场回传记录。
- “淘汰赛晋级”不再作为独立结果标签，已合并到赛果复核页底部，半决赛、三四名和决赛按已复核比分同步展示晋级关系。
- 新增“赛事战报”页：提供橙焰赛事、白底简报、深色战报三种代码渲染版式，支持按比赛日预览、二维码/标题版面设置及独立打印预览。
- “积分与球队”改为读取已复核小组赛赛果动态计算胜/平/负、得分、失分、净胜分和积分；未复核或退回的赛果不参与排名。
- `admin/tournament-center.html` 资源版本更新为 `20260814-128` / `20260814-84`，避免浏览器继续使用旧缓存。

## 赛果回归验证

- `node --check admin/tournament-center.js` 通过。
- `node --check tools/test-tournament-onsite.js` 通过。
- `node tools/test-tournament-onsite.js` 通过：真实赛程载入、现场回传、后台修正并结束、单场复核、图片战报、二维码版面保存及既有现场执行回归均通过。

## 赛果列表版式调整

- 赛果列表的对阵列改为左右队徽 + 队名 + VS 的横向结构，避免两个球队卡片在单元格内上下挤压。
- 增大场次行高、比分与来源标签的可读性，重新分配组别、对阵、比分、时间、来源、状态和操作列宽度。
- 在较窄后台窗口自动将详情面板放到列表下方，并保留列表横向完整展示，避免操作按钮和比赛信息被截断。
- 比分不再单独占一列，改为放在两队队名与队徽的正中间，中心同时显示比分和 VS，表格列数进一步收敛。
- 赛果列表中心移除 VS：已完成场次显示 `主队比分 : 客队比分`，未录入场次显示 `- : -`。
- 淘汰赛赛果不再单独渲染晋级卡片，半决赛、三四名和决赛与小组赛共用上方赛果列表，通过阶段/轮次字段区分。
- 赛果列表头部新增“快速选择场次”下拉，完整列出全部比赛并可直接切换右侧复核详情，避免列表下方场次不可见。
- 赛果列表移动到统计卡片之前，统计卡片压缩为 58px 紧凑栏；列表面板改为独立滚动并固定表头，保证全部场次可浏览。

## 赛制驱动的积分榜与赛果矩阵

- “积分与球队”不再用一套固定汇总逻辑：先读取每个组别的赛制配置，再按可排名阶段生成数据。
- 小组循环赛按实际子组拆分积分榜与赛果矩阵，例如 U8 启蒙组的 A 组、B 组分别统计；晋级组单循环等后续循环阶段也单独成表。
- 半决赛、三四名、决赛及其他淘汰阶段不进入小组积分或小组矩阵，只在“赛果与复核”中承担晋级和最终名次。
- 胜/平/负积分读取组别赛制中的动态配置，矩阵图例、积分榜和排名规则弹窗同步显示当前规则，不再固定写死胜 2 分。
- 新增赛制摘要、统计范围和晋级关系提示，明确“循环赛计分、淘汰赛晋级”的边界。
- 回归验证新增 A/B 分组、动态积分规则和淘汰赛排除断言，全部通过。
- `admin/tournament-center.html` 资源版本保持为 `20260814-129` / `20260814-85`。

## 赛果复核内置淘汰赛对阵图

- 淘汰赛对阵图直接并入“赛果与复核”页，不新增顶部导航标签；读取当前赛程中的半决赛、三四名和决赛场次。
- 对阵图按组别和阶段分列展示，卡片显示场序、队徽、队名、比分、比赛时间、场地和复核状态；待录入场次显示 `- : -`。
- 赛果复核通过后，对阵卡片同步变为已确认状态；晋级关系继续沿“场序胜者 / 场序负者”承接，淘汰赛不进入积分榜。
- 修正积分矩阵同队对角单元格：使用单元格内独立斜线，避免斜杠文字溢出成跨表格的错误长线。
- `admin/tournament-center.html` 资源版本更新为 `20260814-130` / `20260814-86`。

## 积分矩阵连续对角线与淘汰赛入口

- 积分矩阵同队单元格改为由矩阵容器绘制一条连续的左上至右下对角线，不再按单元格拼接斜杠。
- “积分与球队”页新增“查看淘汰赛赛果”入口，直接跳转到已合并淘汰赛对阵图的“赛果与复核”页；不新增独立顶部标签。
- 资源版本更新为 `20260814-130` / `20260814-86`。

## 积分矩阵连续对角线方向修正

- 将连续对角线从错误的左下至右上调整为左上至右下，符合赛果矩阵常规阅读方向。
- CSS 资源版本更新为 `20260814-87`，避免旧方向样式继续命中浏览器缓存。

## 已录入赛果即时试算积分

- 小组循环赛比分保存后立即同步更新积分榜和赛果矩阵，不再等待主办方完成复核才显示。
- 待复核赛果以“试算”状态计入当前排名；复核通过后转为正式积分，退回或异常赛果自动排除。
- 积分榜球队行显示已复核场次与待复核试算场次，矩阵比分同步标注试算状态，避免与正式发布结果混淆。
- 资源版本更新为 `20260814-131` / `20260814-88`。

## 赛果复核定位与淘汰赛晋级图归位

- 保存或修正比分后，同时恢复赛果列表内部滚动位置和页面位置，并保持刚处理的场次为当前选中项，不再跳回列表第一场。
- 待复核场次在列表操作列直接显示“复核通过”按钮；详情区仍保留复核备注、退回修改和确认赛果能力。
- 淘汰赛晋级图从“赛果与复核”移至“积分与球队”，使赛果页专注录入复核，积分页统一承载积分榜、赛果矩阵与晋级关系。
- 半决赛到决赛、三四名赛改为蛇形分支连线：胜者线进入决赛，负者线进入三四名赛；卡片继续读取真实场序、队徽、比分、时间和场地。
- 修复测试模式在当前已位于赛果页时“模拟现场回传”不触发重绘的问题。
- `node --check admin/tournament-center.js`、`node --check tools/test-tournament-onsite.js`、`git diff --check` 与 `node tools/test-tournament-onsite.js` 均通过。
- 资源版本更新为 `20260814-132` / `20260814-89`。

## 后台补录免复核

- 赛果列表移除独立“复核状态”列和重复的“待复核”文字；现场回传比分在操作列直接显示“复核”按钮。
- PC 赛事后台录入或修改比分时，由于操作者本身是赛事管理员，保存后自动标记已通过并直接进入正式积分与战报，不再要求二次复核。
- 既有记录来源为“PC后台 / 赛事后台”的已录入比分在读取时按已生效处理，避免历史后台补录数据继续卡在确认环节。
- 现场计分台回传仍保留人工确认边界；确认前以试算积分展示，点击“复核”后转为正式积分。
- 赛果列表重新收敛为六列，操作列显示“复核 / 已复核 / 已生效”，并继续保留保存后的场次和滚动位置。
- 回归验证覆盖后台免复核、现场按钮复核、状态列移除、积分试算及正式积分同步，全部通过。
- 资源版本更新为 `20260814-133` / `20260814-90`。

## 未录入场次状态修正

- 修复未录入比分场次错误落入“已复核”展示分支的问题。
- 赛果操作改为显式三态：未录入仅显示“录入比分”；现场已回传显示“修改比分 + 复核”；已确认或后台录入显示“修改比分 + 已复核 / 已生效”。
- 新增“未录入比分场次不显示已复核”回归断言，完整现场执行与赛果测试通过。
- JS 资源版本更新为 `20260814-134`。

## 赛果矩阵胜方比分强调

- 赛果矩阵将每个比分拆分为球队得分、分隔符和对手得分，较高的胜方得分使用赛小蜂橙色强调，负方得分保持白色。
- 平局不强制标记胜方；现场待确认比分仍保留试算标签，但不影响胜负颜色判断。
- 新增胜方得分颜色差异回归断言，完整测试通过。
- 资源版本更新为 `20260814-135` / `20260814-91`。

## 球队认领虚拟球队与青少年演示数据

- 在“报名与资格”的“入驻进度”和“球队认领”顶部新增“添加虚拟球队”入口。
- 新增 16 支原创虚构青少年球队，覆盖 U8、U10、U12、U14；每队 12 人，共 192 名虚拟球员。
- 虚拟球队写入当前赛事时保留用户原有球队，自动按当前组别平均分配，并标记为已认领、测试免审核，可用于抽签、排赛、计分和球员数据流程。
- 每支虚拟球队提供原创队徽和真人摄影感的虚构球员头像；同队球服版型、领口、纹样固定，号码统一使用同一字体、描边、字号与胸前位置，号码范围为 4–15。
- 虚拟球队详情弹窗展示 12 人名单、头像、号码、位置、年龄、身高和体重；再次点击入口可刷新整套资料或整批移除，整批移除不影响真实球队。
- 原始数据包位于 `演示数据/青少年篮球16队/`；后台使用压缩后的 256px 队徽和 320px WebP 头像，避免原始 PNG 增加页面载入体积。
- 数据与页面均使用 `isVirtual`、`isDemo` 和 `virtualDatasetId` 隔离，不提供真实手机号，不进入真实邀请、保险、支付或身份审核。

## 虚拟球队验证

- 当前分支：`codex/education-2.0`；工作区存在其他连续任务改动，本次未切换分支、未提交、未推送。
- `node --check admin/tournament-center.js` 通过。
- `node tools/validate-youth-demo-data.js` 通过：16 队、192 人、16 枚队徽、192 张头像、号码与性别分布、图片尺寸和文件哈希均符合约束。
- `node tools/test-tournament-virtual-teams.js` 通过：入口、导入提示、保留真实球队、16 队/192 人写入、组别平均分配、队徽和头像加载、12 人名单弹窗、刷新持久化与整批移除全部通过。
- 浏览器验证截图及机器报告输出到 `outputs/虚拟球队验证/`。
- 赛事中心资源版本更新为 JS `20260814-134`、CSS `20260814-90`，虚拟球队数据模块版本为 `20260814-1`。

## 虚拟球队改为按需分批添加

- “添加虚拟球队”不再一次写入全部 16 支球队，改为先打开球队库，由赛事运营人员点击卡片选择需要的球队。
- 默认不选择任何球队；支持单选或多选，单次最多添加 4 支，确认按钮实时显示“添加所选 N 支球队”。
- 已经写入当前赛事的球队标记为“已加入”并禁止重复选择；关闭后可再次进入球队库继续添加。
- 添加时保留用户原有真实报名球队，并将新选球队优先分配到球队较少的组别；“移除全部虚拟球队”仍只删除演示球队。
- 选择弹窗改为固定头部、可滚动内容区和固定底部操作区，避免球队较多时确认按钮落到屏幕之外。

## 分批添加验证

- `node --check admin/tournament-center.js` 与 `node --check tools/test-tournament-virtual-teams.js` 通过。
- `node tools/test-tournament-virtual-teams.js` 通过 18 项：默认空选、单队添加、单次最多 4 支、已加入锁定、继续添加 2 支、刷新持久化、真实球队保留和整批移除边界均符合预期。
- `node tools/validate-youth-demo-data.js` 通过：16 支球队、192 名球员、16 枚队徽、192 张唯一头像均完整。
- 已重新生成并检查 `outputs/虚拟球队验证/虚拟球队-选择添加.png`，底部操作按钮在弹窗内持续可见。
- 赛事中心资源版本更新为 JS `20260815-136`、CSS `20260815-92`，避免浏览器继续使用一次性导入的旧缓存。

## 虚拟球队扩充为每组 8 支

- 在原有 U8、U10、U12、U14 各 4 支球队基础上，每个年龄组新增 4 支，当前共 32 支球队、384 名球员，每组正好 8 支。
- 新增 16 支球队均提供原创队徽和 12 名真人摄影感虚构儿童头像；同队使用固定球服版型、主辅色、领口、胸前纹样、号码字体、描边颜色和号码位置。
- 头像继续通过 4×3 母版统一裁切为 320×320 文件，并由生成流程叠加 4—15 号，避免号码样式随图像生成结果变化。
- 演示数据目录由 `演示数据/青少年篮球16队/` 更新为 `演示数据/青少年篮球32队/`；后台压缩素材同步扩充为 32 枚 256px 队徽和 384 张 320px WebP 头像。
- 虚拟球队选择器新增“全部 / U8 / U10 / U12 / U14”筛选，每个年龄组明确显示 8 支；仍维持默认空选和单次最多添加 4 支的规则。
- 新数据集标识更新为 `sxf-youth-32-v2`，同时兼容并可清理旧的 `sxf-youth-16-v1` 已导入记录。

## 32 支球队验证

- `node tools/validate-youth-demo-data.js` 通过：32 支球队、384 名球员、U8/U10/U12/U14 各 8 支、32 枚队徽、384 张唯一头像全部符合约束。
- `node tools/test-tournament-virtual-teams.js` 通过 20 项：32/384 数据展示、年龄组筛选、按需添加、单次上限、重复锁定、刷新持久化和移除边界均正常。
- 新增 16 队头像母版总览输出到 `outputs/虚拟球队验证/新增16队头像母版总览.webp`，已人工检查队服区分与同队一致性。
- 赛事中心资源版本更新为数据 `20260815-2`、JS `20260815-138`、CSS `20260815-94`。
