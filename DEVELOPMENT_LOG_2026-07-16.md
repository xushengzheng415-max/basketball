# 赛小蜂篮球开发日志 — 2026-07-16

## 一、当日目标

- 完成教练端教务管理 2.0 体验链路的集中迭代。
- 修复工作台、赛事、登录、课程与横屏计分板的关键体验问题。
- 将本次成果整理为 `1.5.1` 体验版本，同步 GitHub、Gitee、微信开发者工具与飞书版本记录。
- 固化“代码发布 + 飞书对外公告 + 开发交接”的标准版本流程。

## 二、教务与教练端

1. 新增教练教务工作台、我的班级、我的课程、课堂详情、课程计划、学员名单、课堂点名、课堂执行、待完成计划等页面链路。
2. 补齐一级、二级、三级页面的返回关系；一级页面保留统一六栏底部导航，业务详情页按页面栈返回。
3. 课程计划支持训练模块模板、自定义模块、模块编辑、删除与拖拽排序。
4. 点名完成后进入课堂执行流程，课程完成后再进入学员课后评价，避免点名后直接跳入单个学员评价。
5. 学员评价采用多维五星体系，支持半星评分、综合分值、标签化日报与教练修改。
6. 新增学员历史课后评价、预约下一课、周报/月报提醒与周期报告中心。
7. 新增 `sxGeneratePeriodReport` 云函数，用于结合训练日报辅助生成周报和月报草稿。
8. 登录与账号同步链路补充手机号授权失败兜底，并继续使用现有云端球员/球队同步能力。

## 三、工作台与赛事体验

1. 工作台设置为首页，保留快速比赛和教务系统专业版付费解锁区域，并适配不同高度的手机屏幕。
2. 专业版广告卡片改为与首页深色视觉一体化，放大重点文案，补充多校区协同、教练工作端、PC 管理后台和经营价值说明。
3. “查看方案”进入独立专业版内容页；付费解锁入口接入企业微信客服，不再使用复制链接弹窗作为主要交互。
4. 最近比赛内容从赛事页独立列表中移除，统一归入“全部比赛 / 未结束 / 已完成”筛选结果。
5. 优化赛事页顶部不透明背景、卡片高度和首页赛事统计入口。

## 四、横屏计分板

1. 修复从首页快速比赛进入比赛设置后，开始比赛出现黑屏的问题。
2. 移除固定的罚球、两分、三分按钮，主客队仅保留醒目的 `-` 和 `+` 单步计分按钮；多分通过连续点击完成。
3. 优化累计犯规与暂停区域为独立操作方块，放大标签、数字和按钮，避免挤压。
4. 保留时间、节次、交换场地、撤销、结束比赛与 MC 音效能力，并调整横屏空间层级。
5. 拆分计分板基础样式和覆盖样式，保持原有计分逻辑的同时提升横屏可读性。

## 五、导航与安全区

1. 六栏底部导航统一为：工作台、赛事、球员、教务、数据、我的。
2. 补齐六栏导航图标及选中态素材。
3. 所有自定义导航页面必须避开微信右上角胶囊区域；标题、日期、统计数字和操作按钮不得与胶囊重叠。
4. 胶囊安全区规则已写入 `AGENTS.md`，后续页面默认按窄屏 iPhone 竖屏验证。

## 六、版本与仓库

- 正确版本号：`1.5.1`。
- 当前分支：`codex/education-2.0`。
- 版本提交：
  - `720f06a`：准备教务 2.0 体验版本及相关功能更新。
  - `1c1e8b3`：将误写的 `1.51` 更正为标准语义版本 `1.5.1`。
- GitHub `origin` 与 Gitee `gitee` 已同步到 `1c1e8b3e097f9e096e9a77f6ce9fc5e2c9d97163`，当时验证领先 `0`、落后 `0`。
- 微信开发者工具已成功上传 `1.5.1` 开发/体验候选包，AppID 为 `wx06d735da15276acd`。
- 发布边界：当前只走体验版，不提交正式审核，不发布正式版本。
- 本地排除项：`service-account-h5/`、飞书授权二维码截图和 Python 缓存文件未纳入版本提交。

## 七、飞书版本登记

### 多维表格

- Base：`https://lxcmobp6gun.feishu.cn/base/X90rb0D18a9J8isi5OfcykK5nZe`
- 版本表：`版本更新记录`（`tblvciPEwoSstRny`）
- 版本记录：`recvpx7PYuLggl`
- 版本号：`1.5.1`
- 发布状态：测试中
- 验证结果：待验证
- 更新标题：教务管理、球员成长与比赛计分体验升级
- 更新目录已改为对外公示的七项编号内容，不包含分支、提交号和内部技术细节。

### 对外知识库公告

- 目标标题：`赛小蜂篮球 1.5.1 版本更新公告`
- 内容标准：面向客户与合作伙伴，包含版本号、更新时间、体验阶段、七项用户可感知更新、体验说明及客服反馈入口。
- 目标节点已定位：知识库根节点“赛小蜂篮球”，节点 token 为 `UnB6wTejMiCwrhkScRhcQCOInnb`，空间 ID 为 `7595063276883512505`。
- 当前状态：公告尚未创建。在已定位的根节点下创建子文档仍需用户权限 `wiki:space:read`；如需重新列出全部知识空间，则另需 `wiki:space:retrieve`。
- 下一步：授权 `wiki:space:read` 后，直接在已定位的根节点下创建公告，并通过文档回读验证标题和正文，无需重新查找知识库。

## 八、对外公示更新目录

1、更新教练教务工作台，新增我的班级、今日教学任务和教学提醒等快捷入口。

2、更新课程管理流程，支持按日期查看课程，并可进入课堂详情、课程计划和学员名单。

3、更新备课与课堂执行，支持训练模块模板、自定义模块、拖拽排序、课堂点名及上课流程记录。

4、更新学员评价与成长记录，支持多维星级评价、训练日报、历史课后评价和下节课预约。

5、更新周期报告能力，可根据日常训练记录辅助生成球员周报与月报，减少重复整理工作。

6、更新比赛计分体验，修复比分板异常，优化单步加减分、犯规和暂停操作。

7、更新工作台与导航体验，优化快速比赛、赛事记录、专业版介绍及客服咨询入口。

## 九、下次对话接续点

1. 先阅读 `AGENTS.md` 与本日志，不重复梳理今日已完成内容。
2. 授权飞书用户权限 `wiki:space:read`；目标根节点和空间 ID 已记录在上文。
3. 完成《赛小蜂篮球 1.5.1 版本更新公告》的知识库创建与回读核验。
4. 继续进行 `1.5.1` 真机体验验收；仅记录问题并按用户指定页面范围修改。
5. 未获明确授权前，不提交正式版审核，不发布生产版本。
