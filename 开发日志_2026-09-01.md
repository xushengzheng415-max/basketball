# 开发日志（2026-09-01）

## 计分板音效跨账号播放修复

- 问题：音频云存储原规则为“仅创建者和管理员可读写”，导致上传/管理账号可以播放，其他微信账号首次进入计分板时无法读取进攻、防守音乐及部分音效。
- 线上修复：云存储改为自定义规则，现有 21 个 `mc-mp3/` 公共 MC 音频文件允许读取；其他文件继续仅归属用户可读，所有客户端写入仍仅限文件归属用户。
- 播报稳定性：`sxCreateScoreVoice` 云函数超时从 3 秒调整为 15 秒，避免腾讯云语音合成偶发超过 3 秒后被中断。
- 版本边界：小程序版本仍为 `2.1.0`，未递增版本号，未上传小程序、未提交审核、未正式发布，也未推送 Git 远端。
- 验证：已核对线上 MC 音频共 21 个且目标文件存在；已回读云存储权限为自定义规则；已回读语音云函数配置。

## 未决与下一步

- 请使用一个此前未播放过音效的普通微信账号，在真机计分板依次测试进攻、防守、2 分、3 分、蜂鸣器、倒计时及“播报当前比分”。
- 云存储权限规则通常需要 1—3 分钟生效；如立即测试仍失败，退出小程序后重新进入再测。

## “我的”页版本入口

- 在退出登录下方新增“版本 2.1.4 · 查看更新日志”和“由赛小蜂篮球提供技术支持”。
- 点击版本入口后展示可滚动的历次更新日志，当前收录 2.1.4、2.1.3、2.1.2、2.1.1、2.1.0、2.0.3、2.0.1 的用户可见更新摘要。
- 用户确认当前已上传版本为 2.1.4，上传备注为修补计分板音频文件播放失败；截图显示上一线上版本为 2.1.3。
- 本地项目与依赖锁定文件的版本字段已同步为 2.1.4；本次未重复上传小程序、未提交审核或正式发布，审核与正式发布状态仍以微信后台为准。

## 2.1.4 比分播报无声修复

- 线上复测确认 `sxCreateScoreVoice` 返回 `AuthFailure.SecretIdNotFound`，失效发生在腾讯云语音合成鉴权阶段，尚未生成音频，并非手机静音或播放器故障。
- 语音合成改为优先使用云函数运行角色提供的临时凭据及会话令牌，长期 TTS 凭据仅保留为兼容回退，降低固定密钥失效造成全量播报中断的风险。
- 云函数代码部署后需用真实比分文案回读验证；若运行角色未配置语音合成权限，应补充最小权限后再次验证。
- 运行角色临时凭据回测已生效，但腾讯云返回 `AuthFailure.UnauthorizedOperation`，确认 `TCB_QcsRole` 缺少 `tts:TextToVoice`。当前本地 CloudBase API Key 也不具备 CAM 策略管理权限，无法代替主账号完成授权。
- 云函数与计分板已补充“语音播报权限未开通”的明确错误码和用户提示，避免继续笼统显示“生成失败”。
- 用户已在腾讯云 CAM 创建最小权限策略 `SxfBasketballScoreVoiceTts`，仅包含 `tts:TextToVoice`，并绑定到云函数实际运行角色 `TCB_QcsRole`；未绑定足球项目用户或运行日志角色。
- 授权后线上回测通过：标准、现场、儿童三种现有音色均成功生成 MP3，临时音频地址读取状态均为 HTTP 200；标准音色生成耗时约 1.3 秒，其余音色约 1.1 秒。

## 2.1.4 MC 播报音色直接选择

- MC 设置的播报音色改为男声、女声、童声三枚并排单选按钮，并增加“现场有力、清晰自然、活力童声”备注。
- 点击任一音色会立即成为唯一选中项、写入本机设置并自动生成试听；不再打开播报员下拉列表，不跳转页面，不显示选择弹窗。
- 简约播报与完整播报的现有选择保持不变。

## 2.1.4 审核前收口

- 当前对外版本再次确认并锁定为 2.1.4；本地项目根版本、依赖锁定根版本和“我的”页当前版本均为 2.1.4，2.1.5 不进入本次公开公告。
- 新增 `发布公告_2.1.4.md`、`发布记录_2.1.4_明细.json` 和本地知识库中的 2.1.4 公告/更新记录；公告本地渲染为 15 个飞书知识块。
- 审核前检查：相关 JavaScript 与 JSON 通过；MC 三音色直接单选行为通过；Taro 小程序构建成功；全量 TypeScript 仍被既有 `config/index.ts` 的 `import.meta` 模块选项错误阻塞。
- 云端复测：标准女声、现场男声、童声均成功生成 MP3，临时音频地址均返回 HTTP 200；公共 MC 音效存储继续使用指定音频只读、其他文件按归属私有的自定义规则。
- 飞书同步阻塞：固定 `feishu.env` 与加密用户授权依赖的旧 App Secret 已失效，OpenAPI 返回错误码 10014；本地公告和版本明细已准备完成，待更新有效 App Secret 后再写入并回读版本 Base 与知识库公告。
- 微信发布边界：用户将自行上传并提交 2.1.4 微信审核；本次不代替用户提交审核或正式发布。
