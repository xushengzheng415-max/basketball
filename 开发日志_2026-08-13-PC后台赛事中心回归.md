# 开发日志：PC 后台赛事中心回归

日期：2026-08-13

## 本次修复

- 修复赛程设置三级页路由：`#schedule/settings` 现在可直接访问，刷新、返回和从工作台进入时不会错误回退到赛程日历。
- 赛程设置允许作为自动排赛前的资源配置页打开；赛程日历和场次列表仍受“抽签已保存”前置条件约束。
- 更新场馆测试路径：场馆、球场和时间段已从赛事管理移入赛程设置。
- 更新抽签导出测试数据：球队需同时满足“已认领”和“审核通过”才会进入抽签、球队管理及后续赛程链路。

## 已验证

| 范围 | 结果 |
| --- | --- |
| 赛程设置三级页、场馆、球场、半场 A/B 拆分、时间例外、删除确认、保存回写 | 通过 |
| 赛程设置 1672×941 与 1440×900 无横向溢出 | 通过 |
| 小组与淘汰赛赛制生成 | 通过 |
| 抽签表格与分组海报导出（队徽、队名智能大小、横竖版、三种样式） | 19 项通过 |
| 赛事规程联动、报名二维码、发布与 Word 导出 | 通过 |

## 测试资产同步项

- `tools/test-tournament-venues.js` 已从旧的 `#event/venues` 切换为 `#schedule/settings`，并改为验证动态资源摘要，而非依赖过期日期下的固定容量数字。
- `tools/test-tournament-draw-export.js` 的测试球队已补齐赛事资格“已通过”。

## 后续待测

- 报名与赛事创建两条旧用例仍在等待“队徽裁切确认”按钮；需要改为当前图片上传的异步加载完成断言后再执行确认。
- 下一轮应专门覆盖统一日历：拖拽到空场地、与已有比赛交换、手动增加场次、保存同步至场次表、清空后重新配置。

## 本轮补充：赛程时间段

- 移除赛程设置中与上午、下午、晚上配置重复的“手动时间段”。
- 系统推算的每日开赛时间改为逐条可调整的时间卡片；调整结果保存为自动排赛实际使用的时间段。
- 提供“按基础时段重新推算”，用于在修改上午、下午、晚上、单场时长或缓冲时间后恢复系统自动计算。
- 回归通过：不再显示重复手动时间段、逐条调整、写入排赛资源、重新推算恢复。

## 本轮补充：时间段与资源摘要联动

- 中午最晚结束时间默认统一为 `12:00`，兼容旧数据中的 `12:30`。
- 移除按日期维护的例外时段；每日开赛时间支持手动添加、逐条调整和按基础时段重新推算。
- 资源摘要改为动态计算：启用球场数 × 可用比赛日数 × 每日开赛时间段数，并在预计可排场次下展示每日时间段数量。
- 回归验证通过：场馆/球场切换、半场 A/B 拆分、时间段增删改、摘要刷新、保存配置及 1672×941 / 1440×900 视口检查。
- 修复比赛日摘要曾按周折算的问题；现在日期范围首尾均计入，修改起止日期后会立即显示实际自然日，并同步重算预计可排场次。
- 修复刷新页面时日期输入框已跳到当前日期、摘要仍读取旧日期的状态不同步；摘要现在始终以页面当前显示的起止日期为准。
- 修复桌面时间选择器已显示新时间、保存校验仍读取旧状态而误报的问题；保存时会重新读取当前表单，并只校验实际启用的上午、下午及晚上时段。

## 本轮补充：竞赛日程与球队体力约束

- 将“场次列表”更名并重构为“竞赛日程”，按日期、时间、轮次、组别、比赛队、场地、序号展示；相邻相同日期与轮次自动合并单元格。
- 竞赛日程保留阶段、轮次、组别、球队、场地、状态筛选，并同步调整 CSV 导出和 A4 纵向打印格式。
- 新增硬约束：同一球队同一天上午、下午、晚上各最多一场；自动生成、手动加场、拖拽交换与保存赛程统一校验。
- 当日期与时段容量不足时，自动排赛不再生成体力冲突赛程，会提示增加比赛日或启用更多时段。
- “直接打印”改为打开独立竞赛日程打印预览页，继承当前A4横竖方向和筛选结果；预览页提供打印与关闭按钮，并在浏览器允许时自动拉起系统打印窗口。
## 竞赛日程 A4 纸张与导出一致性

- 将竞赛日程预览从普通响应式白色容器改为真实 A4 比例：竖版 `210mm × 297mm`，横版 `297mm × 210mm`；赛程不足一页时保留纸张底部空白。
- “导出表格”不再输出无样式 CSV，改为输出 Excel 可打开的正式竞赛日程，保留标题、场馆、7 列字段、列宽、日期/轮次合并单元格、场序、淘汰赛引用和当前 A4 横竖方向。
- 更新竞赛日程回归测试，新增真实 A4 比例、整页高度、竖版/横版导出方向、Excel MIME、合并单元格与淘汰赛文本验证。
- 2026-08-14 修正 Excel 打开 HTML `.xls` 后制式漂移的问题：浏览器端改为生成真实 OOXML `.xlsx`，显式写入标题合并、列宽、行高、字体、边框、日期/轮次合并、A4 页面方向、打印区域与重复表头；页面与导出不再依赖 Excel 对 HTML/CSS 的二次解释。

## 2026-08-14：赛程阶段依赖与比赛日不足提醒

- 自动排赛新增阶段硬约束：同一竞赛组别必须依次完成小组赛各轮、半决赛、决赛/三四名决赛；后一阶段只能进入更晚的上午、下午或晚间时段。
- 三四名决赛和决赛被视为同一阶段，可在半决赛全部结束后并行安排；不再错误地互相串行占用两个阶段。
- 两天白天仅有 4 个半天时段，而 8 队两组的小组循环加淘汰赛需要 5 个连续时段，系统会阻止生成错误赛程，并提供“开启晚间场次”与“增加至 3 天”两个直接解决方案。
- 两天含晚间的推荐结果为：第 1 天上午第 1 轮、下午第 2 轮，第 2 天上午第 3 轮、下午半决赛、晚间决赛；三天白天方案将决赛安排在第 3 天上午。
- 赛制预览、自动排赛和赛程日历统一展示容量提醒；已有赛程如存在阶段倒置，会提示重新排赛且禁止保存。
- 新增 `tools/test-tournament-stage-dependency.js`，验证两天白天拦截、两天晚间方案、三天白天方案和完整 16 场生成；竞赛日程 A4/Excel 回归继续通过。
- 自动排赛新增关键场次默认规则：小组赛继续按多场地并行，半决赛、决赛和三四名决赛集中到主场地逐场进行；该规则属于默认排赛策略而非保存硬限制，主办方仍可在日历中手动拖拽为并行场次。
- 赛程日历会标记“关键场次”，摘要展示逐场进行数量；专项回归已验证 4 场关键比赛均使用主场地且开赛时间互不重复。
- 赛程日历比赛卡片信息整体放大约一倍：阶段/组别、队名、对阵符号、队徽和时间格同步放大，并增加卡片高度与间距，保证大字号下仍可快速识别。
- 根据实际展示反馈收紧卡片尺寸：保留醒目字号但恢复紧凑高度，卡片内容使用可用空间居中填充；日历网格改为按场地数量自适应均分，2、3、4 块场地均不产生横向溢出，窄屏再保留横向滚动保护。
- 关键场次的逐场规则继续保留在自动排赛逻辑中，但移除比赛卡片内的“关键场次”文字标签，只保留正常阶段与轮次信息。
- 比赛卡片改为单元格内居中紧凑宽度：两块场地时最大约 360px，三、四块场地时自动收缩到对应列宽；空白单元格仍保持整格可拖拽和添加场次。
- 进一步收紧日历整体网格：两块场地时网格本身按红框宽度左对齐，不再保留左侧大空区；三、四块场地时网格自动扩展并均分列宽，专项测试验证四场地无横向溢出。
- 日期卡片的阶段标签（如“小组赛”）改到日期信息左侧区域，与比赛日和场次数量保持同一左对齐起点，不再悬浮在右上角。

## 2026-08-14：现场执行按赛程接通

- 现场执行“人员与任务”不再使用固定 G1001-G1005 示例，改为读取当前事件已保存的 `scheduleRows`，按比赛日期、组别、场馆和人员状态筛选。
- 现场任务表补齐时间、场序、阶段、轮次、组别、场地、计分员、记录员、MC 和准备状态；右侧详情随筛选结果切换到对应场次。
- 人员安排写入当前赛事的 `onsiteAssignments`，移除席位、确认安排和保存安排均会实时回写；保存时只有全部场次完成必要席位才会将现场执行标记为已安排。
- 球队模式下记录员席位显示“不适用”，球队+球员模式下记录员仍作为必需席位；外请人员权限说明限定在被分配的本场任务内。
- 新增 `tools/test-tournament-onsite.js`，验证已保存赛程载入、默认比赛日、阶段/轮次/场地展示、组别筛选、真实场次绑定和待补齐提示；输出 `outputs/赛事中心验证/现场执行-按赛程任务-1672x941.png`。

## 2026-08-14：裁判名单与动态技术台排班

- 现场执行新增“裁判名单”页，名单按当前赛事维护，不建立独立机构裁判库。
- 裁判按比赛预先配置 1 人、2 人或 3 人裁判组，绑定后直接发送比赛任务；首次授权完成后，后续场次自动进入任务列表，不再逐场确认。
- 计分员、记录员和 MC 保持按场次动态排班，允许同一人员结束上一场后继续执行下一场；裁判组和技术台任务分开存储。
- 新增 `refereeAssignments` 数据结构，记录裁判组成员、裁判组人数、任务发送状态和发送时间。
- 回归验证补充裁判名单、1–3 人裁判组绑定与直接发送任务；输出 `outputs/赛事中心验证/现场执行-裁判名单-1672x941.png`。
## 2026-08-14：裁判名单按赛事自行维护

- 正常业务模式下裁判名单默认保持 0 人，由主办方通过“新增裁判”自行建立，不再自动添加虚拟裁判。
- 仅 `demo=1` 的自动化回归夹具使用内置裁判数据；上一轮本地联调自动生成且尚未绑定的虚拟裁判会自动清理。
- 回归验证：`node tools/test-tournament-onsite.js` 全部通过。
