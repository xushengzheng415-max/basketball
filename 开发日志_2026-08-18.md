# 开发日志 2026-08-18

## 赛事中心：青少年赛制优先调整

- 将新建竞赛组别的默认赛制改为“小组循环 + 全员分层排位”。
- 新增“全员分层排位”阶段：小组单循环后，全体球队按组内相同名次进入对应排位层；可配置每队排位赛场次。
- 赛制选择文案改为青少年赛事语境，区分成长排位赛、争冠淘汰赛与邀请赛淘汰赛；旧赛事名称仍会自动兼容为新名称。
- 分组页、赛制阶段预览和自动排赛蓝图已识别该新赛制。

## 验证

- `node --check admin/tournament-center.js` 通过。
- `node tools/test-tournament-rules.js` 未完成：现有测试在无页面来源的 Node 环境读取 `localStorage` 时出现 `SecurityError`，与本次赛制逻辑无关，待后续为测试环境指定页面 URL 后恢复。

## 待继续

- 为 U6—U12 提供可一键套用的规则预设（比赛形式、时长、友爱规则、平分决胜、A/B 阵容等）。
- 将多组别的大规模分层排位具体对阵算法补充为可视化、可编辑的赛程模板。

## 企业微信渠道客户欢迎语

- 新增 `sxWecomCustomerRouter` 云函数：校验企微回调签名、解密加密消息，只处理配置白名单内的渠道 `state`；无渠道参数的主动添加客户不执行欢迎语。
- 渠道命中后支持添加企业客户标签，并在收到有效 `WelcomeCode` 时发送文字及最多 9 个附件。
- 新增带渠道参数的「联系我」二维码创建脚本，默认仅预览，必须显式设置 `WECOM_APPLY=true` 才会创建云端配置。
- 新增中文配置说明，明确必须关闭企微原生通用“加客户欢迎语”，真实 Secret、Token、EncodingAESKey 和标签 ID 仅通过云端环境变量配置，不写入仓库。
- 已部署云函数 `sxWecomCustomerRouter`，运行时 `Nodejs20.19`、内存 256 MB、超时 10 秒；已创建 HTTP 访问路径 `/sxWecomCustomerRouter`。
- 当前 `WECOM_ROUTER_ENABLED=false`，未配置任何企微密钥，不会发送欢迎语或修改客户标签。

### 验证

- `node tools/test-wecom-customer-router.js` 通过，覆盖 AES-CBC 解密、回调签名、XML 解析、渠道匹配、标签/欢迎语调用以及主动添加不触发。
- `node --check cloudfunctions/sxWecomCustomerRouter/index.js` 通过。
- `node --check cloudfunctions/sxWecomCustomerRouter/core.js` 通过。
- `node --check tools/wecom-create-contact-way.js` 通过。
- `git diff --check` 对本任务文件通过。
- CloudBase 回读确认函数状态为 `Deployment completed`，HTTP 服务已关联对应函数；未配置回调参数时入口返回预期的安全失败响应。

### 当前边界与下一步

- 当前分支：`codex/education-2.0`，工作区存在大量既有未提交改动；本任务未切分支、未回滚或暂存其他文件。
- 待管理员在企微“客户联系 API”页面配置接收事件服务器，并将敏感配置直接录入 CloudBase 函数环境变量。
- 完成回调验证后再创建带 `state` 的渠道二维码、关闭通用加客户欢迎语，并使用两个外部微信账号分别验证扫码添加与主动添加。
- 本次未提交、未推送 GitHub 或 Gitee。

## 企业微信渠道分流迁移至轻量服务器

- 购买并初始化上海轻量应用服务器 `saixiaofeng`，公网固定 IP 为 `124.221.239.63`，系统为 Ubuntu 24.04 LTS，当前规格 2 核 2 GB、40 GB SSD。
- 完成 SSH 公钥登录、禁用密码登录和 root 远程登录，启用 UFW、Fail2ban、自动安全更新；仅开放 22、80、443 端口。
- 安装 Docker、Docker Compose、Nginx、Certbot，并配置腾讯云内网 Docker 镜像加速。
- 新增 `api.saixiaofeng.com` A 记录指向轻量服务器；Let's Encrypt HTTPS 证书已签发并启用自动续期，HTTP 自动跳转 HTTPS。
- 将企微回调服务迁移为 Docker 容器 `sxf-wecom-customer-router`，部署目录为 `/opt/sxf-platform`，容器仅监听本机 `127.0.0.1:3100`，由 Nginx 对外转发。
- 企业微信自建应用“赛小蜂客户分流”已配置可信 IP、可调用客户联系接口和外部联系人变更回调；企微回调验证返回 HTTP 200。
- 生成渠道参数 `sxf_official_article_footer` 的“公众号文章统一客户入口”二维码，源文件位于仓库外 `E:\Documents\saixoafeng_basketball\公众号\通用图\公众号文章统一企微入口.png`。
- 公众号扫码客户自动添加“来源渠道 / 公众号”标签；原生通用加好友欢迎语已删除，成员主动从群内添加客户不会命中渠道规则。
- 分流流程已升级为个性化 H5：客户添加企微后收到身份选择入口，选择“机构人员”或“赛事主理人”后自动互斥设置“机构客户/赛事客户”标签，并只显示对应长期群活码。
- 身份选择 URL 为 `https://api.saixiaofeng.com/wecom/identity`，使用加密、限时身份令牌，不在 URL 中明文暴露客户标识。
- 本地单元测试、H5 双选项测试、HTTPS 健康检查、企微接口调用、渠道二维码和容器配置回读均通过；服务当前启用。

### 安全与待验收

- 企业微信 Secret、回调 Token、EncodingAESKey 和身份令牌密钥仅保存在服务器权限为 0600 的环境文件及仓库外受限本地配置中，未写入仓库和开发日志。
- 另一个企微自建应用可以复用公网 IP `124.221.239.63` 与域名网关，但必须使用独立回调路径、独立 Secret、Token、EncodingAESKey、环境文件和最小接口权限。
- 身份选择新版本尚需使用全新微信客户完成一次真实端到端验收：扫码添加、收到身份入口、选择角色、检查互斥标签、识别对应群码。
- CloudBase 旧版 `sxWecomCustomerRouter` 云函数仍保持关闭，后续确认轻量服务器稳定后应清理其敏感环境变量和旧 HTTP 入口。

## 官网每日 AEO/GEO 与 SEO 内容批次

执行时间：2026-08-18 14:37—14:46（北京时间）。

### 内容生产

- 核心长文《篮球比赛打平后怎么进入加时？计分台设置与赛果核对流程》，搜索意图为“常规时间打平后的加时确认、设置与赛果收口”，正文约 1536 个汉字，质量评分 46/50。
- 单一问题答疑《篮球比赛没有 MC 也能正常计分吗？》，搜索意图为“MC 是否为基础计分的必要条件”，正文约 726 个汉字，质量评分 45/50。
- 具体场景文《篮球比赛中途更换计分员怎么交接？》，搜索意图为“比赛进行中计分人员换手与状态交接”，正文约 1172 个汉字，质量评分 46/50。
- 三篇均包含直接答案、自指 canonical、Article JSON-LD、正文可见 FAQ 与 FAQPage、自然内链、可见官网关键词链接、产品与证据边界。
- 加时规则只写“以本场规程和现场决定为准”；没有虚构赛事案例、客户、排名、价格、政策或产品自动裁定能力。已按中文去 AI 痕迹规则审校。

### 接入与验证

- 新增文章已接入 `articles/index.html`、`sitemap.xml` 和 `llms.txt`；内容中心更新日期为 2026-08-18，Sitemap 增至 46 个 URL。
- `npm run site:validate` 通过：39 篇文章、46 个 Sitemap URL。
- 首次 `npm run site:deploy` 在上传前因 Windows PowerShell 未自动加载 `Microsoft.PowerShell.Security` 失败；显式加载系统模块后，以相同命令和项目统一启动器重试成功。
- 部署固定进入 `sxf-basketball-d9gp6yt0rd1f7be4d`，未使用全局 tcb 登录、未切换 envId、未读取或输出凭据。
- 独立回读正式域名与 CloudBase 系统域名：每个域名 46 个 Sitemap URL，加 Sitemap、robots.txt、llms.txt，共 49 项，各 49/49 HTTP 200。
- 两个生产入口的 39 篇文章标题和 canonical 均与本地一致；三篇新文章在内容中心可见。

### 收录巡检

- 百度最后平台证据仍为 8 月 13 日：首页抓取诊断成功、此前索引量 0、抓取频次接近 0，5 个重点 URL 已手动提交。今天无登录态后台新证据，未重复提交；状态为“已提交重点 URL，待复查”。
- Bing 最后平台证据仍为 8 月 9 日：新站点已验证，Sitemap 正在处理、已发现 URL 0、错误 0、警告 0。今天无最新 URL 检查或 Sitemap 后台回读，未重复提交。
- 百度与 Bing 的“赛小蜂”“赛小蜂篮球”和首页精确标题公开查询均未取得可核验官网结果；Bing `site:` 页中的域名来自查询词和页面元数据，不计作搜索结果。
- 今天三篇仅标记“已上线，待复查”。HTTP 200、进入 Sitemap、内容中心或 llms.txt 均不作为收录证据。
- 展示、查询词、点击、平均位置、最新已发现 URL 数与 URL 级索引状态不可得；下一次建议 2026-08-21 回读百度索引量/抓取频次/提交反馈和 Bing Sitemap/首页 URL 检查。

### 外链巡检与人工计划

- GitHub 公开页 200，但仍有不带 `www` 的地址和已停用旧域名；CSDN 已知文章 200，但仍指向已停用旧域名；Gitee 本轮 405。
- 严格口径下当前可核验有效直接引用域为 0；新增有效外链 0、新增无链接品牌提及 0，已知旧域名失效链接至少 2 条。
- 今日唯一人工任务：在 CSDN 已发布文章中，把旧“篮球比赛现场计分指南”链接替换为 `https://www.sxfbasketball.cn/guides/basketball-scoring.html`，不新增其他推广链接。自动化未登录、编辑或发布。

### 文件与 Git 边界

- 本批次只新增 3 篇官网文章，并更新内容中心、Sitemap、llms.txt、收录台账、外链台账和本日志；未修改小程序、云函数、PC 后台、支付、会员或自媒体业务文件。
- 当前分支仍为既有分支，工作区包含大量其他任务改动；本批次未创建分支、未暂存、未提交、未推送 GitHub 或 Gitee。

## 官网域名收敛与公开仓库链接修正

执行时间：2026-08-18 16:43—17:25（北京时间）。

### DNS 与入口状态

- 用户在 DNSPod 将根域 `@` 从暂停的 CNAME 改为启用的“显性 URL”，目标为 `https://www.sxfbasketball.cn/`，线路默认、TTL 600。
- DNSPod 权威节点和公共 DNS 已发布记录；直连转发节点确认 `http://sxfbasketball.cn/` 单跳 301 到正式官网。
- CloudBase 个人版只能绑定 1 个自定义域名，继续保留给正式 `www.sxfbasketball.cn`。DNSPod URL 转发前地址不支持 HTTPS，因此 `https://sxfbasketball.cn/` 暂不可用；未用 DNS 或证书结果虚报为已完成全协议收敛。
- 现有篮球证书覆盖裸域与 `www`，但证书部署页没有 DNSPod URL 转发资源，未执行错误部署。
- 待处理：`http://www` 强制 HTTPS、`/index.html` 到根首页 301，以及裸域 HTTPS 的独立边缘重定向方案。

### GitHub

- 用户明确授权 Codex 修改 GitHub 和 Gitee，CSDN 由用户自行处理。
- GitHub About 网站地址已改为 `https://www.sxfbasketball.cn/`。
- GitHub `main` README 删除停用旧域和裸域，新增正式官网、内容中心及唯一正式域名说明；提交 `75d1efe63b276914c52ad8871aa0c787f830c0be`。
- 公开仓库页回读 200，正式官网与内容中心的实际 `href` 正确，`rel` 包含 `nofollow`；未发现 `54football.top` 或 `https://sxfbasketball.cn/`。

### Gitee

- Gitee 默认分支为 `master`。从隔离克隆只修改 README 的官网、内容中心和唯一正式域名说明，提交 `46b49fb92df05f545572fccca58cdc4f8b661665` 并推送 Gitee `master`。
- Gitee API、提交级 raw 页面和缓存刷新后的 `master` raw 页面均确认新内容生效、旧域消失；公开仓库页本轮仍返回 405，未据此虚报直接 `href`。
- Gitee 仓库描述“赛小蜂篮球功能”不含错误 URL，因此未修改；仓库 homepage 字段为空。
- 隔离克隆位于 `work/gitee-seo-sync-20260818`；自动清理命令被本地安全策略阻止，未改用跨 shell 删除，后续可在安全策略允许时清理。

### 当前边界

- CSDN 文章仍由用户手动把旧指南链接替换为 `https://www.sxfbasketball.cn/guides/basketball-scoring.html`。
- 当前严格口径有效直接引用域由 0 恢复为 1（GitHub）；Gitee 为公开品牌跳转入口，CSDN 待修正。
- 本次远端写入仅包含 GitHub About、GitHub README 和 Gitee README；未提交或推送当前本地篮球项目的其他改动，未操作百度/Bing 提交。

### CSDN 用户修正回读

- 用户已手动把 CSDN 文章 `https://blog.csdn.net/weixin_50328081/article/details/163215052` 的旧指南链接改为 `https://www.sxfbasketball.cn/guides/basketball-scoring.html`。
- 公开文章回读 200，canonical 正常；实际锚点 `href` 直接指向正式指南，`rel="nofollow"`，页面不再包含 `54football.top`。
- 正式指南目标页返回 200，canonical 自指。
- 当前严格口径有效直接引用域由 1 增至 2：GitHub、CSDN；Gitee 保持公开品牌跳转入口。已知可控公开平台中的旧域链接本轮均已清理。

## 企业微信文档助手与客户赛事需求登记表

执行时间：2026-08-18 17:55—18:21（北京时间）。

### 应用与域名配置

- 新建通用企业微信自建应用“赛小蜂文档助手”，用于统一创建后续文档、表格、智能表格和收集表，不按单个文档重复创建应用。
- 应用已配置可信域名 `api.saixiaofeng.com`、企业可信 IP `124.221.239.63`，并加入“协作 → 文档 → API”的可调用应用列表。
- 企业微信域名归属校验文件已部署到 `api.saixiaofeng.com` 的精确静态路径；Nginx 配置检查和重载通过，校验文件 HTTPS 返回 200 且内容哈希与下载源文件一致。

### 工具与安全边界

- 新增 `ops/lightserver/wecom-doc-assistant/create-collect-form.mjs`，支持从 JSON 模板创建收集表、读取表单详情、获取分享链接，并通过结果文件阻止同一路径重复创建。
- 新增模板 `ops/lightserver/wecom-doc-assistant/templates/客户赛事需求登记表.json`，包含赛事名称、赛事周期、参赛队伍数量、赛制、联系人、联系电话六个必填字段。
- 本地敏感配置仅保存在仓库外 `C:\Users\15043\AppData\Local\SxfPlatform\config\wecom-doc-assistant.env`，服务器配置保存在 `/opt/sxf-platform/config/wecom-doc-assistant.env`；两端均限制当前授权用户访问，服务器文件权限为 0600。
- 工具不输出或记录 CorpID、应用 Secret、access_token；未读取、修改或复用“赛小蜂客户分流”应用的敏感配置。

### 创建与验证

- 已由上海轻量服务器的固定可信出口 IP 调用企业微信 `wedoc/create_collect` 创建“客户赛事需求登记表”。
- API 回读确认标题正确、字段数为 6、字段名称和顺序正确，填写权限为所有人可填写；分享链接公开 GET 返回 HTTP 200。
- 结果元数据仅保存在服务器 `/opt/sxf-platform/output/wecom-doc-assistant/customer-event-requirement.json`，权限为 0600。
- 既有 `https://api.saixiaofeng.com/health` 回读仍为 HTTP 200，客户分流容器保持健康，未重启或修改现有业务容器。

### Git 边界

- 当前仍处于既有 `codex/education-2.0` 分支，工作区在本任务开始前已包含大量其他任务改动；本次只新增文档助手工具、模板并追加本日志。
- 未暂存、未提交、未推送 GitHub 或 Gitee。

## 蜂狂篮球公众号草稿图片间距修正

执行时间：2026-08-18 18:29（北京时间）。

- 原因：`tools/wechat-official/wechat-draft.js` 在替换图片占位符时，为图片生成新的 `<p>`；文章HTML外层已有 `<p>`，形成嵌套段落。微信公众号自动修复嵌套后产生默认上下间距，导致分隔符、序号图和正文图片周围出现大块空白。
- 修正：图片包装改为无默认外边距、零行高的块级 `span`；自动图注也改为块级 `span`，避免再次生成嵌套段落。
- 验证：`node --check tools/wechat-official/wechat-draft.js` 通过；8月21日蜂狂篮球草稿更新成功，原 `media_id` 不变。
- 回读：公众号实际HTML包含10个零间距图片包装，未发现嵌套图片段落、占位符或旧包装；基于公众号实际HTML生成的预览确认分隔符上下空白已消除。
- 边界：只更新现有草稿，未定时、未群发、未发布；未暂存、未提交、未推送。

### 客户赛事需求登记表字段调整

- 保持原表单 ID 和分享链接不变，通过 `wedoc/modify_collect` 全量更新问题列表。
- 原“赛事周期”拆分为“赛事周期单位”和“赛事周期数量”：单位使用下拉菜单，选项为“天、月”；数量使用必填文本，提示按上一题填写天数或月数。
- “赛制”改为下拉菜单，包含小组循环赛、小组循环 + 晋级组单循环、小组循环 + 单败淘汰、小组循环 + 双败淘汰、全部球队单循环、全部球队双循环、单败淘汰、双败淘汰 8 个选项，并在题目说明中保留各赛制定义及 U8 启蒙组现行说明。
- 新增“参赛组别（可多选）”，选项覆盖 U6、U7、U8、U9、U10、U11、U12。
- API 回读确认表单共 8 题，周期单位与赛制的题型均为下拉列表，参赛组别题型为多选，选项数量分别为 2、8、7，外部填写权限和原分享链接保持不变。
- 新增 `ops/lightserver/wecom-doc-assistant/update-collect-form.mjs`，用于按模板安全更新已创建的收集表并回读字段类型、选项和分享链接。

### 文档提交回调与赛事线索接入

- 表单新增“机构/主办方名称”和“所在城市”两个必填客户识别字段；为保护已有问题 ID，新增题使用独立 ID 并通过 `pos` 调整到表单开头。API 回读确认原链接不变、总题数为 10。
- 新增独立服务 `cloudfunctions/sxWecomDocAssistant/`，负责企业微信回调验签与解密、收集表答案补偿同步、答案 ID 去重、赛事线索结构化保存和负责人应用消息通知。
- 服务不只依赖 `collection_doc_complete` 回调，还按 300 秒周期补查答案；线索以 `repeated_id + answer_id` 去重，通知失败保留 pending 状态并在后续同步重试。
- 新增 `tools/test-wecom-doc-assistant.js` 和 `tools/verify-wecom-doc-assistant-callback.js`；本地解析/映射测试通过，使用本地受限配置生成加密验证请求后，公网回调完成验签、解密并原样返回，HTTP 200。
- 轻量服务器新增独立容器 `sxf-wecom-doc-assistant`，仅监听 `127.0.0.1:3200`；Nginx 仅开放精确路径 `https://api.saixiaofeng.com/wecom/doc-assistant`。新旧两个企微容器均为 healthy，原健康检查保持 HTTP 200。
- 文档助手环境文件位于服务器 `/opt/sxf-platform/config/wecom-doc-assistant.env`，权限 0600；线索目录 `/opt/sxf-platform/data/wecom-doc-assistant` 权限 0700，状态文件权限 0600。Secret、Token、EncodingAESKey 和 access_token 均未写入仓库、日志或聊天。
- 当前线索库为空，待企业微信后台保存文档助手的接收消息服务器配置后，使用测试客户提交一条表单完成真实端到端验收。

### 周期数量与计划开赛时间补充

- “赛事周期数量”由文本输入改为下拉列表，提供 1—36 共 36 个数字选项；与上一题“赛事周期单位”的“天/月”组合形成完整周期。
- 新增必填日期题“计划开赛时间”，使用企业微信收集表日期题型；为保护既有答案字段映射，使用新增问题 ID 并调整后续显示顺序。
- 文档助手线索结构新增 `planned_start_date`，负责人通知中同步展示计划开赛日期。
- API 回读确认原分享链接不变，表单共 11 题：周期数量为下拉类型且包含 36 个选项，计划开赛时间为日期类型。
- 文档助手镜像已重建，容器为 healthy；启动补偿同步无新增线索、无错误，公网加密回调验证再次返回 HTTP 200 且解密内容一致。

### 回调 Token 字符集兼容

- 企业微信管理后台的 Token 输入框仅接受英文字母和数字，首次生成的 URL-safe Token 可能包含 `-` 或 `_`，页面校验不通过。
- 已在本地受限配置中重新生成 32 位纯字母数字 Token，同步更新服务器 0600 环境文件，并强制重建文档助手容器以重新加载环境变量。
- 新 Token 未输出到聊天、日志或仓库；公网加密回调再次完成验签与解密，HTTP 200 且回显一致。
- 管理后台同样限制 EncodingAESKey 只能由字母和数字组成；已重新生成 43 位纯字母数字 AESKey，同步服务器并强制重建容器。新 AESKey 完成公网验签、AES 解密与回显验证，HTTP 200。

### 赛事需求表专属企微入口

- 新建永久“联系我”配置，渠道参数为 `sxf_event_requirement_form`，备注为“客户赛事需求表企微入口”，使用成员为 `ZhengXuSheng`；配置 ID 和二维码元数据仅保存在服务器 0600 结果文件中。
- 在企业标签组“来源渠道”下新增“赛事需求表”标签，并将该渠道规则配置为自动添加“来源渠道 / 赛事需求表”和“客户类型 / 赛事客户”两个标签。
- 新增欢迎语：“已收到您的赛事需求，请回复机构或主办方名称”，不复用公众号渠道的身份选择欢迎语。
- 新增公开页面 `https://api.saixiaofeng.com/wecom/event-consultant`，二维码镜像到自有 HTTPS 静态路径；页面和二维码均返回 HTTP 200，二维码图像已做可视检查。
- 收集表描述增加赛事顾问入口，并在表单末尾增加必填单选题“后续对接方式”，明确提示提交后打开专属页面、添加企微并回复机构或主办方名称。API 回读确认表单总题数为 12，原分享链接不变。
- 客户分流环境文件修改前已在服务器受限目录备份；容器强制重建后确认新渠道规则已加载、路由启用，客户分流和文档助手两个容器均为 healthy。
- 技术验证完成，尚需使用全新微信账号真实扫描该专属二维码，确认添加成功、两项标签准确、欢迎语正确且不出现公众号身份选择入口。

### 新客户与已添加客户双路径修正

- 真实扫码确认：新微信首次添加会收到赛事需求专属欢迎文本，但二维码不会再次发送；已经是好友的微信再次扫码不会触发新增客户事件，也不会重发欢迎语。这是企业微信“欢迎语仅随首次添加事件触发”的平台行为。
- 首次添加欢迎语调整为要求发送“赛事需求已提交 + 机构或主办方名称”，并新增“填写或再次提交赛事需求”的链接卡片。
- 专属添加页面增加已添加客户说明：再次扫码无提示时，直接回到原聊天窗口发送“赛事需求已提交 + 机构或主办方名称”。
- 表单末尾“后续对接方式”扩展为两个选项：“尚未添加，提交后扫码添加”和“已经添加，提交后在原聊天发送机构名称”；表单描述与题目说明同步更新。
- 客户分流容器重建后确认新渠道规则和欢迎链接卡片均已加载，客户分流与文档助手容器均为 healthy；页面与原表单返回 HTTP 200。
- 仍需用全新微信再验一次首次添加的链接卡片；已添加客户路径只能通过原聊天主动发消息，企业微信 API 不会因重复扫码自动向存量好友推送消息。

### “立即跟进”应用卡片

- 企业微信官方应用消息按钮只能打开 URL，不能直接按手机号发起好友申请；“联系我”能力也仅支持客户主动添加成员。因此实现最接近官方能力的安全链路：应用文本卡片 → 立即跟进 → 内部需求详情 → 一键复制手机号 → 企业微信添加客户中搜索手机号。
- 文档助手通知由纯文本升级为 `textcard`，按钮文案为“立即跟进”；卡片展示机构、联系人、赛事、组别、赛制和计划开赛时间。
- 新增带 HMAC 签名和有效期的线索详情链接，URL 不含手机号、机构名等明文数据；详情页设置 noindex、禁止嵌入、无缓存，并对用户输入做 HTML 转义。
- 详情页展示完整赛事需求，提供“复制客户手机号”和“复制客户添加入口”两个操作，并区分未添加客户与已有好友的跟进步骤。
- 本地新增签名密钥仅保存在仓库外受限配置，服务器环境文件权限保持 0600；未输出密钥或将其写入日志、仓库。
- 使用现有测试线索重置通知状态并成功发送新卡片：服务日志确认 `notified: 1`。独立验证确认详情页 HTTP 200、展示对应线索且包含复制手机号操作，文档助手容器为 healthy。

### 登记表移除强制企微添加步骤

- 用户确认采用“客户填表 → 内部收到立即跟进卡片 → 复制手机号添加客户”的主流程，不再要求客户填写前后主动添加赛事顾问。
- 表单描述恢复为纯需求收集说明，删除二维码页面地址；移除必填题“后续对接方式”，保留机构、城市、赛事、周期、计划开赛时间、队伍数量、赛制、组别、联系人和联系电话 11 个业务字段。
- API 回读确认表单共 11 题、原分享链接不变，公开表单返回 HTTP 200。
- 专属二维码页面、来源标签和欢迎语规则保留为内部跟进页的备用“客户添加入口”，不再出现在登记表主流程；备用页面返回 HTTP 200。
- 客户分流与文档助手两个容器均保持 healthy，未影响现有公众号渠道分流。

### 赛事中心青少年赛制全链路回归

- 以未来比赛日验证了“报名准入 → 已审核球队抽签 → 组别赛制 → 赛程编排 → 现场执行 → 赛果录入/复核 → 积分与淘汰对阵”的完整本地链路；测试使用隔离浏览器数据，不影响人工测试保留的赛事数据。
- 已验证组别新增、同名组别独立编辑、组内循环、晋级组单循环、全员分层排位、单/双循环、邀请赛单/双败，以及 3 组 9 队、8 组 32 队、单组循环等边界；旧版“分组双败淘汰”数据统一迁移为“分组循环 + 争冠单败淘汰”，避免回退到错误赛制。
- 已验证抽签仅读取审核通过的球队，分组表格与海报导出可用；赛程验证覆盖两日容量提醒、加晚场/增加比赛日、阶段严格顺序、关键场次主场地逐场、同队半天不重复、2/4 场地自适应，以及竞赛日程 A4 横竖版、打印和 Excel 导出。
- 已验证后台补录比分立即生效、现场回传提供复核、未录入比分不误标已复核、积分榜与赛果矩阵即时更新、胜方分数强调、矩阵使用左上至右下连续对角线；淘汰对阵图位于“积分与球队”。
- 已修复赛果矩阵对角线方向：统一为左上至右下连续斜线，避免此前的反向显示。
- 已执行 `node --check admin/tournament-center.js`；组别赛制、赛程阶段依赖、抽签导出、现场执行和竞赛日程自动化验证均通过。下一步由产品进行真实手工回归。

### 青少年常用赛制收敛

- 组别编辑的主赛制收敛为四项：`小组循环 + 淘汰赛`、`小组循环 + 单循环`、`单败淘汰`、`全部球队单循环`；双循环、双败、单独组内排名等历史选项不再暴露给新建赛事。
- “小组循环 + 淘汰赛”内新增“淘汰赛后排位赛”选项，可选择不进行或开启全员分层排位；历史的“带/不带排位赛”数据会自动归入同一主赛制并保留对应赛制结构。
- “单败淘汰”及小组淘汰赛保留“淘汰赛排位赛”设置，可选择是否增加三、四名赛；由两场半决赛负者决定第三、第四名，不影响冠军线。
