# 2026-07-23 开发记录

## 蜂家日记公众号草稿自动化

- 新增独立工具 `tools/wechat-official/`，用于 AI 生成“蜂家日记”文章并通过微信公众平台官方接口写入“蜂狂篮球”草稿箱。
- 支持 `doctor`、`generate`、`draft`、`run` 四种命令；不包含自动群发或自动发布能力。
- 微信 AppSecret、OpenAI API Key、访问令牌缓存和生成稿件均只保存在 Git 排除的本机文件中。
- 已将用户提供的公众号 AppID 写入本机 `.env.local`；AppSecret 与 OpenAI API Key 仍待用户在本机填写。
- 验证：`node --check tools/wechat-official/wechat-draft.js` 通过；帮助命令通过；`git diff --check -- tools/wechat-official` 通过；本机敏感配置、缓存和输出目录的 Git 排除规则验证通过。
- 未完成的外部验证：尚未调用真实公众号草稿接口。下一步填写本机密钥、配置公众号 IP 白名单，然后运行 `doctor`；个人主体未认证账号是否具备草稿接口权限，以公众号后台和接口返回为准。
- 当前分支：沿用现有工作分支，未创建、提交或推送分支。

## 机构 PC 管理后台原型落地

- 以 `赛小蜂篮球UI/2.0版本/机构PC后台/` 中 I01-I14 原型为视觉与功能依据，增长功能优先采用“增长版-v2”页面。
- `admin/index.html` 调整为机构端默认入口；补齐账号密码登录、机构经营工作台、员工与权限、课程课包、复杂排课、招生 CRM、试听转化、续费策略、家长运营、内容与转介绍、财务、经营数据、经营归因和多校区对比。
- 原有平台运营工具保留为 `admin/platform-ops.html`，机构后台仅在页面底部提供低调的“平台运营入口”，避免机构角色与平台角色混用。
- 复用原型素材包中允许生产使用的登录背景和品牌 Logo；所有文字、数据、按钮、卡片、表格和图表均由 HTML/CSS/JavaScript 渲染，没有使用整页原型截图冒充页面。
- 当前为可交互 PC 原型，登录、菜单切换、弹窗和演示操作可用；真实机构认证、权限、课程、学员、财务和增长数据接口仍待后续云端接入。
- 验证：`institution-growth.js` 通过 Node 语法检查；1586×992 浏览器实测无横向溢出；15 个侧栏入口逐一切换且每次仅显示一个正式页面；登录页与工作台已按原型截图对比并修正为黑橙视觉体系。
- 当前分支：`codex/education-2.0`；未提交、未推送、未部署线上。

### 机构平台名称与背景视觉修正

- 根据《赛小蜂篮球机构训赛经营增长平台-产品介绍》确认正式名称为“赛小蜂篮球机构训赛经营增长平台”，同步修正网页标题、登录页定位、侧栏副标题、页脚和后台说明。
- 产品定位统一为：训练与比赛持续转化为家长认可、续费行动与机构增长；不再使用“赛小蜂教务 2.0”作为平台名称。
- 使用 Image 2 生成新的黑橙篮球馆背景，融入球场光轨、蜂巢数据纹理和低对比内容安全区；图片内不生成文字或 Logo，正式文案继续由 HTML 渲染。
- 新背景保存为 `admin/assets/institution/growth-platform-background-v2.png`，同时用于登录页和机构工作台底层氛围。
- 1586×992 浏览器截图复核通过：正式名称清晰、登录卡可读、无脚本错误、无横向溢出。
