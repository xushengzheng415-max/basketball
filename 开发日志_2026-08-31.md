# 赛小蜂篮球开发日志（2026-08-31）

## 2.1.0 第4次补丁：家长建档头像完整处理流程

- 修复家长自主建档上传手机原图时触发 CloudBase EXCEED_MAX_PAYLOAD_SIZE 的问题。
- 建档头像流程改为：选择照片 → 浏览器本地加载 → 拖动与双指/滑杆缩放 → 640×640 裁剪压缩 → 百度人像分割 → 保存透明 PNG 文件ID。
- 原始大图不再发送给云函数；建档接口只接收不超过 900KB 的裁剪小图，表单最终仅保存云存储文件ID。
- 建档页面新增圆形取景框、棋盘透明背景、步骤状态、重新选择、缩放和“确认裁剪并抠图”操作。
- 建档接口复用 sxUploadAvatar 的百度人像分割能力，并返回透明头像临时预览地址。
- 内部建档调用与普通小程序头像调用保持隔离；普通小程序上传继续执行微信图片内容安全检查。
- 变更文件：education-student-onboarding.html、website-assets/education-student-onboarding.js、website-assets/education-student-onboarding.css、cloudfunctions/sxEducationPublic/index.js、cloudfunctions/sxUploadAvatar/index.js。
- 验证：相关 JavaScript 语法检查通过；tools/test-education-student-avatar-flow.js 通过；线上 HTML、JS、CSS 回读确认裁剪面板、压缩、手势和人像分割调用均已生效。
- 部署：sxEducationPublic、sxUploadAvatar 已部署成功；建档 HTML、JS、CSS 已发布正式静态托管并完成线上回读。
- 版本边界：产品版本仍为 2.1.0，本次记为第4次补丁；未创建新版本号，未提交小程序审核，未发布新的正式小程序版本，未推送 GitHub/Gitee。
