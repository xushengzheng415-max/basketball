# 开发记录

## 2026-07-23 官网 AEO / GEO 首轮优化

- 2026-07-24 部署纠正：首次部署误将仓库中 7 月 2 日旧版 `index.html` 覆盖到线上。通过 CloudBase 文件清单找回 7 月 22 日保留的 `website-assets/` 主视觉和文章中心设计体系，随后重建并重新部署新版黑橙官网首页。
- 新版首页定位统一为“赛小蜂篮球机构训赛经营增长平台”，采用原有球馆主视觉与品牌 Logo，整合机构经营、赛事、计分、数据沉淀和运营指南；无任何公开价格，保留 ICP 与公安备案。
- 公网回读已确认新版首页及 `website-assets/site.css` 生效，关键文案、双备案、样式资源均匹配，且没有价格残留。
- 优化 `/articles/` 内容中心：浏览器标题缩短为“篮球运营指南｜赛小蜂篮球”，3 篇深度文章之外接入 5 篇实用指南，增加分区标题、双列指南列表与响应式布局，减少宽屏留白；3 篇文章页面标题同步缩短。
- CloudBase 目录部署必须显式传入云端目录；已纠正一次目录上传落到根路径的问题，并分别回读确认 `/` 为新版机构训赛经营增长首页、`/articles/` 为新版内容中心。

- 分支：`codex/education-2.0`；本轮仅处理官网公开页面和搜索优化资料，未改动小程序及已有后台业务改动。
- 官网首页增加搜索抓取指令、Open Graph / Twitter 分享摘要、Organization / SoftwareApplication / FAQPage 结构化数据，以及与结构化数据一致的可见常见问题区。
- 新增 `robots.txt` 与 `llms.txt`，明确公开抓取权限并集中描述品牌、目标用户、核心能力和产品边界。
- 新增 `docs/AEO_GEO_AUDIT_2026-07-23.md`，记录搜索可见性基线、已完成事项、未实施原因和后续 P0-P2 工作。
- 新增篮球现场计分、临时比赛快速开赛、校园赛事管理三个独立答案型指南页，并加入首页入口、页面互链、Sitemap 和 `llms.txt`。
- 新增球队与球员库、篮球现场 MC 音效两篇指南；根据最新产品策略，官网已取消全部价格、套餐金额和 AggregateOffer 结构化价格，避免传播未确认的价格体系。
- 修复 `package.json` 中的中文描述乱码并补充产品关键词。
- 正式官网域名确认为 `https://www.sxfbasketball.cn/`；已补齐 canonical、`og:url`、绝对分享图片、结构化数据实体 URL、`sitemap.xml` 及爬虫 Sitemap 声明。
- 官网备案号确认为 `豫ICP备2026019672号-2`；已展示在首页和全部公开指南页底部，并链接工信部备案查询平台。
- 公安备案号已更新为 `豫公网安备41010202003962号`；官网全部公开页面展示公安徽章、备案号，并链接全国互联网安全管理服务平台。
- 官网已通过 CloudBase CLI 部署到环境 `cloudbase-d4g93f0re5f3274c1`，共上传 26 个静态文件；自定义域名 `https://www.sxfbasketball.cn/`、`robots.txt`、`sitemap.xml`、`llms.txt` 和指南页公网回读均返回 HTTP 200。
- 公网原始 UTF-8 字节回读验证首页、`llms.txt` 和指南页均包含 ICP 与公安备案号，且不存在旧价格或结构化价格残留。CloudBase 当前响应头未附带 charset，但 HTML 页面含 `<meta charset="utf-8">`。
- 验证：JSON-LD 和 `package.json` 可解析，页面可见 FAQ 与结构化 FAQ 数量一致，UTF-8 品牌文本正常，`git diff --check` 通过。

## 2026-07-01 小程序原型设计第一轮

### 本次目标

使用 `saixiaofeng-basketball` 技能启动赛小蜂篮球小程序原型设计，重点让第一版不只是工程骨架，而是具备篮球赛事运营和计分台的真实产品形态。

### 已完成

- 创建 Taro + React + TypeScript 小程序前端工程。
- 配置首页、计分台、赛事、球队、技术统计五类页面。
- 配置 GitHub 与 Gitee 双远端。
- 生成并纳入 `package-lock.json`，固定依赖解析结果。
- 首页升级为运营工作台：今日比赛、待确认阵容、待审核赛果、快捷入口、运营提醒、今日赛程。
- 计分台升级为比赛现场工作台：比分、节次、计时、同步状态、模式切换、场上阵容、主客队加分、犯规、暂停、换人、技术统计快捷键、事件流、撤销上一条、底部比赛控制。
- 数据层补充运营指标、提醒事项、场上阵容、计分模式和技术统计动作。

### 设计原则

- 第一屏必须是可用工作台，不做营销落地页。
- 计分场景优先满足高压、快速、少误触的操作。
- 比分、时间、节次、犯规、暂停和事件流必须可快速扫描。
- 所有后续接口应围绕比赛、球队、球员、事件、赛程、赛事、统计这些结构化对象扩展。

### 下一步建议

- 接入真实接口前，先补齐比赛详情页和赛前阵容确认流程。
- 给计分台增加本地事件队列、离线重试和冲突处理状态。
- 增加赛事报名、支付、家长端观赛和分享链路。
- 用微信开发者工具跑 `npm run dev:weapp` 后做移动端真机尺寸检查。

## 2026-07-01 微信开发者工具接入

### 已完成

- 补全依赖安装，固定 `webpack@5.88.2`，避免 Taro 3.6 与过新 Webpack 的 ProgressPlugin 参数不兼容。
- 新增 `babel.config.js`，安装 `babel-preset-taro@3.6.40`，让 Taro 正确编译 TypeScript/React。
- 在 `config/index.ts` 增加 `@ -> src` alias，修复页面组件导入。
- 成功运行 `npm run build:weapp`，生成 `dist/`。
- 已后台启动 `npm run dev:weapp` watch，微信开发者工具可直接读取 `dist/`。
- `npm run typecheck` 通过。

### 注意

- 本机 Node 版本是 v24.15.0，Taro 3.6 可以跑通，但长期建议使用 Node 18 或 Node 20 LTS。
- `project.private.config.json` 和 `.swc/` 为本地开发工具/缓存文件，已加入 `.gitignore`。

## 2026-07-01 页面空白修复

### 问题

微信开发者工具中导航栏和 TabBar 正常出现，但首页主体为空白。

### 处理

- 将 `src/app.tsx` 从函数组件改为 Taro 官方模板常用的 class App 写法，确保页面 children 在小程序运行时稳定挂载。
- 重新执行 `npm run build:weapp`，确认 `dist/pages/home/index.js` 包含首页内容。
- 重启 `npm run dev:weapp` watch，当前已重新进入监听状态。

## 2026-07-01 MVP 流程简化

### 用户反馈

首页和计分流程过于复杂，当前阶段只需要快速开赛、横屏计分、结束生成赛果，以及简单创建赛事和球员名单。

### 已调整

- 首页改为简单入口：进入比赛积分系统、最近赛果、创建赛事、球员名单。
- 比赛页改为两步流程：先输入两支队伍名称，再进入横屏计分盘。
- 计分盘支持 +1 / +2 / +3、撤销、重开、结束比赛。
- 结束比赛后写入 `latestMatchResult` 本地缓存，并自动回到首页展示最近赛果。
- 赛事页简化为赛事名称、地点、日期创建。
- 球员页简化为球员姓名和号码录入。

## 2026-07-01 横屏计分页路由修复

### 问题

用户反馈：首页进入比赛无反应，底部点击比赛后虽然横屏但页面为空白。

### 原因判断

`pages/scorer/index` 同时作为 TabBar 页面并配置 `pageOrientation: 'landscape'`，在微信开发者工具中容易出现横屏容器切换后页面主体不渲染的问题。

### 已调整

- 从底部 TabBar 移除比赛页，只保留首页、赛事、球员。
- 首页“进入比赛积分系统”改为 `Taro.navigateTo({ url: '/pages/scorer/index' })`，打开独立横屏页面。
- 首页入口从 Button 改成 View 样式按钮，减少 Button 默认行为干扰。
- 保留 `pages/scorer/index` 的 `pageOrientation: 'landscape'`，让独立计分页继续横屏。
- 已确认 `dist/app.json` 不再包含比赛 Tab，首页产物包含 `navigateTo`。

## 2026-07-01 切换原生小程序 MVP

### 问题

Taro React 构建产物在微信开发者工具中出现 TabBar 和导航栏正常，但页面主体持续空白的问题。多次确认 dist 页面 JS 有内容，但 Taro runtime 未稳定挂载页面 root。

### 决策

为了先跑通 MVP，不再依赖 Taro React 渲染层，改为微信开发者工具直接读取原生小程序产物 `native-dist/`。

### 已完成

- `project.config.json` 的 `miniprogramRoot` 改为 `native-dist/`。
- 新增原生小程序 `app.json/app.js/app.wxss`。
- 原生首页：进入比赛积分系统、最近赛果、创建赛事、球员名单入口。
- 原生横屏计分页：输入两队名称、开始计分、+1/+2/+3、撤销、重开、结束比赛并回首页生成赛果。
- 原生赛事页：创建赛事并本地保存。
- 原生球员页：添加球员并本地保存。

### 说明

Taro 源码暂时保留，后续可在 MVP 跑通后再决定是否迁回 Taro 或继续原生小程序开发。
## 2026-07-01 微信支付申请页面原型

### 背景

用户正在申请微信支付，当前先补齐小程序内可展示、可演示的支付相关页面，真实支付能力等商户号和支付参数通过后再接入。

### 已完成

- 新增“赛事服务”页面：展示赛事报名费、现场计分服务包、技术统计服务包。
- 新增“确认订单”页面：支持填写联系人和手机号，生成本地模拟订单。
- 新增“支付结果”页面：展示订单号、服务名称、金额和支付时间。
- 首页新增“购买赛事服务”入口，可进入赛事服务选择页。

### 说明

当前为模拟支付流程，暂未调用 `wx.requestPayment`。后续微信支付申请通过后，把确认订单页的本地模拟逻辑替换为后端下单和微信支付参数拉起即可。
## 2026-07-01 横屏比赛页适配

### 用户反馈

真机横屏下创建比赛和比赛计时页面显示不完整，顶部导航和底部安全区压缩后，原有竖向布局高度过大。

### 已调整

- 首页顶部“快速开始一场比赛”卡片改为可点击入口，移除重复的“进入比赛积分系统”按钮。
- 创建比赛页改为横屏紧凑布局：主队、客队、每节时长、计时方式两列排列。
- 计分页改为固定 100vh 网格布局，压缩顶部栏、计时区、比分卡和底部记录区。
- 新增每节时长、正计时/倒计时、开始/暂停、复位、下一节等时间控制。

### 说明

横屏真机可用高度明显低于竖屏，后续比赛页控件都按横屏一屏完整显示优先设计。
## 2026-07-01 每节时长设置优化

### 用户反馈

横屏设置每节时长时会弹出数字键盘，遮挡页面，无法清楚看到已设置的时间。

### 已调整

- 移除每节时长的数字输入框，避免横屏弹出系统键盘。
- 改为 `- / +` 微调和 6、8、10、12 分钟快捷选择。
- 选择时长后立即同步更新倒计时显示。
## 2026-07-01 比赛节数设置

### 用户反馈

快速比赛设置页缺少比赛总节数选项，只有每节时长和计时方式。

### 已调整

- 新增比赛节数设置，默认 4 节。
- 支持 `- / +` 调整总节数，并提供 2、4、6 节快捷选择。
- 计分页显示 `第 X / Y 节`。
- 点击“下一节”到最后一节时不再继续增加，并提示已到最后一节。
## 2026-07-01 首页赛果、赛事球员与我的页面

### 用户反馈

首页最近赛果需要支持左滑删除，创建赛事需要补齐主客队和球员选择，并在底部 TabBar 增加“我的”页面做基础设置。

### 已调整

- 首页最近赛果改为左滑卡片，左滑露出“删除”，删除后清空本地最新赛果。
- 创建赛事页新增主队名称、客队名称、主队球员、客队球员。
- 创建赛事页可以从球员库把球员加入主队或客队，也可以从已选名单中移除。
- 底部 TabBar 新增“我的”页面。
- “我的”页面提供小程序名称、默认每节时长、默认比赛节数、默认计时方式和清空本地测试数据。
- 快速比赛设置页会读取“我的”里的默认比赛参数。
## 2026-07-01 我的页面改为账号中心

### 用户反馈

“我的”页面不应承载默认比赛设置，应更像个人账号中心，包含登录、昵称修改和反馈机制。

### 已调整

- 移除“我的”页面里的默认每节时长、默认比赛节数和默认计时方式。
- “我的”页面改为个人账号中心：微信授权登录、头像、昵称编辑、退出登录。
- 新增意见反馈模块：功能建议、问题反馈、赛事合作三类，支持填写内容和联系方式。
- 反馈内容暂存本地，页面显示已提交数量。
- 快速比赛页不再读取“我的”页面设置，比赛参数仍在快速比赛创建页内设置。
## 2026-07-01 登录页入口

### 用户反馈

新增了 Taro 登录页源码，并希望小程序打开后先进入登录页。

### 已调整

- 保留 `src/pages/login` Taro 登录页源码，并确认 `src/app.config.ts` 已将 `pages/login/index` 放在第一位。
- 当前微信开发者工具读取 `native-dist/`，因此同步新增原生登录页 `native-dist/pages/login/index`。
- 原生 `native-dist/app.json` 已将 `pages/login/index` 放到第一位。
- 登录页支持微信授权登录、游客体验和用户协议勾选。
- 登录成功后写入 `loginProfile` 和 `userProfile`，并进入首页。
## 2026-07-01 登录页视觉同步

### 用户反馈

`src/pages/login/index.tsx` 和 `src/pages/login/index.scss` 有新版登录页视觉，需要同步到当前小程序预览。

### 已调整

- 保留 Taro 登录页新版结构和样式。
- 将新版登录页视觉同步到原生小程序 `native-dist/pages/login/index`。
- 原生登录页补齐蜂形标志、篮球、光束、球场线、底部登录面板、协议链接样式。
- `npm.cmd run typecheck` 通过，原生 JSON 和 JS 校验通过。
## 2026-07-01 退出登录机制

### 用户反馈

小程序需要退出机制，退出登录后回到授权主页面。

### 已调整

- “我的”页面退出登录增加确认弹窗。
- 退出时同时清除 `loginProfile` 和 `userProfile`。
- 退出后通过 `wx.reLaunch` 回到 `/pages/login/index` 授权登录页。
- “我的”页面未登录状态的登录入口也会回到授权登录页。
## 2026-07-01 首页组件图替换

### 用户反馈

首页组件图已经设计完成，需要替换到小程序首页。

### 已调整

- 新增首页素材目录 `native-dist/assets/home/`。
- 接入 `首页背景.png` 为首页底图，首页背景已转为高质量 JPG 以控制小程序包体。
- 接入 `主LOGO.png` 到首页快速比赛主视觉。
- 接入 `篮球小图标.png` 到赛事服务入口和快捷入口。
- 重写首页文案为正常 UTF-8 中文，修复显示乱码风险。
## 2026-07-01 登录页组件图替换

### 用户反馈

登录页仍然是旧画面，需要同步替换为已提供的组件图。

### 已调整

- 新增登录按钮素材目录 `native-dist/assets/login/`。
- 登录页背景改用首页背景素材。
- 登录页主视觉改用 `主LOGO.png`。
- 微信登录和游客体验按钮改用用户提供的图片按钮。
- 保留原有登录逻辑、游客体验逻辑和协议勾选逻辑。
## 2026-07-01 登录页动画与重复文字处理

### 用户反馈

登录页按钮需要上下浮动动效，中间大 LOGO 也需要轻微动效，同时页面中出现了多余英文文字。

### 已调整

- 移除登录页额外的品牌文字、英文和口号，避免与主 LOGO 图片内文字重复。
- 主 LOGO 增加轻微上下浮动和缩放动效。
- 登录按钮和游客按钮增加错峰上下浮动动效。
- 面板篮球小图标增加轻微浮动和旋转动效。
## 2026-07-01 登录页入场动画

### 用户反馈

登录页首次刷新出现时，按钮区域需要从下向上推出来。

### 已调整

- 底部登录面板增加从屏幕底部向上滑入的入场动画。
- 微信登录按钮和游客体验按钮增加延迟上浮入场，之后再进入轻微循环浮动。
- 面板篮球小图标同步增加上浮入场，避免与按钮动效割裂。
## 2026-07-01 游客体验直达计分

### 用户反馈

游客身份的核心是体验计分功能，不应先进入首页。

### 已调整

- 登录页“游客体验”改为直接进入 `/pages/scorer/index`。
- 微信一键登录仍进入首页。
- 游客模式仍会写入本地登录态，便于后续识别体验模式。

## 2026-07-01 赛事比赛二级流程与球衣号唯一性

### 用户反馈

添加球员时球衣号需要筛选，某个号码被选后其他球员不能再使用；赛事流程需要拆成多级页面：先创建赛事，再在赛事内创建比赛，最后在比赛页添加主客队球员。

### 已调整
- 球员页新增球衣号唯一性校验，重复号码会提示“号码已被占用”。
- 球员页显示已占用号码，方便录入时避开重复号码。
- 赛事页简化为“创建赛事 + 赛事列表”，不再直接混入比赛和球员选择。
- 新增赛事详情页 `native-dist/pages/tournament-detail/index`，用于在某个赛事下创建和查看比赛。
- 新增比赛球员页 `native-dist/pages/game-detail/index`，用于从球员库给主队、客队分配球员。
- 比赛球员页中，同一名球员加入一方时会自动从另一方移除，避免同一人同时出现在主客队。
- `native-dist/app.json` 已注册新的二级页面，微信开发者工具可直接跳转。
## 2026-07-01 场次文案与待选球员池

### 用户反馈

已创建赛事后应创建“场次”，不再使用“创建比赛”的说法；赛事和场次日期都应通过日期表选择。比赛球员页中，球员加入主队或客队后，应从下方球员库自动减少，不再重复显示。

### 已调整
- 赛事详情页文案统一为“创建场次”“保存场次”“赛事场次”。
- 赛事日期和场次日期均改为微信小程序日期选择器，不再手动输入日期文本。
- 比赛球员页新增待选球员池 `availablePlayers`，已加入主队/客队的球员会自动从球员库隐藏。
- 移除主队/客队球员后，该球员会自动回到可选球员库。
## 2026-07-01 个人资料确认修改与 PC 后台原型

### 用户反馈

个人资料需要“确认修改”按钮；需要一个 PC 后台查看小程序使用数据，包括微信号、电话资料，并能查看用户反馈信息。

### 已调整
- “我的”页个人资料改为草稿编辑模式，昵称、微信号、手机号需要点击“确认修改”后保存。
- 用户资料保存后会写入本地 `userProfile`，同时维护一份后台数据结构 `adminUsers`。
- 反馈提交时会带上当前用户昵称、微信号和手机号，方便后台关联用户。
- 新增 PC 后台原型 `admin/index.html`，包含数据概览、用户资料表、反馈信息表。
- 新增后台示例数据 `admin/sample-data.json` 和说明文档 `admin/README.md`。
- 新增 `npm run admin:serve`，可在本地启动 PC 后台静态服务。

### 说明

当前小程序数据仍存储在用户设备本地，PC 后台无法直接读取每台手机的本地缓存。正式上线查看真实用户数据时，需要接入微信云开发或自建服务端数据库。
## 2026-07-01 去除微信号与手机号手填

### 用户反馈

登录授权后手机号和微信相关资料应由授权流程获取，个人资料里再让用户手填微信号和手机号没有意义。

### 已调整
- “我的”页移除微信号、手机号手动输入框。
- 个人资料保留昵称编辑和“确认修改”按钮。
- “我的”页新增授权状态展示：微信授权标识、授权手机号。
- 反馈数据结构改为携带 `wxOpenId` / `wxUnionId` / `phoneNumber` 授权字段。
- PC 后台用户表改为展示“微信授权标识”和“授权手机号”，不再展示手填微信号。

### 说明

手机号需要走微信手机号授权并由服务端解密保存。真实微信号通常不会通过小程序 API 直接开放，后台更适合使用 openid / unionid 作为微信授权标识。
## 2026-07-01 球员库命名与删除功能

### 用户反馈

底部“球员”页应命名为球员库，并支持删除球员。

### 已调整
- 球员页导航标题从“球员名单”改为“球员库”。
- 页面主标题和列表标题统一改为球员库语义。
- 球员列表每一项新增“删除”按钮。
- 删除球员前增加确认弹窗，避免误删。
- 重写球员库页面为正常 UTF-8 中文，降低后续乱码风险。
## 2026-07-01 付费功能产品化入口

### 用户反馈

免费功能基本完成，后续付费功能主要分两块：MC 系统和计分板 2.0 球员数据统计。付费模式包含单场 9.9、首场 4.9、单月 39.9、首月 19.9、买断 199、惊喜价 99。

### 已调整
- 购买页重构为“付费功能 + 购买套餐”结构。
- 新增付费功能说明：MC 现场音频系统、计分板 2.0 球员数据统计。
- 新增三档套餐：单场体验、月度会员、买断授权，并展示首场价、首月价、惊喜价。
- 新增 MC 音频系统预览页 `native-dist/pages/mc-system/index`，按比赛音效、进攻防守音乐、暂停休息音乐、仪式音乐展示。
- 新增计分板 2.0 预览页 `native-dist/pages/stats-scorer/index`，展示球员级数据统计能力。
- 订单页去除手填联系人/手机号，改为账号信息和授权手机号展示。
- 支付结果页改为正常中文，并展示 Pro 订单金额。
- 首页付费入口改为“升级 Pro 功能”，指向 MC 系统和计分板 2.0。

### 说明

当前不把音频文件直接打入小程序包，避免主包体积过大。后续正式接入时建议使用云存储、分包或按需下载方式承载 MC 音频资源。
## 2026-07-01 免费计分板犯规次数

### 用户反馈

免费版计分板也需要记录犯规次数。

### 已调整
- 免费横屏计分板为主队、客队新增犯规次数显示。
- 每队新增 `+犯规` 和 `-` 按钮，用于增加或减少犯规次数。
- 犯规操作进入最近记录，并支持“撤销”回退。
- 重开比赛会同步清空比分、犯规和记录。
- 结束比赛生成赛果时会保存主客队犯规次数。
- 首页最近赛果卡片新增犯规次数展示。
## 2026-07-01 云开发环境接入

### 用户反馈

云开发已购买，环境名称为 `cloudbase`，环境 ID 为 `cloudbase-d4g93f0re5f3274c1`。

### 已调整
- 小程序启动时通过 `wx.cloud.init` 初始化云开发环境。
- `App.globalData` 中记录云环境名称和环境 ID，方便页面和后续云函数复用。
- `project.config.json` 新增 `cloudfunctionRoot`，预留 `cloudfunctions/` 云函数目录。

### 后续计划

后续可把登录授权手机号、用户资料、反馈、订单、权益、赛事和计分数据逐步从本地缓存迁移到云数据库。
## 2026-07-01 MC 系统接入计分盘

### 用户反馈

云开发已接入后，希望先把 MC 系统做到页面里。蜂鸣器用于暂停、每节结束和比赛结束；三分球需要长按确认并显示进度条，长按到终点才加 3 分并播放三分音效；比赛中需要进攻音乐、防守音乐按钮随机播放；新增常规音效包含 2 分进球有效、投篮未进、欢呼声。同时开发者工具出现首页 WXML 编译错误。

### 已调整
- 修复 `native-dist/pages/home/index.wxml` 标签错误，恢复首页正常中文文案。
- 新增小体积本地音频资源到 `native-dist/assets/audio/`：蜂鸣器、三分球、2 分进球有效、投篮未进、欢呼声、进攻音效、防守音效。
- 免费计分盘中加入 MC 控制条：进攻音乐、防守音乐、2 分有效、投篮未进、欢呼声。
- 暂停、每节结束、正常比赛结束会尝试播放蜂鸣器音效。
- 暂停和每节结束会弹窗询问是否播放暂停/休息音乐；当前先使用已接入的短音频作为原型，长音乐后续建议放云存储按需播放。
- 三分加分改为长按确认，显示进度条；长按到终点才加 3 分并播放三分球音效，中断则不加分。
- MC 预览页改为真实音效列表，已购买 Pro 后可播放本地小音效。

### 说明

暂停休息音乐文件较大，直接打进主包容易影响小程序包体。后续建议上传到云存储，用 fileID 按需播放。
## 2026-07-01 云函数与数据库集合补齐

### 用户反馈

云开发已经连上，希望补齐云函数和数据库；如果无法直接建数据库，需要说明应该创建哪些集合。

### 已调整
- 新增云函数：`sxLogin`、`sxSaveUser`、`sxSubmitFeedback`、`sxCreateOrder`、`sxSaveMatchResult`、`sxAdminDashboard`。
- 新增小程序端云函数调用工具 `native-dist/utils/cloud.js`，云端未部署时会静默兜底，不影响本地基础功能。
- 登录、游客进入、资料保存、反馈提交、订单创建、赛果保存已接入云函数调用。
- 新增 `cloudfunctions/README.md`，列明需要创建的数据库集合、权限建议和索引建议。

### 需要在云开发控制台创建的集合

- `sx_users`
- `sx_feedback`
- `sx_orders`
- `sx_entitlements`
- `sx_match_results`

### 说明

当前订单云函数仍是原型阶段模拟支付，会写入 `mock_paid` 订单和 Pro 权益。正式接微信支付后，需要把权益写入放到支付回调确认之后。
## 2026-07-02 Pro 权益云端校验

### 用户反馈

微信支付回调等小程序认证结束后再接入，今天先完成昨天计划中的付费权限云端化。

### 已调整

- 新增云函数 `sxCheckEntitlement`，从 `sx_entitlements` 查询当前用户是否拥有指定功能权益。
- MC 系统和计分盘现在优先读取云端权益，云函数不可用时才使用本地已支付订单兜底。
- 原型下单云函数 `sxCreateOrder` 写入更完整的权益结构：单场含 `remainingUses`，月卡含 `expiresAt`，买断长期有效。
- 原型支付成功后会缓存 `proEntitlement`，返回计分盘或 MC 页时能立即看到解锁状态。

### 后续计划

正式接入微信支付后，把 `sxCreateOrder` 的模拟支付改成 `pending` 下单，并在支付回调确认成功后写入 `sx_entitlements`。

## 2026-07-02 AI 比分播报三种风格

### 用户反馈

Pro 版模板语音播报第一版先做 3 个播报风格，希望播报不要太生硬。

### 已调整

- 新增 `native-dist/utils/scoreVoice.js`，封装比分播报文案生成逻辑。
- 第一版支持 3 种风格：标准播报、现场 MC、儿童友好。
- 播报文案会根据节次、时间、比分、分差和最近一次得分自动生成不同表达。
- 计分盘新增 AI 播报控制条，可切换风格并点击“播报比分”。
- AI 播报仍归属 MC/Pro 权益，未解锁时提示购买 Pro，不影响免费计分功能。

### 后续计划

确认口播风格后，再接入云函数 TTS，把当前生成的文案转换为真实语音并播放。

## 2026-07-02 腾讯云 TTS 比分播报接入

### 用户反馈

用户已领取腾讯云语音合成免费资源包，希望先做基础版语音播报，不下载客户端 SDK。

### 已调整

- 新增云函数 `sxCreateScoreVoice`，服务端调用腾讯云 `TextToVoice` 生成 mp3 语音。
- 云函数会先校验 `mc_system` 权益，未解锁 Pro 不生成语音。
- 云函数把生成的音频上传到云存储 `score-voice/`，并返回临时播放链接。
- 计分盘“播报比分”按钮从弹窗文案升级为调用云函数并播放真实语音。
- 腾讯云密钥只通过云函数环境变量读取，不写入小程序前端。

### 需要配置

- 上传并部署云函数 `sxCreateScoreVoice`。
- 在云函数环境变量里配置：`SXF_TTS_SECRET_ID`、`SXF_TTS_SECRET_KEY`、`TTS_REGION`、`TTS_VOICE_TYPE`。
- 建议新增数据库集合 `sx_voice_logs`，用于记录每次 AI 播报生成日志。

## 2026-07-02 AI 播报音色命名

### 用户反馈

已选择三款腾讯云基础音色：男声 `101054` 命名为赛小智，女声 `101001` 命名为赛小瑜，童声 `101015` 命名为赛小萌。

### 已调整

- 标准播报默认使用赛小瑜 `101001`。
- 现场 MC 默认使用赛小智 `101054`。
- 儿童友好默认使用赛小萌 `101015`。
- `sxCreateScoreVoice` 生成日志和返回值增加 `voiceName`，方便后续后台查看使用了哪个播报音色。

## 2026-07-02 云函数环境变量前缀修正

### 用户反馈

部署 `sxCreateScoreVoice` 环境变量时报错：环境变量 Key 不能包含 `SCF_`、`QCLOUD_` 或 `TENCENTCLOUD_` 前缀。

### 已调整

- 将腾讯云 TTS 密钥环境变量从 `TENCENTCLOUD_SECRET_ID` / `TENCENTCLOUD_SECRET_KEY` 改为 `SXF_TTS_SECRET_ID` / `SXF_TTS_SECRET_KEY`。
- 更新云函数错误提示和文档，避免使用云函数保留前缀。

## 2026-07-02 TTS 云函数包体优化

### 用户反馈

微信开发者工具自动真机调试时报错：`source size 2641KB exceed max limit 2MB`。

### 已调整

- `sxCreateScoreVoice` 移除 `tencentcloud-sdk-nodejs-tts` 依赖，改为使用 Node.js 内置 `https` 和 `crypto` 直接调用腾讯云 `TextToVoice` API。
- 云函数 `package.json` 仅保留 `wx-server-sdk`，显著降低上传包体大小。
- 保持原有功能不变：校验 Pro 权益、生成 TTS、上传云存储、返回临时播放链接。

### 说明

自动真机调试对云函数源码包有 2MB 限制，尽量不要把大型 SDK 或 `node_modules` 一起打进需要真机调试的云函数。

## 2026-07-02 微信支付 JSAPI 骨架

### 用户反馈

微信支付商户号 `1114763686` 已开通并关联小程序 `wx06d735da15276acd`，希望继续配合接入真实支付。

### 已调整

- 新增云函数 `sxCreateWxPayOrder`，用于微信支付 JSAPI 预下单并生成 `wx.requestPayment` 参数。
- 新增云函数 `sxWxPayNotify`，用于支付通知解密、订单状态更新和 Pro 权益写入。
- 订单页优先尝试真实微信支付；若支付云函数配置未完成或下单失败，会自动降级到原来的模拟支付，保证开发流程不断。
- 文档补充微信支付相关环境变量和证书/密钥注意事项。

### 后续计划

配置商户 API 证书序列号、私钥、APIv3 密钥和支付通知 URL 后，测试真实 0.01 或低价支付链路，再关闭模拟支付兜底。

## 2026-07-02 ������԰����Ż�

### �û�����

�Զ�������Ա����`source size 2642KB exceed max limit 2MB`��

### �ѵ���
- �� MC ��Ч mp3 �� `native-dist/assets/audio` ����Ŀ¼�Ƴ�������������Ժ��ϴ������� 2MB��
- �Ʒ��̸�Ϊ���ƴ洢 `mc-mp3/` ��ȡ MC ��Ч�����ڲ���ǰͨ�� `wx.cloud.getTempFileURL` ת����ʱ���ŵ�ַ��
- �޸��Ʒ��̺� AI �ȷֲ���ģ���е������ַ������ָ����Ľ����� JS �﷨��
- ��ǰ `native-dist` ����Լ 1335.6KB������������� 2MB ���ơ�

## 2026-07-02 计分盘 MC 侧边手柄

### 用户反馈

横屏计分盘顶部音效和播报按钮过多，主计分区显得拥挤，希望改成类似游戏虚拟手柄或隐形侧边栏。

### 已调整
- 移除计分盘顶部常驻 MC 音效区和播报区，让比分、时间、犯规成为主界面焦点。
- 新增右侧浮动 MC 手柄入口，点击后滑出半透明音效盘。
- 音效盘内保留进攻音乐、防守音乐、2 分有效、投篮未进、欢呼声，以及三种 AI 比分播报风格。
- 保持 Pro 权益校验不变，未解锁用户仍可正常使用免费计分功能。

## 2026-07-02 计分盘左侧隐藏栏优化

### 用户反馈

右侧透明圆形 MC 入口观感不够好，计分盘仍显拥挤；犯规加减条和 `+犯规` 文案过长，不够协调。

### 已调整
- 将 MC 控制台从右侧圆形入口改为左侧隐藏侧边栏，默认只露出窄条拉手。
- 侧边栏加入“快捷自定义”区域，预留常用功能配置入口。
- 主界面新增轻量快捷点：进攻、欢呼、播报，便于快速触发高频动作。
- 犯规操作改为紧凑的 `-` / `+` 两个小按钮，减少横向占位。

## 2026-07-02 MC 控制台基础逻辑收敛

### 用户反馈

MC 控制台需要先聚焦基础功能：取消主界面三个小快捷按钮；快捷自定义先保留欢呼与空位；进攻、防守音乐互斥循环播放，并用按钮颜色表达播放状态；AI 播报不要放在抽屉里。

### 已调整
- 移除主界面 `攻 / 呼 / 播` 三个小快捷按钮。
- 快捷自定义区改为 `欢呼 / 空 / 空 / 空`，右侧增加编辑入口样式。
- 进攻、防守音乐改为互斥循环播放：点进攻会停防守，点防守会停进攻，再点当前播放按钮会暂停。
- 进攻、防守按钮播放中变黄色，暂停后恢复默认色。
- AI 播报风格与播报按钮移到中间控制区的“重开”下方。
- 云音效临时地址失败时会在控制台打印全部候选 fileID，便于对照云存储真实 fileID。

## 2026-07-02 云音效目录修正

### 用户反馈

云存储真实 fileID 示例为：`cloud://cloudbase-d4g93f0re5f3274c1.636c-cloudbase-d4g93f0re5f3274c1-1446269281/mc-mp3/比赛音效/投篮未进音效.mp3`。

### 已调整
- 比赛音效优先从 `mc-mp3/比赛音效/` 目录读取，同时保留根目录候选。
- 进攻、防守音乐优先从 `mc-mp3/进攻防守音乐/` 目录读取，同时保留根目录候选。
- 继续通过 `wx.cloud.getTempFileURL` 转临时 URL 后播放。

## 2026-07-02 云存储文件夹结构对齐

### 用户反馈

云存储 `mc-mp3` 下实际包含 `暂停休息音乐/`、`比赛音效/`、`进攻防守音乐/` 三个目录。

### 已调整
- 比赛音效统一优先读取 `mc-mp3/比赛音效/`。
- 进攻、防守音乐统一优先读取 `mc-mp3/进攻防守音乐/`。
- 新增 `rest` 休息音乐通道，暂停、本节结束时确认播放 `mc-mp3/暂停休息音乐/` 下的休息音乐。

## 2026-07-02 MC 音频单通道播放

### 用户反馈

所有音频只能出一路，不能叠加播放。

### 已调整
- 将计分盘内的短音效、循环音乐、AI 播报统一到同一个 `InnerAudioContext`。
- 播放任意新音频前都会先停止当前音频，避免进攻/防守音乐、欢呼声、蜂鸣器和播报叠加。
- 播放短音效或 AI 播报时会清除进攻/防守/休息音乐的播放状态。

## 2026-07-02 真机音效兜底与比分动效

### 用户反馈

三分音效应在长按开始时播放，长按中断也要完整播放音效；电脑端音效正常，但预览或真机提示 MC 音效未配置；比分数字希望更大，并有加分特效。

### 已调整
- 三分长按开始时立即播放三分音效，长按完成后只负责加 3 分。
- 新增云函数 `sxGetAudioUrl`，用于真机端云存储临时 URL 兜底转换。
- 小程序端先尝试 `wx.cloud.getTempFileURL`，失败后自动调用 `sxGetAudioUrl`。
- 主客队比分字体放大，加分时触发短促放大和发光动画。

## 2026-07-02 休息音乐真实文件名补齐

### 用户反馈

暂停休息音乐目录下实际文件为 `Fort Minor - Remember the Name_L.mp3` 和 `Studio 99 - Its My Life_L.mp3`，点击播放休息音乐未命中。

### 已调整
- `rest` 休息音乐通道加入两首真实云存储文件名。
- 暂停和节间休息弹窗确认播放时，会从这两首休息音乐中随机播放。

## 2026-07-02 休息音乐停止入口

### 用户反馈

播放休息音乐后没有暂停或取消按钮，用户无法主动停止。

### 已调整
- 播放进攻、防守或休息音乐时，计分盘底部显示音乐播放状态条。
- 状态条右侧新增“停止”按钮，可立即停止当前音乐。
- 停止后同步清除按钮播放状态。

## 2026-07-03 MC 音效设置 2.0

- 新增 `native-dist/pages/mc-settings/`，用于管理 2分/3分音效开关、休息/进攻/防守/氛围音乐随机或固定播放、6 键快捷键动作替换。
- 计分盘 MC 抽屉支持 6 键蓝牙小键盘快捷键：可监听按键、绑定键值、触发 6 个自定义音效。
- 计分盘开始比赛时尝试播放比赛开始音效，2分/3分音效会遵守设置页开关，进攻/防守/休息音乐遵守随机/固定策略。
- `我的` 页面新增 MC 音效设置入口；计分盘抽屉保留音效与快捷键设置入口。
- PC 后台 MC 音乐库类型扩展为：2分音效、3分音效、休息音乐、防守音乐、进攻音乐、蜂鸣器、比赛开始音效、播报音色库、欢呼声、掌声、投篮未进、氛围音效。
- `sxSaveAudioLibrary` 云函数扩展 channel 白名单，支持新增音效类型。

## 2026-07-03 MC settings local audio

- Fixed native-dist/app.json page registration JSON so pages/mc-settings/index can be resolved by WeChat DevTools preview.
- Rebuilt native-dist/pages/mc-settings as the MC audio settings page, including six-key shortcut action mapping and local audio import/clear controls.
- Added local audio playback priority on scorer page: when local audio chaining is enabled, imported device audio plays first and cloud audio remains the fallback.
- Verified with node syntax checks, JSON parsing, and npm run build:weapp.

## 2026-07-06 计分板 2.0 原型接入

- 按横版/竖版比分板原型重做 `native-dist/pages/scorer` 页面骨架。
- 接入已拆解素材目录到 `native-dist/assets/pages/scorer-v2`，包含背景、技术统计图标、队标和球员头像。
- 新增横竖屏自适配布局、首发/替补名单区域、外置 MC 音效面板、技术统计区。
- 保留快速比赛设置、计分、犯规、暂停、换边、重置确认、长按三分进度和比分动画。
- 通过 `node --check native-dist/pages/scorer/index.js` 语法检查。

## 2026-07-06 计分设置页乱码修复

- 修复 `native-dist/pages/scorer/index.wxml` 中中文实体被小程序原样显示的问题，改为从 JS 文案字典绑定展示。
- 修复 `native-dist/pages/scorer/index.json` 的页面标题编码问题。
- 保留比赛设置页左上角返回按钮，并通过 `goBack` 兼容无历史栈返回首页。
## 2026-07-08 repository sync checkpoint

- Cleaned BOM headers from native mini-program files that caused WeChat DevTools parse/startup failures.
- Verified native-dist JSON parsing and JavaScript syntax with a Node-based scan.
- Prepared pending native-dist pages, shared tab bar, scorer updates, assets, and prototype documents for GitHub/Gitee sync.


## 2026-07-09 Account roster cloud sync

- Added deployed cloud function `sxSyncRoster` using the authenticated WeChat OPENID and the existing `sx_users` record.
- Added `native-dist/utils/roster-sync.js` to synchronize teams, team drafts, team categories, and players between local cache and cloud.
- Team management and player pages now pull cloud data on entry and upload after create, edit, enable/disable, or delete operations.
- Verified JavaScript syntax with `node --check`; deployed `sxSyncRoster` successfully to `cloudbase-d4g93f0re5f3274c1`.
- First migration: open Team Management once in the developer tool to upload its local roster, then open the same page in phone preview to download it.

## 2026-07-16 v1.5.1 experience iteration

- Branch: `codex/education-2.0`.
- Scope: coach education workflow, AI period reports, workbench paid-unlock experience, login authorization, recent matches, scoreboard controls, tournament refinements, roster sync, and the unified six-tab navigation.
- Version: mini-program and package semantic version `1.5.1`.
- Release boundary: upload only as a WeChat development/experience candidate. Do not submit for review and do not publish as a production release.
- Verification: JavaScript and JSON scans, WXML expression scan, TypeScript check, Git diff check, and WeChat DevTools compile/upload checks are recorded in the release handoff.
- Local-only exclusions: `service-account-h5/`, Lark authorization screenshots, and generated Python cache files are not part of this release.
- Detailed handoff: see `DEVELOPMENT_LOG_2026-07-16.md` for the complete education workflow, workbench, tournament, login, scoreboard, repository, WeChat experience upload, Feishu Base registration, and Knowledge Base announcement status.
- Release process: `AGENTS.md` now requires every version iteration to complete semantic version verification, scoped validation, dual-remote sync, WeChat release-stage recording, public-facing Feishu Base notes, a Feishu Knowledge Base announcement, and post-write readback verification.

## 2026-07-18 校区负责人增长版 62 页接入

- 分支：`codex/education-2.0`。
- 新增 `native-dist/pages/campus-manager/`，按最终原型链路接入 62 个校区负责人页面；采用模块内共享组件统一自定义导航、字号、卡片间距、指标栅格和橙黑视觉样式。
- 返回键规则：页面栈存在时优先 `navigateBack`；直接打开页面时按映射返回明确父路由；校区负责人根页无历史栈时回到首页角色入口，不进入教练工作台。
- 首页角色选择器新增跳转：选择“校区负责人”进入校区经营工作台，选择“教练”保持进入现有教练工作台。
- 页面数据当前为原型联调用本地数据；62 页层级、下钻、提醒、保存、审核、发布与导出交互已建立，可在后续接口阶段替换 `screen-data.js` 数据层。
- 验证：62/62 页面已注册，62/62 返回父路由有效，64 个模块 JavaScript 文件通过 `node --check`，WXML 复杂表达式扫描为空，模块体积约 144.6 KB。
- 已知项目级阻碍：`npm run typecheck` 仍因 `config/index.ts` 的 `import.meta` 与现有 TypeScript `module` 配置不兼容而失败；该文件不属于本次页面范围，未修改。
- 范围保护：未修改任何 `native-dist/pages/coach-*` 页面及用户现有改动。

## 2026-07-18 开发工具测试身份切换

- 教练工作台顶部“教练端”身份标识在微信开发者工具的 `develop` 环境显示“测试⌄”，可直接进入校区负责人工作台。
- 校区负责人页面的身份标识提供对称测试入口，可返回教练工作台；使用 `redirectTo` 避免两个角色页面反复压栈。
- 测试入口仅在 `platform=devtools` 且环境为 `develop` 时启用，不写入 `loginProfile`、`userProfile` 或后台权限数据；体验版与正式版只展示后台确定的身份。
- 验证：教练工作台与校区负责人共享组件通过 JavaScript 语法检查，校区负责人首页路由已注册。

## 2026-07-18 校区经营驾驶舱与底部导航修正

- 校区负责人一级工作台改为紧凑经营驾驶舱，不再渲染 16 个纵向长列表入口。
- 驾驶舱包含：本月课消/目标完成/待处理三项总指标、今日待处理、经营健康、8 个教务快捷入口、5 项增长机会（含平台协助）。
- M01 的 16 个一级业务路径全部保留并可点击进入；详情页继续使用原有卡片/表单结构。
- M01 复用项目公共 `components/sxf-tabbar` 六栏底部导航，并高亮“教务”；仅一级工作台显示，详情页不重复展示。
- 增加底部导航安全区留白，避免驾驶舱最后一行被固定导航遮挡。
- 验证：62 个原型页面、62 条返回链路、18 个驾驶舱目标路由与底部导航注册均通过；模块 JavaScript/JSON 与 WXML 复杂表达式检查通过。
- 微信开发者工具 CLI 预览编译成功：AppID `wx06d735da15276acd`，总包约 2.1 MB，校区负责人分包约 104.8 KB。

## 2026-07-18 负责人数字通知与逐步处理闭环

- 校区经营驾驶舱新增“负责人待办通知”主入口，初始聚合 11 条待处理事项；今日待处理卡片和教务快捷入口同步显示业务数字角标。
- 新增系统编排页 `native-dist/pages/campus-manager/task-inbox/`，将超时未点名、续费风险、异常更正和经营报告拆成 4 组任务，每组提供 3 个明确处理步骤和对应业务页面入口。
- 新增 `task-data.js` 本地任务数据层：完成当前步骤后持久化进度，返回驾驶舱自动重算待办总数、分项数字角标与完成进度；全部完成时 11 条待办归零。
- 当前仅实现原型联调用本地状态，不伪造后台身份、权限或任务接口；后续可由真实负责人任务接口替换数据层。
- 路由口径保持 62 张正式 PNG 原型页面，并增加 1 个不计入原型数量的系统待办编排页，共注册 63 个校区负责人页面。
- 验证：62 条原型路由、1 条系统扩展路由、17 个驾驶舱目标路由均通过；66 个 JavaScript 文件、65 个 JSON 文件和 64 个 WXML 文件通过语法及复杂表达式检查。
- 微信开发者工具 CLI 预览产物生成成功：总包 2,248,182 bytes，校区负责人分包 124,261 bytes。

## 2026-07-19 教务未提交改动收口

- 分支：`codex/education-2.0`。
- 收口范围：教练端教务页面、教务账户状态与开户入口、校区负责人 62 个原型页面 + 1 个系统待办页、PC 端开户页、教务权益下发与周期报告云端校验。
- 修正开发工具测试身份边界：仅 `platform=devtools` 且环境为 `develop` 时显示角色切换，体验版与正式版不再出现测试入口。
- 恢复负责人驾驶舱遗漏入口：负责人待办通知、请假补课、异常更正、增长任务、家长反馈、成长内容、转介绍线索与平台协助均可从一级工作台进入；18 个驾驶舱目标路由全部可达。
- 修正任务完成闭环：4 组共 11 条待办全部完成后，总数、分项角标和进度同步归零/100%。
- 验证：80 个教务相关 JavaScript 文件通过 `node --check`，65 个 JSON 文件解析通过，63/63 校区负责人页面注册及四件套完整，84 条页面引用无失效路由，WXML 复杂表达式扫描为空；PC 开户页内联脚本语法检查通过。
- 微信开发者工具 CLI 实际预览编译成功：AppID `wx06d735da15276acd`，总包 2,838,669 bytes，主包 380,387 bytes，校区负责人分包 707,558 bytes。
- 项目级 `npm run typecheck` 仍仅因既有 `config/index.ts` 的 `import.meta` 与 TypeScript `module` 配置不兼容而失败；该项目级配置不在本次页面范围内，未修改。
- 范围保护：`AGENTS.md` 图标规范、`native-dist/pages/scorer-board/` 音频路径、`native-dist/pages/mc-system/`、`native-dist/pages/stats-scorer/`、飞书授权截图、`service-account-h5/` 与 Python 缓存未纳入本次教务提交。

## 2026-07-20 v2.0.1 校区经营与教务体验发布

- 分支：`codex/education-2.0`。
- 版本：项目及依赖锁定文件统一更新为 `2.0.1`。
- 校长端首页以销课统计、实收统计和四类待办为核心，管理中心收敛为教练管理、学员管理，并保留可排序的更多管理抽屉。
- 校长端待办流程改为业务动作完成后自动消减数字并闭环，不再要求返回待办页重复确认；点名监督从负责人异常处理主流程中移除。
- 销课与实收报表支持在线查看明细，并提供 `1周 / 1月 / 1季度 / 1年` 和多校区筛选；导出降为查看后的可选操作。
- 校长端与教练端的预览身份切换改为微信原生选择器，并取消仅开发者工具可用的错误环境限制，手机预览可双向切换。
- 补齐课程、班级、教练、学员、请假补课、续费风险、异常更正、权限与任务详情等页面的进出口和状态联动。
- 同步纳入 MC 音效、计分板与数据计分页的已跟踪体验修正。
- 发布阶段：代码版本同步 GitHub 与 Gitee；本次不代表微信正式版已提交审核或发布。
- 本地专用排除项：`service-account-h5/`、飞书授权截图和 Python 缓存继续不纳入主仓库。

## 2026-07-20 v2.0.1 正式版收口与代码上传

- 分支：`codex/education-2.0`；正式版收口提交：`b9f3117037d0f6bb9e23e61233981cde9abf829a`。
- 发布范围：在既有 2.0.1 校区经营与教务体验基础上，纳入赛事创建/编辑、赛事球队与赛程管理、横屏计分板、暂停与节间休息倒计时、MC 自定义音效及计分操作修复。
- 横屏计分板：进攻计时默认关闭；临时主客队默认“赛小蜂勇者 / 赛小蜂闪电”；球员得分保留 +1 / +2 / +3；暂停默认 1 分钟并询问比赛总时间是否继续；节间休息 2 分钟、中场休息 15 分钟；倒计时结束停止音乐并鸣笛。
- MC 音效：2 分、3 分音效加入统一自定义音效池，可与其他音效互相替换；修复替换窗口和计分板播放配置未真正联动的问题；欢呼声使用最新云文件。
- 验证：修改 JavaScript 语法、JSON 解析、WXML 复杂表达式、删除演示资源引用和 `git diff --check` 均通过；`npm run build:weapp` 通过（Taro 3.6.40 / Webpack 编译成功）。
- 已知基线问题：`npm run typecheck` 仍因原有 `config/index.ts` 的 `import.meta` 与 TypeScript module 配置不兼容而失败，本次未越界修改项目级配置。
- 仓库同步：GitHub `origin` 与 Gitee `gitee` 的 `codex/education-2.0` 均已核对指向 `b9f3117`。
- 微信阶段：已通过微信开发者工具 CLI 上传代码版本 `2.0.1`，AppID `wx06d735da15276acd`，代码包总计 2,932,066 bytes；当前是“代码版本已上传”，尚不等于微信审核通过或线上发布。
- 本地排除：`content-output/`、公众号/文档材料、飞书授权截图、`service-account-h5/` 和 Python 缓存未纳入提交。

## 2026-07-24 篮球/足球双账号 CloudBase 统一入口

- 分支：`codex/education-2.0`；新增 `tools/sxf-cloud.ps1` 与 `tools/sxf-cloud.cmd`。
- 统一别名：`basketball` 绑定 `E:\Documents\sxf-basketball` / `cloudbase-d4g93f0re5f3274c1`，`football` 绑定 `E:\Documents\sxf-football` / `cloud1-7g8ckb3c7815a011`。
- 两套 CloudBase API Key 通过 Windows 当前用户 DPAPI 分别加密保存在仓库外的 `%LOCALAPPDATA%\SxfCloud\credentials\`；脚本、日志与 Git 不包含明文密钥。
- 每次调用都在独立临时配置目录执行 `tcb login --cloudbase-api-key`，命令结束后立即清理，避免复用电脑上已有的单账号 CLI 登录态。
- 安全边界：禁止通过命令参数手动覆盖 `envId`；执行前显示目标项目、本地路径和云环境。
- 验证：CloudBase CLI 3.6.4 分别使用两套 API Key 成功完成只读云函数列表查询；篮球命中 `sxLogin` 等函数，足球命中 `webLoginApi` 等函数，证明账号与环境隔离正确。
- 用户 PATH 已加入 `E:\Documents\sxf-basketball\tools`；新开终端后可在任意目录执行 `sxf-cloud`。
- `AGENTS.md` 已记录双账号别名、统一入口、凭据安全边界与新会话复用规则；同机同 Windows 用户的新 Codex 会话不应重复索要授权或 API Key。
- 本次仅执行配置、登录兑换与只读列表验证，未部署函数、未修改数据库或云端资源。

## 2026-07-25 篮球 CloudBase 跨账号迁移与共享资源修复

- 篮球云开发环境已从旧腾讯云账号迁移至公司 A 账号下的 `sxf-basketball-d9gp6yt0rd1f7be4d`；旧环境已释放，小程序与旧账号已解绑。
- 新环境完成数据库、云存储、19 个正式云函数、静态托管、HTTP 路由、触发器和关键环境配置迁移；数据库最终核对为 11 个集合、282 条记录，云存储核对为 461 个文件。
- 公司 A 账号已有足球小程序直绑，因此篮球小程序通过官方“环境共享”方式访问独立篮球环境；足球与篮球的数据库、存储、函数和计费仍保持隔离。
- 原生小程序统一改为共享云实例调用，并兼容共享身份字段 `FROM_OPENID / FROM_UNIONID / FROM_APPID`。
- 修复共享环境下直接使用 `cloud://` 无法显示的问题：页面资源统一通过共享实例批量换取 HTTPS 临时地址，保留原始 fileID，不把临时地址回写为永久业务数据。
- 历史数据库扫描 282 条记录，将 119 条记录中的 121 个旧云存储地址切换到新环境；复查旧地址剩余 0 条。涉及历史赛果报告和语音日志，迁移前备份位于仓库外 `E:\Documents\SxfCloudBackups\`。
- `sxGetAudioUrl` 支持一次解析最多 50 个文件，作为共享客户端无法直接换取临时地址时的服务端后备；头像、队徽、MC 音乐和静态图标共用同一套解析机制。
- `sxf-cloud basketball` 默认目标已切换到新篮球环境，并复用加密的 `basketball-target` 凭据；`basketball-target` 作为兼容别名保留。
- 验证：131 个 JSON、139 个 JavaScript 文件检查通过；运行时代码旧环境引用为 0；WXML/WXSS 直接 `cloud://` 引用为 0；共享资源适配器单元烟测通过；`npm run build:weapp` 和微信开发者工具 CLI 预览成功，总包约 2.7 MB、主包约 374.2 KB。
- 发布边界：本次仅生成手机预览版，尚未替用户提交微信审核或发布新的线上正式版。

## 2026-07-25 v2.0.2 云环境迁移与资源恢复

- 版本：项目与依赖锁定文件统一更新为 `2.0.2`。
- 发布阶段：迁移修复已完成并通过微信预览验证；尚未上传新的微信代码版本，尚未提交审核或发布正式版。
- 用户可感知更新：
  1、完成官网与小程序服务环境切换，恢复主要页面和业务数据访问。
  2、恢复用户头像、球队队徽、赛事 Logo 和页面图标显示。
  3、恢复 MC 音乐、比赛音效和相关音频播放链接。
  4、优化赛事 Logo 保存方式，避免临时链接过期后图片再次失效。
  5、优化小程序资源加载与失败回退，提升共享环境下的访问稳定性。
  6、保持篮球与足球项目的数据、文件和业务服务相互独立。
  7、完成官网自定义域名与小程序预览验证，当前主要访问链路正常。
- 验证：云端数据、存储文件和正式函数已核对；相关 JavaScript、JSON、WXML、资源链接检查通过；`npm run build:weapp` 及微信开发者工具预览构建通过。
- 仓库同步：发布提交 `3e134c9` 已同步至 GitHub `origin` 与 Gitee `gitee` 的 `codex/education-2.0` 分支，两个远端分支均已回读核对一致。
- 飞书登记：版本 Base 已创建并回读验证 `2.0.2` 记录 `recvqnFNBFfZKD`；知识库入口 `Cy2ew2TDWidvIpkG4MEc8R0znZc` 与该 Base 为同一份版本表。
- 飞书公告：已在用户持有的页面 `SqSBwzTxpiv66pkGyfycnLk8nqc` 写入并回读验证 `赛小蜂篮球 2.0.2 版本更新公告`；标题、版本、日期、阶段、七项更新、发布说明和反馈指引完整。

## 2026-08-01 赛事管理组别与赛制工作台

- 使用 `yuanxing` 按确认稿 `赛事管理-组别与赛制-v2.png` 完成原型拆解，生成并校验标准 `_assets` 素材包；正式页面未使用原型切片作为背景或界面素材。
- 正式路由 `admin/tournament-center.html#event/groups` 调整为“先列竞赛组别，再在组别内设置赛制”。新增空白流程和四个常见预设：小组赛 + 淘汰赛、单场淘汰、小组赛 + 单循环、全部球队循环赛。
- 小组赛 + 淘汰赛预设包含小组循环、淘汰赛、半决赛、决赛；支持手动添加和删除阶段、复制设置、参数冲突校验、实时场次数预览、保存及进入报名设置。
- 校正原型中的演示场次数：4 组 × 6 队的小组单循环按真实组合数计算为 60 场，不沿用错误的 24 场展示。
- 验证：`node --check admin/tournament-center.js`、`node --check tools/test-tournament-groups.js` 通过；组别与赛制专项交互测试全部通过，并在 `1672×941`、`1440×900` 下完成截图对照，无页面横向溢出。
- 本次未暂存、未提交、未推送仓库；保留工作树中其他既有改动。

### 组别与赛制页面拆分

- 根据使用反馈将原上下混排页面拆为两层：`#event/groups` 只展示和管理组别，`#event/format` 只编辑当前组别赛制。
- 组别列表每行提供“编辑组别”和“赛制设置”；赛制页提供“返回组别列表”和“保存并进入下一步”，避免同页信息过多。
- 专项测试新增页面分离、赛制入口、返回路径与下一步跳转验证；`1672×941` 组别列表和赛制编辑页均完成截图检查。
- 修复赛制预设卡片和阶段参数在较窄内容区的文字溢出：预设标题允许受控换行，窄屏隐藏辅助图标与说明，晋级规则改用短标签；新增约 `1100×760` 的组件溢出检查。

### 单场淘汰赛上下半区规则修正

- 单场淘汰预设改为按球队数量自动生成完整轮次；8 支球队固定生成“8进4 → 4进2 → 决赛”，场次数为 `4 + 2 + 1`，不再错误显示为“7 场淘汰赛 + 1 场决赛”。
- 前两轮按上半区、下半区展示签位与晋级链路，决赛明确为“上半区冠军 vs 下半区冠军”；非 2 的幂次球队自动计算标准签位和轮空数量。
- 淘汰赛阶段移除无关的小组数、每组球队数和积分规则，改为本轮球队、本轮场次、晋级球队及单场淘汰说明。
- 验证：语法检查及组别与赛制专项交互测试全部通过，覆盖 8 队三轮淘汰、上下半区、场次数、淘汰赛专用参数和既有页面回归。

### 竞赛组别删除保护

- 组别列表新增“删除”按钮；空组别点击后必须二次确认，确认后同步移除组别资料及其赛制配置。
- 已有入驻球队、已完成抽签或已生成赛程的组别禁止直接删除，并提示先解除关联数据，避免球队、签位和赛程断链。
- 专项测试覆盖空组别确认删除、删除后列表与本地状态同步、已有 24 支球队的组别阻断删除，全部通过。

### 同名组别删除与窄屏页首修正

### 组别独立身份与自定义命名修复

### 报名球队编辑、删除与可读性优化

- 入驻进度列表为每支球队新增“编辑、邀请、删除”操作；编辑可修改球队名称、参赛组别、领队和手机号，并保留原邀请状态与记录。
- 删除球队增加二次确认；已关联抽签或赛程时禁止直接删除，避免签位、赛程和球队数据断链。
- 报名球队表格正文提升至 13px、球队名称提升至 14px；在 1450px 及以下隐藏次要的球员资料和更新时间列，为球队信息与操作按钮让出空间。
- `tools/test-tournament-registration.js` 新增编辑、删除、字号和状态保留验证；1672×941、1440×900 共 41 项测试全部通过，无横向溢出。

- 竞赛组别改为稳定独立 ID 识别；旧数据加载时自动补齐缺失 ID，并修复重复 ID，编辑、赛制查看、赛制复制与删除不再按名称误匹配。
- 组别编辑器拆分“标准组别”和“组别名称”：标准组别负责年龄资格默认值，组别名称允许人工改为 `U8甲组`、`U8乙组` 等显示名称。
- 新建或编辑时阻止完全重名，提示使用甲组、乙组等名称区分；保存时只更新当前 ID 对应的单个组别。
- 专项测试新增两个同类型组别的独立编辑场景，确认修改第二个组别不会联动第一个；组别、赛制、删除、窄屏布局共 52 项验证通过。

- 修复同名组别按名称查找导致删除错行的问题；删除操作改为读取当前列表行索引，两个同名组别也能分别识别各自的球队数量和赛制数据。
- 新增重复名称回归场景：第一行 24 支球队、第二行 0 支球队时，第二行可确认删除，第一行仍保留且继续受球队关联保护。
- 赛事页首增加 `1200px` 与 `1080px` 两档受控收缩：缩短赛事选择框、压缩间距、账户区保留头像、机构经营入口在更窄尺寸改为图标，避免返回按钮、赛事名称、状态和账户区重叠。
- 二级导航在窄屏缩小间距，保存入口压缩为图标按钮；专项测试新增页首左右区域与二级导航操作区的边界碰撞检查，均通过。

### 组别设定规模与实际报名数量解耦

- 组别设置页不再读取或展示实际入驻球队数量，仅展示该组别设定的参赛规模，例如“8 支”；真实已报名、待认领和资格状态统一在“报名与资格”中查看。
- 旧数据中的 `joined` 缓存字段在加载时自动清理，避免 U6 等空组别被历史演示数量错误显示为已有 4 支球队。
- 删除组别时仍实时检查当前赛事真实报名记录；只有确实存在关联球队、抽签或赛程时才阻止删除，兼顾页面口径清晰和数据安全。
- 组别改名时同步迁移真实报名球队的组别归属，避免改名后报名筛选失联。
- 验证：组别与赛制专项测试 60 项全部通过；报名与资格回归测试 41 项全部通过；语法检查通过。

### 小组循环加晋级组单循环赛制

- 组别竞赛赛制新增“**小组循环 + 晋级组单循环**”：第一阶段各小组内部单循环，按每组设定名次晋级；第二阶段由全部晋级球队再次单循环并按积分排定最终名次。
- 修正晋级组循环赛场次数计算，不再错误使用组别全部球队数量；阶段预览明确展示晋级球队数、循环场次和最终排名方式。
- 9 支球队示例已验证：3 组 × 3 队、每组第 1 名晋级，第一阶段 9 场，第二阶段 3 场，共 12 场。
- 晋级组单循环不再触发淘汰赛签位或轮空提示；少于 2 支晋级球队时阻止保存。
- 验证：赛事组别与赛制专项交互测试全部通过，页面语法检查通过。

### 报名完成后进入真实抽签链路

- 入驻进度页按组别核对“已添加球队 / 设定参赛规模”；达到目标后显示“进入抽签与分组”下一步入口，球队尚未认领不阻断测试抽签。
- 点击下一步自动带入当前竞赛组别、真实报名球队、分组数、每组球队数和晋级规则，不再读取原有 24 支静态演示球队。
- 抽签页支持多组别切换、人工拖拽、自动抽签、清空重排和保存；分组结果按赛事与组别分别保存。
- 晋级摘要按当前赛制展示：小组循环、晋级组单循环或淘汰赛均读取组别配置，不再固定显示八强对阵。
- 验证：赛事全生命周期 33 项断言通过；报名与资格回归 43 项通过；控制台无脚本错误。

### 抽签入口与实际球队状态修正

- 抽签页面不再读取旧的“资格审核已完成”布尔状态作为唯一门槛；只要当前赛事存在人工添加或小程序入驻的真实球队，即可进入抽签，并按球队所属组别带入名单。
- 抽签顶部二级入口改为跟随当前组别赛制动态显示：小组抽签、晋级组、循环赛排序或淘汰签位不再同时固定展示，切换组别时同步切换对应入口与页面。
- 纯循环赛和淘汰赛页面改为读取当前组别真实球队数量与名称，并据此计算轮次、场次、每队场次及淘汰签位，不再回退静态演示球队。
- 验证：报名与资格回归新增“未完成资格审核仍带入实际球队”和“顶部仅显示对应赛制”两项，合计 45 项全部通过；赛事全生命周期 33 项通过；JavaScript 语法检查通过。
- 修正抽签页上下文栏的通用文字选择器，避免其覆盖主按钮颜色；“保存分组”按钮文字和图标恢复为白色。

## 2026-08-20 小程序赛事板块：班班赛与机构周赛赛制

- 工作分支：`codex/education-2.0`；本次只修改微信小程序赛事板块及其独立云函数，不修改 PC 赛事中心。
- 赛事首页新增“创建班班赛”“创建机构周赛”两个主入口，并按“我创建的 / 我参加的”区分赛事；杯赛、友谊赛等保留在“其他赛事”。
- 班班赛以学校、年级、班级形成班级参赛队；机构周赛复用本机球队库。创建者可生成通用邀请码与小程序码，受邀球队独立提交资料后由创建者审核。
- 新增单循环、主客双循环、小组循环加淘汰赛三种赛制；赛程只生成轮次、主客队和对阵，不设置日期、时间或场馆。支持奇数队轮空、对阵预览、发布前确认及未开赛场次交换主客。
- 新增积分规则配置，默认胜 2 分、负 1 分、弃权 0 分；排名依次比较积分、相互战绩、净胜分和总得分，完全相同时并列。小组赛结束后按晋级名额生成下一轮淘汰赛。
- 新增独立云函数 `sxTournamentLeague`，负责赛事、报名审核、对阵、积分和旧赛事幂等迁移；赛事创建者与参赛球队的写权限在云端校验。二维码场景只携带短邀请码，不暴露用户或球队内部标识。
- 旧版本机赛事支持登录后确认迁移为“其他赛事”，迁移保留可识别的球队、球员快照、场次和已完成比分，原本机数据在云端确认前不删除。
- 比赛卡继续进入现有横屏计分板；本次明确未开发裁判指派、计分板改造、双方球队数据记录或球队统计提交。
- 验证：赛事领域自动测试通过，覆盖 4/5/8 队单循环、奇数轮空、双循环、小组赛、积分排序和并列名次；新增及修改 JavaScript 语法检查、JSON 解析、WXML 复杂表达式扫描、页面事件绑定审计和 `git diff --check` 均通过。
- 已知既有阻断：`npm run typecheck` 仍因 `config/index.ts` 使用 `import.meta` 但 TypeScript `module` 配置不兼容而失败，与本次原生小程序赛事改动无关。
- 发布边界：未部署云函数、未上传微信体验版、未提交或推送 GitHub/Gitee；需在微信开发者工具中连接已部署的 `sxTournamentLeague` 与更新后的 `sxCreateTournamentQrCode` 后进行多账号扫码联调。

### 班班赛学段、年级、班级与球队绑定

- 将原来的年级、班级自由输入改为结构化三级选择：学段固定为小学、初中、高中；年级根据学段联动为小学一至六年级、初一至初三、高一至高三；班级提供 1—10 班与自定义班级。
- 赛事信息保存学校、学段和比赛年级；创建者自己的班级移入“本队信息”，避免把赛事范围与参赛队身份混在同一组字段中。
- 新增稳定班级身份键，由学校、学段、年级编码和班级编码组成。创建或报名时优先匹配已绑定球队；没有匹配球队时自动创建班级队，避免同一班级重复建队。
- 可将已有小程序球队绑定为班级队并复用其球员；赛事云端保存班级身份字段和报名时球队快照，扫码报名页自动带入赛事学校、学段及年级，只要求受邀方选择自己的班级。
- 验证：新增班级绑定专项测试，覆盖三类学段、各学段年级数量、1—10 班与自定义、首次自动建队、相同班级复用及重复建队拦截；赛事领域测试、JavaScript 语法、WXML 表达式与页面事件绑定检查均通过。
- 修正不同赛事类型共用“例如：三年级班班赛”占位文案的问题。机构周赛改为机构周赛示例、机构名称和周赛说明；其他赛事使用杯赛示例与主办方文案；班班赛继续保留学校班赛文案。

### 创建成功详情页与赛事列表即时显示

- 创建赛事成功后进入赛事详情首屏，明确显示“赛事创建成功”，并按“本队已加入 → 邀请球队报名 → 审核后生成对阵”引导创建者完成下一步。
- 新增本机赛事即时缓存：云函数暂不可用时仍可完成创建、进入详情并在赛事首页“我创建的”中显示，卡片标记“待云端同步”。
- 赛事首页每次显示时自动尝试补同步本机赛事；同步成功后移除本机待同步记录并读取云端赛事，失败时继续保留本机记录，不阻塞页面使用。
- 本机待同步赛事的详情页只展示创建结果、球队和同步说明，不开放二维码邀请与对阵发布，避免产生无法跨账号访问的伪邀请；返回赛事首页同步后再继续云端操作。
- 验证：新增本机创建、列表回读和详情回读测试；赛事排赛、班级绑定测试、JavaScript 语法、WXML 复杂表达式和页面事件绑定检查均通过。

### 待同步赛事邀请入口修复

- 修复本机待同步赛事创建成功页只有“返回赛事首页”、没有邀请入口的问题；创建成功首屏与管理页现在都明确提供“邀请球队报名”。
- 点击邀请时自动执行“同步当前赛事 → 使用云端赛事 ID 重新打开详情 → 生成通用邀请码和小程序码”，用户不需要先手动返回赛事首页。
- 云端赛事服务不可用时保留本机赛事，并明确提示跨账号邀请依赖云端服务，不再隐藏按钮或给出模糊失败反馈。
- 邀请创建成功但小程序码暂时生成失败时仍保留邀请码和转发页面路径，避免二维码单点失败阻断全部邀请方式。

### 球队邀请卡片、海报与邀请码

- 邀请面板升级为三种并列方式：微信好友/群小程序卡片为主入口，动态邀请海报用于朋友圈和线下传播，复制邀请码作为识别失败与现场传递的兜底。
- 新增小程序画布动态生成邀请海报，内容包括赛事类型、赛事名称、主办方、赛制、小程序码、邀请码和扫码报名说明；支持预览及保存到系统相册。
- 保存海报时处理相册权限拒绝，提供跳转系统设置的恢复入口；小程序码尚未生成时阻止生成空海报并给出重试提示。
- 赛事首页新增“已有赛事邀请码”，输入并验证邀请码后直接进入对应球队报名页；邀请码、海报扫码和微信分享卡片共用同一个报名身份与审核流程。
- 验证：赛事与班级绑定测试、JavaScript 语法、WXML 复杂表达式、页面事件绑定及改动差异检查通过；动态海报仍需在微信开发者工具或真机上验证小程序码下载、画布导出及相册授权。

### 小程序赛事板块飞书测试执行表

- 创建普通飞书 Docx 文档《赛小蜂篮球小程序赛事板块测试执行表》，未放入知识库、未覆盖现有飞书页面。
- 测试表按预览版创建页面、球队绑定与创建结果、体验版云端权限、三种邀请方式、报名审核、赛制对阵、积分榜及异常回归分为八个阶段。
- 文档写入 128 个内容块，其中 88 项为飞书原生可勾选 Todo；每项使用稳定测试编号，文末提供失败问题记录模板与阶段结论。
- 已回读验证文档标题、内容块数量和 Todo 数量；飞书文档 ID 为 `Hu04djuHeog1NexoR0pcEnlwnbh`。

### 赛事资料与创建者班级编辑

- 赛事详情页为创建者新增“编辑赛事”入口；编辑复用创建表单，可修改赛事名称、主办方/学校、赛事说明、赛制、积分规则、分组参数和创建者绑定的班级或球队。
- 班班赛创建/报名校验改为逐字段提示：学校为空提示“请填写学校名称”，学段为空提示“请选择学段”，年级为空提示“请选择比赛年级”，班级为空按普通班级或自定义班级分别提示。
- 云端更新接口同步更新赛事资料与创建者参赛队快照，并校验创建者身份和班级/球队重复报名。
- 对阵已发布后，为保护既有赛程、积分与历史赛果，锁定赛制、积分、分组和创建者绑定班级/球队；仍可编辑赛事名称、主办方和赛事说明。
- 本机待同步赛事同样支持编辑，并更新其后续云端补同步载荷。
- 验证：赛事领域与班级绑定测试、JavaScript 语法、页面事件绑定、WXML 复杂表达式和 `git diff --check` 均通过；未部署云函数、未上传微信版本或推送仓库。

### 小程序球队队徽上传、裁剪与透明背景处理

- 根据本地 PC 赛事中心的成熟实现，重做小程序球队编辑页的队徽更换流程：选择图片后不再直接上传，改为“上传 → 1:1 裁剪 → 外围连通背景抠除 → 确认透明队徽 → 云存储上传”。
- 裁剪面板支持原图尺寸识别、30%—300% 缩放、水平/垂直位置调整、透明棋盘格预览、去底开关和12—90容差调整；最终固定导出 512×512 透明 PNG。
- 抠图算法只从图片四周向内遍历与背景相连的像素，保留被队徽边框包围的内部白色、浅色文字和图形细节，与 PC 端规则一致。
- 保留现有云存储上传与球队资料同步路径；确认处理成功后才更新 `logoUrl/logoFileID`，失败或取消不会覆盖现有队徽。
- 验证：新增队徽裁剪/抠图专项测试，覆盖宽图和竖图裁剪定位、外围背景透明、队徽内部白色和主体边缘保留；赛事领域和班级绑定回归、JavaScript 语法、页面事件绑定、WXML 复杂表达式与差异检查均通过。
- 发布边界：未部署云函数、未上传微信体验版或推送仓库；需要在微信开发者工具/真机验证临时图片读取、画布像素处理、透明 PNG 导出和云存储上传。

### 队徽自动居中修正

- 修复原图主体偏右、偏下时默认按原图中心方形裁剪，导致透明队徽画面不居中的问题。
- 新流程改为先完整等比绘制原图、抠除外围连通背景，再读取透明主体边界并将主体按 82% 安全区自动缩放居中；缩放和水平/垂直滑杆继续用于人工微调。
- 当关闭去底时，仍按完整原图等比居中预览，避免无背景处理时出现默认裁切缺角。
- 专项测试新增偏右下主体的透明像素边界用例，验证处理后主体移动到画布中心并适度放大；裁剪、抠图、页面事件和差异检查均通过。

### 队徽画布手势编辑

- 移除队徽裁剪弹层中的缩放、水平位置和垂直位置滑杆，改为直接在预览画布上单指拖动队徽位置、双指捏合缩放。
- 新增“恢复居中”按钮，手势调整后可一键回到自动居中默认构图；保留去底开关和去底容差滑杆，因为容差属于图像处理参数而非构图位置。
- 手势移动采用节流预览，手势结束后强制完成一次透明背景及主体居中重绘；确认透明队徽继续读取同一组缩放和位置数据导出。
- 验证：队徽裁剪/抠图、页面触摸事件绑定、WXML 复杂表达式和差异检查通过；需要在真机进行单指拖动、双指缩放和画布手势冲突的最终体验验收。

### 队徽裁剪先于去底

- 按使用反馈将队徽处理顺序固定为“先裁剪构图，后抠除背景”：固定 1:1 裁剪框使用 cover 规则铺满原图，竖图默认填满宽度并允许上下拖动，横图默认填满高度并允许左右拖动。
- 移除去底后的自动主体缩放/居中，避免系统改变用户通过手势确定的最终构图；外围背景处理只作用于方框内已确认的画面。
- 预览说明改为“先裁剪构图，再抠除背景”，保持单指拖动、双指缩放和恢复居中能力。
- 专项测试新增竖图固定方框与位置偏移断言；队徽处理、页面手势绑定和差异检查通过。

### 队徽四向拖动修正

- 手势坐标兼容微信小程序事件的 `pageX/pageY`，不再只依赖模拟器可能缺失的 `clientX/clientY`，修复单指拖动在部分设备上方向或位移失效的问题。
- 固定裁剪框默认增加 8% 安全出血：在不双指放大的情况下，图片也具备左右上下四个方向的可拖动空间；放大后继续按同一边界限制构图，避免露出透明空白。
- 画布触摸事件直接绑定到 canvas，避免父容器滚动或遮罩抢走手势。
- 验证：新增默认出血下的竖图定位、微信触点坐标回退和画布手势绑定检查；队徽专项测试与差异检查通过。

### 队徽微信原生裁剪优先

- 根据实际操作反馈，队徽主流程改用微信 `chooseImage` 的固定 512×512 原生裁剪能力：用户先在系统裁剪界面使用稳定手势完成构图，再返回小程序执行外围背景去除和透明 PNG 确认。
- 正常环境不再依赖自绘画布实现图片居中；处理面板显示“已完成微信原生 1:1 裁剪”，仅保留去底预览、容差调整、重新裁剪和确认上传。
- 如果低版本环境未返回方形裁剪结果，才降级到现有画布手势裁剪，以保证功能可用但不影响主路径体验。
- 验证：原生裁剪参数、页面分支、队徽算法、事件绑定、WXML 表达式和差异检查通过；待微信开发者工具/真机实际验证原生裁剪界面是否按当前基础库生效。

### 大图先压缩、分步裁剪与抠图

- 根据真机反馈取消微信原生预裁剪主路径，避免大尺寸原图在进入小程序前已被错误截取；重新使用完整原图，并在编辑前按最长边 1200px 等比压缩，保留全部画面和原始宽高比。
- 队徽编辑器拆为两个明确阶段：第一阶段只显示完整压缩图，用户在固定1:1框内单指拖动、双指缩放并完成构图；第二阶段冻结裁剪结果，再执行外围连通背景抠除与容差调整。
- 抠图阶段可返回裁剪重新构图；只有确认透明队徽后才导出512×512 PNG并上传云存储。取消、重新选图或任一步失败均不会覆盖现有队徽。
- 新增隐藏预处理画布，专门将超大图片等比压缩到可稳定处理尺寸，减少大图直接进入512画布导致的截断、错位和内存压力。
- 验证：完整流程文案、步骤切换、超大图压缩分支、触摸事件、抠图算法、WXML表达式和差异检查通过；待开发者工具/真机用原始大图验证压缩画质、手势构图和透明导出。

### 队徽预览画布坐标修正

- 根据开发者工具截图定位到根因：512rpx 预览框在375px宽设备上的实际坐标约为256px，但旧逻辑按512px绘制，导致只显示完整图片的局部象限，看起来像图片提前被裁切。
- 预览画布的内部宽高、裁剪计算、像素回读、去底写回和导出区域统一改为设备实际显示尺寸；最终通过 `destWidth/destHeight` 放大输出512×512 PNG。
- WXML画布宽高与响应式 `logoCanvasSize` 绑定，CSS使用100%填满固定1:1容器，避免逻辑画布与视觉框再次产生二倍坐标差。
- 专项测试新增400×400完整原图映射到256×256实际预览画布的断言；队徽算法、响应式画布坐标与差异检查通过。

### 渐变背景队徽抠图修正

- 修复紫红、橙色等多色渐变背景无法去除的问题。旧算法使用四角平均色作为唯一背景色，四角颜色差异较大时平均色不匹配任何真实区域，导致第二步没有抠图效果。
- 新算法改为从图片四周多点出发，按照相邻像素的局部色差自适应向内遍历；可跨越缓慢变化的渐变背景，并在队徽白边、文字和主体的明显色彩边界处停止。
- 仍然只删除与外边缘连通的背景，白边包围的内部白色、红色、蓝色等组成部分不会被误删。
- 专项测试新增多色渐变背景、白色封闭边框和内部主体保留场景；像素算法、第二步预览路径、语法与差异检查通过。

### 球员头像百度云人像分割

- 审计确认原球员头像链路只有本地320×320裁剪和云存储上传，未接入混元或任何人物分割；已将球员裁剪确认后的上传改为调用 `sxUploadAvatar` 云函数。
- 根据成本与现有免费资源选择百度智能云人体分析 `body_seg` 人像分割接口：云函数先执行微信图片内容安全校验，再用百度 OAuth 获取访问令牌，提交裁剪图并读取透明背景 `foreground`，最终以 PNG 上传到 `player-avatars/`。
- 腾讯云 BDA 试接代码与 SDK依赖已撤回，仓库只保留百度方案；百度 API Key/Secret Key仅从云函数环境变量 `BAIDU_AIP_API_KEY`、`BAIDU_AIP_SECRET_KEY` 读取，不写入仓库或日志，AppID不参与接口认证。
- 人像分割未配置、授权失败、未返回透明图或内容安全失败时均明确报错，不再回退保存带背景原图。
- 球员裁剪页按钮改为“裁剪并智能抠图”，处理时显示“智能抠图中”，成功后保存透明 PNG fileID。
- 只读检查：篮球云环境 `sxUploadAvatar` 已部署，但远端尚无百度人像分割环境变量；需要配置后重新部署当前函数代码。
- 验证：新增百度 OAuth、`body_seg` 请求、透明 `foreground` 解码及缺少配置的专项测试；云函数、球员页面语法、JSON、调用契约与差异检查通过。未部署或修改远端环境变量。

### sxUploadAvatar 百度人像分割部署

- 用户在 CloudBase 控制台完成 `BAIDU_AIP_API_KEY` 与 `BAIDU_AIP_SECRET_KEY` 环境变量配置；只读回查确认两个变量名均已存在，未读取或记录变量值。
- 经用户明确授权，使用统一篮球 CloudBase 启动器覆盖部署 `sxUploadAvatar`。CloudBase 回读状态为 `Deployment completed`，运行环境为 Node.js 16.13；实现未使用 Node 18 专属 API。
- 使用空请求进行安全调用验证，函数成功返回“图片数据为空”保护结果，未调用百度人像分割接口，不消耗免费资源额度。
- 下一步真机验收：上传正面清晰球员照片，检查“智能抠图中”、透明头像预览、云存储 fileID 保存以及百度授权/人像识别失败提示。

### 队徽同步失败服务端上传修复

- 根据真机“队徽同步失败”反馈，确认失败点位于保存球队时的小程序端直传云存储；新增 `sxUploadTeamLogo` 云函数作为唯一队徽上传路径。
- 小程序将确认处理后的队徽 PNG 以 base64 发送至云函数，云函数执行微信图片内容安全校验后写入 `team-logos/` 并返回稳定 fileID；创建与编辑球队、保存草稿与保存球队均复用该路径。
- 前端错误提示改为展示云函数返回的具体失败原因，方便区分文件过大、内容安全、服务端上传和网络失败。
- 经用户当前任务授权，使用篮球 CloudBase 启动器部署 `sxUploadTeamLogo`，回读状态为 `Deployment completed`；空图片安全调用成功命中“队徽图片为空”保护，未写入云存储。
- 验证：队徽上传服务端调用契约、队徽编辑算法、云函数语法、JSON配置、差异检查和部署回读均通过。需要小程序重新编译后进行真实队徽保存验收。

### 球队资料同步函数缺失修复

- 真机仍提示“队徽同步失败”后进一步审计发现，队徽 `sxUploadTeamLogo` 已部署成功，实际失败发生在保存球队后的 `pushRoster()`：其依赖的 `sxSyncRoster` 云函数远端不存在，页面 catch 又将整个同步异常误报为队徽失败。
- 将本地球队保存与云端阵容同步拆分：球队、草稿和分类写入本机后即视为保存成功；云端同步失败时安排3秒后台重试，并提示“已保存，云端稍后同步”，不再阻塞关闭表单或重复创建球队。
- 将 `sxSyncRoster` 纳入 CloudBase 配置并部署。首次桌面命令等待超时后保持状态跟踪重新部署，CloudBase 明确返回 `Cloud function deployed successfully`，回读状态为 `Deployment completed`。
- 使用空请求验证函数可调用并正确命中 `NO_OPENID` 微信身份保护，说明新函数代码已在线；未写入测试球队数据。
- 验证：队徽上传契约、球队保存降级、后台重试、云函数语法、JSON配置、差异检查、部署和安全调用均通过。等待重新编译后真机保存球队复测。

### 开发者工具队徽画布与保存兼容修正

- 根据开发者工具截图复核，问题不在队徽文件大小；开发者工具在第 1 步切换到第 2 步时，对两个同名 `canvas-id` 的重挂载时序更敏感，可能导致抠图或透明 PNG 导出失败。
- 队徽编辑器改为全流程复用一个 `teamLogoEditorCanvas`，第 2 步仅切换文案和操作区；非裁剪阶段忽略画布触摸，避免重复画布节点造成状态丢失。
- 根据开发者工具实际控制台错误，将抠图读写改为 `type="2d"` Canvas 节点的 `getImageData/putImageData`，旧版基础库仍保留原生 API 回退；不再把开发者工具不支持的 `wx.canvasPutImageData` 作为主路径。
- 兼容开发者工具 Canvas 2D 返回的只读 `ImageData.width/height`，改为使用带尺寸副本参与像素算法，不再给原始 `ImageData` 尺寸属性赋值。
- 保存草稿和保存球队时显式使用刚上传返回的云端 fileID，避免 `setData` 尚未完成时把开发者工具临时路径写入球队资料。
- 验证：球队页面、云函数和工具脚本 JavaScript 语法检查通过；队徽上传契约与透明背景专项测试通过；待开发者工具重新编译后复测完整流程。

### 球队管理顶部胶囊去重

- 删除球队管理页自行绘制的 `.wechat-capsule`、分隔线和圆形按钮，只保留微信原生右上角胶囊，修复开发者工具和真机上出现两套胶囊叠加的问题。
- 验证：页面 WXML/WXSS 差异检查和球队页面 JavaScript 语法检查通过。

### 队徽保存临时地址误上传修正

- 根据开发者工具控制台 `permission denied` 回读，确认共享云资源适配器会把展示用 `logoUrl` 转成临时 HTTPS 地址；保存时旧逻辑误把该地址当作本地文件路径再次上传。
- 球队表单新增独立 `logoFileID`，展示继续使用临时 URL，创建、编辑、草稿和阵容同步统一优先使用 `cloud://` fileID，避免重复上传和权限失败。
- 验证：队徽上传契约、透明背景专项测试和球队页面 JavaScript 语法检查通过；待开发者工具重新编译后复测保存球队。

### 球队入口绑定球员与分队摘要筛选

- 从球队管理页点击“队员”进入球员库后，再点击“添加球员”会把当前球队 key/name 传入球员表单并自动预选所属球队，保存后写入正确的 `team` 和 `filter`，不再默认落到“未分队”。
- 球员库的“球员总数、已分队、未分队”摘要改为可点击筛选；未分队球员的队徽和所属球队行可直接进入球员编辑页重新分队。
- 新增球队入口绑定和摘要筛选专项测试；球队、球员页面 JavaScript 语法检查通过。

### 赛事邀请二维码切换为篮球小程序

- 根据用户截图确认邀请二维码中心图案来自赛小蜂足球，原因是篮球小程序通过共享足球云环境调用 `cloud.openapi.wxacode`，微信按共享环境归属的小程序生成了足球码；不是邀请海报图片上传错误。
- `sxCreateTournamentQrCode` 已改为使用赛小蜂篮球 AppID，通过篮球小程序自己的 access_token 调用 `wxa/getwxacodeunlimit`，报名页和邀请码场景保持不变。
- 由于云函数环境变量按函数隔离，部署前需要在 `sxCreateTournamentQrCode` 单独配置已有的 `SXF_BASKETBALL_APPSECRET`；不能只配置在 `sxLogin`。未读取或写入密钥值。
- 验证：二维码 AppID 契约、云函数 JavaScript 语法和赛事详情页 JavaScript 语法检查通过；待配置变量后部署并重新生成二维码。

### 篮球邀请二维码云函数部署

- 用户已在 `sxCreateTournamentQrCode` 保存篮球小程序 AppSecret 环境变量；未读取或记录变量值。
- 补回该函数的 CloudBase 部署配置后，以覆盖方式部署成功；云端回读状态为 `Deployment completed`，修改时间为 2026-08-21 16:47。
- 后续同一赛事的邀请码不变，重新打开邀请面板会覆盖同一二维码存储路径并生成赛小蜂篮球小程序码。
- 为避免旧足球二维码的临时地址或 CDN 缓存继续显示，篮球码的云存储哈希加入 `basketball-v2` 与篮球 AppID，重新生成时会使用新的 fileID；已于 16:49 再次覆盖部署并回读为 `Deployment completed`。
- 赛事详情页按小程序运行阶段选择二维码目标：正式版生成正式版码，开发工具和体验版统一生成体验版码，避免跨账号扫码落到会过期的开发版。

### 新建内容置顶排序

- 新建球员写入本地球员列表最前，并在球员库按创建时间倒序展示；编辑已有球员不改变其创建顺序。
- 球队管理页按球队创建时间倒序展示；新建球队同步写入球队标签最前，并记录标签时间用于后续稳定排序。
- 赛事首页原本已按最近更新时间倒序，无需调整。
- 验证：球队/球员页面 JavaScript 语法和球队入口绑定专项测试通过。

### 赛事服务号通知配置基础

- 新增并部署 `sxTournamentNotification`，用于承载赛事报名提交、审核结果和赛程公告三类服务号通知配置。
- 三个用户提供的服务号订阅通知模板 ID 已作为非敏感通知配置写入函数；服务号 AppID 与 AppSecret 保持为函数环境变量，不写入代码或开发日志。
- 部署回读：`sxTournamentNotification` 已成功部署；等待用户在该函数补充 `SXF_SERVICE_ACCOUNT_APPID` 与 `SXF_SERVICE_ACCOUNT_APPSECRET` 后继续完成关注绑定、任务中心和消息下发接入。

### 赛事报名服务号关注门槛

- 赛事报名页新增服务号关注门槛：报名者先获取本次邀请专属服务号二维码，长按关注后由后台轮询校验并自动解锁报名表单；未确认关注时无法提交报名。
- 新增 `sxTournamentNotification` 关注二维码、绑定校验和通知出箱基础能力；关注二维码使用服务号临时带参二维码，绑定记录保存小程序 OpenID 与服务号 OpenID 的对应关系。
- 新增 `sxServiceAccountCallback` 云函数，用于校验服务号回调签名、接收扫码关注/取消关注事件，并更新关注门槛与绑定记录。
- `sxTournamentNotification` 和 `sxServiceAccountCallback` 已部署，`www.sxfbasketball.cn/sx-service-account-callback` 已配置为公开 HTTPS 路由；还需要在服务号后台填写该 URL，并在回调函数配置相同的 `SXF_SERVICE_CALLBACK_TOKEN` 后完成微信回调验证。
- 验证：两个云函数、报名页 JavaScript、CloudBase 配置 JSON 和 WXML 表达式检查通过；HTTP 回调和实际扫码关注待服务号后台完成验证后联调。
- 服务号消息推送服务器配置已完成；使用 Token/时间戳/随机数按微信 SHA-1 验签规则请求 `https://www.sxfbasketball.cn/sx-service-account-callback`，成功回显校验字符串，证明 URL、HTTPS 路由和回调 Token 已连通。

### 邀请二维码非阻塞与海报等待优化

- 邀请球队时先立即打开邀请面板并显示邀请码、微信转发和复制入口；小程序码改为后台加载，不再让二维码生成延迟阻塞整个邀请流程。
- 二维码请求增加 25 秒超时与明确重试按钮；即使二维码暂未生成，创建者仍可先分享和复制邀请码。
- 生成邀请海报时，若二维码仍在加载会自动等待最多 10 秒再开始导出，避免用户因先后点击导致“海报生成失败”。
- 验证：赛事详情页 JavaScript 语法、邀请韧性专项测试、二维码 AppID 契约和差异检查通过。
# 2026-08-22 PC二维码登录修复

- 将 PC 登录小程序码的目标版本改为可通过 `SXF_PC_AUTH_ENV_VERSION` 配置，当前默认使用 `trial` 体验版，避免正式版尚未包含 `pages/pc-auth/index` 时扫码无法进入确认页。
- 小程序 PC 登录确认页增加场景参数安全解析和真实云端错误提示。
- 变更文件：`cloudfunctions/sxCollaboration/index.js`、`native-dist/pages/pc-auth/index.js`。
- 云函数：已部署 `sxCollaboration`，并恢复从 `sxLogin` 安全复用的微信 AppID/AppSecret 环境配置；本地未保留密钥文件或明文。
- 微信阶段：已通过微信开发者工具上传 `2.1.0` 代码版本，描述为“PC二维码登录修复：体验版扫码确认、参数校验与错误提示”，未提交审核、未发布正式版。
- 上传处理：微信开发者工具 Nightly 直接扫描大型仓库时触发 `EMFILE`，改用仅包含 `native-dist` 的临时精简项目后成功上传；代码包大小 3,040,574 bytes。
- 线上验证：PC 登录接口成功返回 `miniProgramCodeReady=true`、`miniProgramEnvVersion=trial`，并生成 PNG 小程序码。
# 2026-08-22 PC标准微信扫码登录正式上线

- 用户决定暂停PC与小程序的赛事报名、资格审核、通知和任务联调，避免与小程序内部赛事创建/页面联动测试同时修改数据产生冲突。
- 从本记录起不再部署PC报名页、协同云函数或新小程序报名版本，不再创建、删除、审核或重发真实联调数据，直至用户明确恢复。
- 本日真实测试结果已同步至本地知识库：`E:\Documents\LLM-Wiki-Knowledge-Base\01_Raw\02_赛小蜂篮球\小程序与PC\05_开发与版本\2026-08-22 PC与小程序赛事联动测试记录.md`。
- 新增跨运动项目复用规格 `docs/PC赛事报名生成、服务号绑定与通知联动规则.md`，并在本地知识库建立对应技术架构入口。文档覆盖PC报名码、邀请、小程序球队选择、服务号绑定、通知发件箱、幂等审计、篮球/足球适配层和串行验收清单。
- 用户指定跨项目唯一知识源为 `E:\Documents\LLM-Wiki-Knowledge-Base\01_Raw\00_Shared\赛事数据库\赛事规则\PC赛事报名生成、服务号绑定与通知联动规则.md`；已完整复制规格内容，篮球知识页已改为指向共享规范，仓库文档保留为实施镜像。
- 本次只更新文档与本地知识库，未恢复PC/小程序联调，未部署云函数，未上传小程序版本，未写入真实业务数据。
- 当前本地 `sxCollaboration` 存在未完成、未检查、未部署的小程序通知补偿辅助代码；后续不得将其误认为已上线功能。
- 修复PC“审核资格”只修改浏览器本地状态、缺少明确审核反馈和云端结果的问题。
- `sxCollaboration` 新增 `reviewRegistrationTeam` PC审核接口：校验PC会话、机构与赛事归属，将通过/驳回结果写入 `sx_tournament_teams`，完成主办方报名审核任务，生成领队状态任务并尝试发送服务号通知。
- 资格审核弹窗已补齐球队、领队、联系方式、参赛组别、球员资料数和当前状态；支持填写审核意见，驳回时必须说明需补充的资料。
- 待审核球队显示“审核资格”；已通过/已驳回球队显示“查看审核”，点击后显示审核意见和审核时间，不再重复执行审核。
- `sxCollaboration` 已以代码更新方式部署；`tournament-registration-review.css`、`tournament-center.js` 和 `tournament-center.html` 已发布正式静态托管。线上回读确认审核弹窗、状态按钮、云端动作和无效会话拦截已生效。
- PC“入驻进度”底部的组别抽签条件已改为桌面端单行横向布局：左侧显示总体就绪说明，右侧横向排列各组状态卡。
- 组别卡宽度压缩为 160px，标题、数量和缺口状态保持一行可读；组别较多时仅在状态卡区域横向滚动，不增加整页高度。
- 1180px 以下自动回退为上下布局，640px 以下进一步压缩卡片，保持窄屏可用。
- `tournament-registration-progress-clean.css` 与 `tournament-center.html` 已发布正式静态托管；线上回读确认单行网格、160px卡片、不换行、横向滚动和响应式规则已生效。
- PC“入驻进度”中的组别抽签条件区已从球队列表上方移至列表底部；页面顺序改为“报名指标 → 球队列表 → 抽签条件 → 边界说明”，优先展示主办方当前需处理的报名数据。
- 抽签条件的计算、组别就绪状态和“进入抽签”操作保持不变，仅调整信息层级与页面位置。
- `tournament-center.js` 与 `tournament-center.html` 已发布正式静态托管；线上回读确认球队表格位于抽签条件之前，抽签条件位于页面说明之前。
- 修复小程序报名成功后PC“入驻进度”无数据的问题：原因是小程序写入云端 `sx_tournament_teams`，PC页面仅读取浏览器本地 `registrationTeams`，两个数据源未衔接。
- `sxCollaboration` 新增 `listRegistrationTeams` PC授权接口，服务端校验PC会话、机构归属和赛事归属，不向PC返回球队领队的微信 OpenID。
- PC进入“入驻进度”后自动拉取当前赛事云端报名球队，并映射球队名称、队徽、领队、手机号、报名状态、资格状态、球员快照和更新时间。
- 云端 `pending / approved / rejected` 分别映射为PC“已认领 + 待审核 / 已通过 / 已驳回”；未分配竞赛组别的球队显示“待分组”。
- 同步采用 `teamId` 去重合并，保留PC人工异常补录记录；页面顶部新增“同步报名数据”按钮，并每15秒允许自动补偿同步。
- `sxCollaboration` 已以代码更新方式部署，`tournament-center.js` 和 `tournament-center.html` 已发布正式静态托管；线上回读确认自动同步、手动同步、云端映射和无效会话拦截已生效。
- PC“入驻进度”已收敛为正式报名运营模板：顶部只保留“生成球队报名码 / 入驻设置”，移除虚拟球队、测试直通和重复操作入口。
- 没有报名球队时，页面隐藏空表头、筛选工具和分页，改为“生成报名码 → 领队确认参赛 → 主办方审核”三步引导；人工添加降为异常补录入口。
- 抽签条件区改为紧凑组别就绪概览，与指标卡、球队列表和边界说明使用统一间距。
- `tournament-registration-progress-clean.css`、`tournament-center.js` 与 `tournament-center.html` 已发布到正式静态托管；线上回读确认顶部操作、空状态引导、空表隐藏和页面间距规则已生效。
- PC“新增/编辑竞赛组别”弹窗已改为宽屏工作台：桌面端宽度最高 1120px，基础字段使用三列网格，组别竞赛结构保持整行展示，充分利用两侧空间并减少纵向滚动。
- 弹窗顶部标题与底部操作区改为粘性固定，内容较多时仍可随时关闭、取消或保存；960px 以下自动转为两列，640px 以下转为单列。
- `tournament-group-editor.css`、`tournament-center.js` 和 `tournament-center.html` 已发布到正式静态托管；线上回读确认弹窗专用类、1120px 宽度、三列布局、固定头尾和响应式规则已生效。
- PC赛事空间卡片已完成大字模式下的专项整理：卡片调整为 320–350px 自适应宽度、560px 稳定高度，封面、标题、数据、日期、进度和操作区独立排布。
- 赛事名称最多显示两行并超出截断；组别、球队、比赛数据允许换行，日期和进度保留固定区域，消除标题与元信息重叠。
- “编辑 / 进入赛事空间”按钮保持原有 46px 高度、圆角和等高对齐，未被工作区大字规则拉伸变形。
- `tournament-event-cards.css` 与更新后的 `tournament-center.html` 已发布到 `www.sxfbasketball.cn/admin/`；线上回读确认卡片尺寸、两行标题、分区布局、按钮高度与响应式网格已生效。
- 小程序球队报名页已改为“先选球队，再确认参赛”：直接展示当前微信账号已有球队卡片、球员数和教练信息，选中后解锁“确认参赛”；“新建球队”收为次级入口。
- 新建球队后返回当前赛事报名页并重新读取球队列表，不再丢失 `eventId` / `inviteKey` 赛事上下文。
- 确认参赛后继续调用 `sxTournamentLeague.submitTeam`；云端生成主办方报名审核任务，并通过小程序和服务号通知主办方。
- 服务号关注与通知引导改为报名链路的默认步骤；未完成关注绑定时不提交参赛确认。
- 版本已升级为 `2.1.1`；微信开发者工具已成功上传代码版本 `2.1.1`，描述为“报名流程修复：选择已有球队、确认参赛并通知主办方”，代码包 3,053,349 bytes。
- `sxCollaboration` 已以代码更新方式部署，报名码当前指向 `trial` 体验版；待微信正式版审核发布后切换 `SXF_REGISTRATION_QR_ENV_VERSION=release`。
- 当前发布边界：`2.1.1` 代码已上传，尚需小程序管理员在微信后台选为体验版；尚未提交审核或发布正式版。
- 微信阶段回传：用户已确认测试/体验版上传成功；报名码继续指向 `trial`，可用已加入体验的微信账号验收。本次仍未提交微信审核、未发布正式版。
- 飞书版本 Base 已新增 `2.1.1` 记录，record ID `recvt0ephNeaFY`；回读确认版本号 `2.1.1`、标题“球队报名选择与参赛确认优化”、发布状态“测试中”、验证结果“待验证”。
- 飞书知识库已创建《赛小蜂篮球 2.1.1 版本更新公告》：`https://lxcmobp6gun.feishu.cn/wiki/CiyQwV5GLi9j6DkmxjicJskunIb`；节点类型 `docx`，父节点为版本 Base 知识库入口，写入并回读 18 个直接内容块。
- 飞书替换写入备份：`work/feishu-backups/CiyQwV5GLi9j6DkmxjicJskunIb-20260822T060415Z.json`。
- PC 赛事中心“球队报名二维码”已从演示占位图升级为正式云端链路：使用PC微信会话校验机构，同步赛事、创建唯一邀请记录，并调用 `sxCreateTournamentQrCode` 生成篮球小程序码（当前发布阶段为 `trial`）。
- 报名弹窗已展示真实 320px 报名码、赛事名称及三步引导：进入小程序报名、关注服务号接收通知、在小程序查看球队数据与执行任务。
- 新增复制报名说明、刷新报名码和下载报名码操作；移除弹窗中原有的“演示链路”主操作。
- 报名扫码后继续复用已上线的小程序球队创建/认领、服务号关注绑定、审核与赛程通知、球队数据和任务中心链路。
- 大字工作区样式已修正为“只放大文字”；撤销对按钮、开关、输入框、表格间距和操作列尺寸的强制覆盖，控件恢复原有形状。
- `sxCollaboration` 使用代码更新方式正式部署；`tournament-center.html`、`tournament-center.js`、`tournament-registration-qr.css` 已发布到正式静态托管。线上回读确认新动作、样式、下载和服务号引导全部存在；无效PC会话被服务端正确拒绝。
- 赛事中心右侧业务工作区已上线统一大字模式：正文与表格约 20px，页面主标题约 44px，辅助小字不低于 16px；按钮、开关、输入框和下拉菜单只放大文字，保留原有尺寸与形状。
- 大字范围覆盖动态业务页、二级标签、业务弹窗、表格、按钮与提示，不影响左侧导航和顶部品牌/账号区。
- 组别等密集表格同步增加行高、操作列宽度和按钮高度；复杂页保留横向滚动，不再为了同屏展示缩小文字。
- `tournament-center.html` 与 `tournament-workspace-large-type.css` 已发布到 `www.sxfbasketball.cn/admin/`，线上回读确认新样式、字号、控件高度和工作区宽度规则全部生效。
- 机构资料的省份、城市已由自由文本改为两级联动下拉菜单；未选省份时城市禁用，选择省份后自动加载对应城市。
- 复用 `admin/assets/data/china-regions.js` 正式行政区划数据，覆盖 34 个省级地区和 363 个城市；保存值仍为中文省市名称，兼容现有机构资料和编辑回显。
- 机构入口页、联动脚本和行政区划数据已发布到 `www.sxfbasketball.cn/admin/`；线上回读确认新 `select` 控件、脚本版本和数据统计全部正确。
- 修复微信开放平台官方登录组件被 `224×224` 容器和负边距裁切的问题；线上改为完整 `300×400` 显示区域，取消裁切并扩大登录卡左列。
- 统一账号方案正式确认：微信是主登录身份；首个开通机构者为老板/主账号；PC与小程序共用机构老板身份；主账号可绑定手机号；教练通过扫码、邀请链接或服务号通知加入机构。
- 账号与成员规则已同步至 `AGENTS.md` 及本地知识库 `E:\Documents\LLM-Wiki-Knowledge-Base\01_Raw\02_赛小蜂篮球\小程序与PC`。
- 微信开放平台网站应用已审核通过，网站应用 AppID 为 `wx8ccbf5e5f2e51d5c`，授权回调域为 `www.sxfbasketball.cn`。
- PC 主登录入口已从小程序码改为微信开放平台网站应用扫码登录，授权作用域为 `snsapi_login`。
- 新增 OAuth `state` 防伪校验、授权 `code` 交换、微信网站身份写入、`UnionID` 平台账号合并和 PC 会话兑换。
- 新增线上回调页 `https://www.sxfbasketball.cn/admin/wechat-login-callback.html`，授权完成后PC自动进入组织工作台。
- `sxCollaboration` 使用“仅更新代码”方式正式部署，保留用户在 CloudBase 控制台配置的网站应用 AppSecret，密钥未写入仓库或日志。
- PC 入口、微信登录样式、授权回调页及脚本已发布到 CloudBase 静态托管。
- 线上回读通过：`wechatLoginReady=true`、AppID 匹配、`scope=snsapi_login`、回调地址正确、未返回小程序码，PC入口与回调页均返回 HTTP 200。
- 发布边界：本次为 PC 网站与云函数正式上线；小程序无新代码变更，未重复上传小程序版本。

# 2026-08-23 2.1.2 主教练赛前分工正式部署

- 版本号后续已按用户要求回退并锁定为 2.1.0；2.1.1、2.1.2、2.1.3 不再作为有效发布版本，相关功能均并入 2.1.0 开发迭代。

# 2026-08-31 2.1.0 第4次补丁

- 家长自主建档头像升级为“上传、裁剪调整、压缩、人像分割、透明头像保存”完整流程。
- 修复手机原图 Base64 导致 CloudBase 请求体超限的问题。
- 建档页面静态文件与 sxEducationPublic、sxUploadAvatar 已部署并完成线上回读；详情见 开发日志_2026-08-31.md。

- 后续 2.1.3 已将赛前分工修正为严格三步：选择记录方式 → 添加助教 → 绑定记录任务；微信开发者工具已上传 2.1.3 测试版本。
- 2.1.3 飞书版本记录为 recvt7LO12MYvW，公告为 https://lxcmobp6gun.feishu.cn/wiki/PcbywOtyLiYYhokxxjGcbNTZnmd。

- 已将评审原型落地至正式小程序 match-coach-workspace 页面，任务中心进入本场协作后显示新版横屏三栏工作面。
- 临时助教邀请改为微信好友或群转发；完成分工后云端自动更新助教工作台任务。
- sxCoachTeamOps 已部署并回读成功；微信小程序 2.1.2 已上传测试版本，总包 3,159,311 bytes，未提交审核或发布正式版。
- 飞书版本 Base 记录：recvt7E2jSRUpE；更新公告：https://lxcmobp6gun.feishu.cn/wiki/BME6w6qA4iDTCMkoJEic96BnnAh。
- 验证与发布边界详见 开发日志_2026-08-23.md。

# 2026-08-23 主教练赛前分工原型收敛

- 按评审画板将主教练赛前协作收敛为“选择记录方式 → 添加助教 → 按数据或球员分工 → 系统自动通知”四步流程。
- 临时助教邀请以“转发小程序给微信好友或群”为主入口；手机号、现场二维码和历史教练折叠为次级方式，主页面不再同时展示多个二维码。
- 分工完成按钮统一为“完成分工”；已加入助教立即收到任务，待接受临时助教在接受首次邀请后自动获得本场权限，不再要求二次扫码或再次发送权限。
- PAD 使用三栏完整工作面，手机横屏使用同功能紧凑布局，保留相同的数据项、任务分配和协作状态，不删减手机端功能。
- 更新文件：`docs/原型-裁判教练协同/prototype.js`、`prototype.css`、`评审画板/board-data.js`、`评审画板/README.md` 及相关验收截图。
- 验证：`prototype.js`、`board-data.js` 语法检查通过；评审画板审计通过，5 个区段、30 个正式页面，待补页面、重复引用、缺失路径、缺失逻辑和无效类型均为 0。
- 发布边界：本次仅完成本地原型和评审画板更新，未修改小程序正式页面、未部署云函数、未上传微信版本、未推送远端仓库。

# 2026-08-29 独立教务系统后端与PC首屏正式部署

- 教务系统与赛事系统保持独立：新后端仅访问平台账号、机构成员关系及 `sx_education_*` / `sx_edu_*` 教务集合，不引用赛事、比赛或裁判集合。
- 新增并正式部署 `sxEducationCore`：覆盖PC校区负责人权限、教练小程序权限、教练/学员/班级/排课/课包、课堂提交审核、分钟课消账本及完整审计。
- 课消账本统一保存整数分钟；第一期支持60分钟与90分钟课，前台按课包单位换算显示；课包首次课消后单位锁定。
- 教练课堂提交后立即课消，PC端随后审核；评价采用态度、专注、技能、体能、团队五维1—5分，AI关键词评价必须经教练确认后提交，家长仅能看到PC审核通过的数据。
- 新增并正式部署 `sxEducationApi`，建立HTTP访问路径 `/api/education`；线上未登录回读正确返回“请先使用微信登录”。
- 新增正式PC页面 `admin/education-center.html`、`education-center.css`、`education-center.js`，提供总览、教练、学员、班级、排课、课包、审核和课消流水八个工作区，不写入演示数据。
- 机构开户页“进入教务中心”已改为正式教务中心入口；赛事中心继续使用原独立入口，两套业务数据不互通。
- 验证：两个云函数状态均为 `Deployment completed`；教务规则测试、JS语法、DOM契约和生产静态文件回读通过。
- 发布边界：本次正式部署云函数、HTTP入口和PC静态页面；版本号保持2.1.0；未上传或发布小程序，未创建测试业务数据，未推送Git远端。
- 线上首轮点击发现教务中心空弹窗遮罩常驻：根因是作者样式覆盖了HTML `hidden` 状态，导致导航和操作按钮被透明遮罩拦截；已增加强制隐藏规则并更新静态资源版本至 `20260829-2`。
- 新增浏览器点击级契约测试：验证默认无弹窗、八个工作区切换、教练表单字段、弹窗关闭和课包60/90分钟选项，六项全部通过；生产HTML与CSS回读通过。
- 教务总览移除“权限分工、系统边界、课消口径”等说明型大段文字，仅保留今日课消与待审核课堂两个即时指标。
- 新增真实经营图表：近7日课消柱状图、近7日课堂/审核双折线、出勤结构环图、课包金额进度、已课消/剩余课时结构、课堂审核结果分布；空集合只显示零值，不生成演示数据。
- 课包模板新增金额字段，教务聚合接口按正式学员课包和课消比例计算课包总额、已课消确认金额及待课消金额。
- 教务侧边栏已与赛事中心统一为56px导航行高、18px导航文字、260px侧栏及相同橙色选中态。
- `sxEducationCore` 与教务中心静态页面已正式部署；线上函数状态、HTML、CSS和JS回读通过，版本保持2.1.0。

# 2026-08-30 3.0教务系统原型正式落地

- 严格执行 `$yuanxing`：为课程管理v3、班级管理v2、排课管理v3、学员管理v1、员工与权限v2建立标准 `_assets` 包；每包5个布局区域、4张参考裁切、完整文字/交互/数据/素材清单，全部校验并依用户连续实施授权标记为 `implemented`。
- PC教务中心按3.0确认稿重构为固定侧栏、顶部栏、筛选工具条、左侧主列表/周课表和右侧详情结构；课程、课包、班级、排课、学员、教练、课堂审核和课消流水均读取正式接口，不写演示数据。
- 新增独立课程数据层；课程与课包保持分开，一门课程可配置多个专属课包，班级必须关联课程，所有选择器使用真实课程、教练、班级和课包，不再要求输入内部ID。
- 教练“添加”改为一次性小程序码与微信URL Link邀请；24小时有效，可复制、撤销和查询记录。教练在小程序填写姓名、手机号和擅长方向后立即加入机构，默认仅能访问本人课堂。
- 学员使用校区可轮换的服务号参数二维码；家长扫码/关注后由服务号回调创建专属家长会话并发送建档入口，可为多个孩子提交基础资料和可选头像。档案先进入待确认，校区补充课程、课包、成交金额、有效期和班级后才正式生效。
- 排课管理支持单节与批量生成、30分钟周课表、60/90分钟课堂、班级/教练/场地冲突检查、调课和停课；修复视觉验收中发现的时间刻度错列并重新截图。
- 新增 `sx_education_courses`、`sx_edu_invites`、`sx_edu_notice_logs` 自动初始化；新增 `sxEducationPublic` 与 `/api/education-public`，家长公共接口与PC会话、赛事集合隔离。
- 已部署 `sxEducationCore`、`sxEducationPublic`、`sxCreateTournamentQrCode`、`sxTournamentNotification`、`sxServiceAccountCallback` 和相关PC/H5静态页面；线上函数均为 `Deployment completed`，HTTP路由与生产文件回读通过。
- 视觉验收输出位于 `output/education-v3-validation/`，覆盖6个工作区的1672×941原型视口和1440×900适配视口；无阻断级或重大差异。
- 发布边界：版本保持2.1.0；本次未上传、未提交审核、未正式发布微信小程序，未推送Git远端。教练邀请的小程序接受页代码已写入 `native-dist`，需随下一次2.1.0小程序代码上传后进入体验联调。
- 收口检查：教务核心契约、PC浏览器点击、JavaScript/JSON/WXML检查全部通过；全仓 `npm run typecheck` 仍被既有 `config/index.ts` 的 `import.meta` 与当前 TypeScript module 配置不一致阻断，该文件不属于本次教务改动。

# 2026-08-30 小程序教务身份与机构绑定去演示化

- 移除教务首页“校长端/教练端”人工预览切换及虚拟课程、学员、课时、待办、周期报告和教练姓名；小程序进入教务页后统一调用 `sxEducationCore access.get` 按真实机构成员关系判定端口。
- 未加入机构的微信账号显示明确的空状态和三步绑定说明；扫描PC生成的教练小程序码后进入姓名、手机号、擅长方向确认页，接受后创建机构成员和教练档案，再自动进入真实教练端。
- 机构老板、校区负责人和教练不再共用演示身份：负责人进入真实教务概览，教练仅看到本人课堂、负责班级和本人学员任务；负责人小程序不再跳入旧校区演示数据页。
- 教练首页、课堂详情、课堂点名和我的班级已改为读取正式教务云数据；点名按出勤、请假、缺勤、补课、补课不扣和试听保存为课堂草稿，不直接展示或写入虚拟记录。
- `lesson.mine` 增加班级、课程和学员人数聚合；新增负责人小程序只读概览接口；课堂点名草稿不再强制要求课后五维评价，正式提交阶段仍保持评价校验。
- 新增 `test:education-mini`，验证无角色预览/演示数据、未绑定状态、扫码绑定、角色自动分流、真实云接口和WXML简单表达式。
- 微信开发者工具CLI预览编译成功：AppID `wx06d735da15276acd`，总包 `3,081,289` bytes，主包 `446,168` bytes，教练点名/班级/课堂详情分包均成功生成。
- `sxEducationCore` 已重新部署并回读成功。发布边界：保持2.1.0；本次只做微信预览编译，未上传代码版本、未提交审核、未发布正式版、未推送Git远端。
- 根据真机测试截图优化教练绑定表单：键盘弹起时改为顶部对齐并预留滚动空间；统一标题层级、必填/选填标识、输入框高度、字号、占位文字、权限说明和主按钮文案；手机号限制11位并增加 `cursor-spacing`，避免键盘遮挡底部操作。
- 中文WXML/WXSS/JS均按UTF-8读取核验，未发现 `U+FFFD` 替换字符或连续问号错码；WXML复杂表达式检查通过。
- 优化后微信开发者工具CLI预览编译再次成功：总包 `3,083,559` bytes，主包 `448,438` bytes。当前改动尚未再次上传测试代码版本。
- 按真机复核意见进一步精简教练绑定页：移除“擅长方向”和其下方权限介绍，仅保留真实姓名、手机号与确认操作；确认按钮改为104rpx高度、34rpx粗体，并使用flex保证文字水平与垂直居中。
- 精简后中文内容与UTF-8编码检查通过，未发现乱码、替换字符或残留“擅长方向”；微信CLI预览编译成功，总包 `3,082,943` bytes。当前改动仍需重新上传2.1.0测试代码后才能在扫码页面看到。

# 2026-09-01 家长建档二维码快速打开与破图回退

- 将“获取长期建档记录”和“远程生成服务号参数二维码”拆为两个接口：`gateGet/gateRotate` 立即返回建档链接，`gateQrRefresh` 在后台准备服务号通道，不再阻塞PC弹窗首屏。
- PC端始终使用本地二维码库即时生成可扫码的家长建档入口；同一机构将二维码Data URL和建档记录写入浏览器缓存，后续打开直接秒显。
- 弹窗先立即展示加载状态；远程服务号图片失败或临时地址失效时继续保留本地二维码，不再替换为白框破图。
- 家长H5在服务号二维码尚未准备完成时每1.5秒补偿查询，最多8次，避免家长比后台生成速度更快时看到空入口。
- 教务核心、PC脚本、样式与家长H5已正式部署；静态资源版本更新为 `20260901-1`，线上回读确认缓存、后台刷新和新资源版本生效。
- 验证：教务核心测试、PC点击测试、本地缓存断言和UTF-8检查全部通过。

# 2026-09-01 学员头像组件与小程序球员头像规则统一

- 家长学员建档页移除网页自建圆形裁剪框，改为复用小程序球员头像组件规则：正方形裁剪框、九宫格、四周遮罩、单指拖动、双指缩放、缩小/放大按钮和100%—300%缩放范围。
- 裁剪算法不只调整视觉：统一使用 `{left:72, top:72, size:496}` 方形范围约束照片边界，并仅将框内区域输出为320×320、质量0.92的JPEG临时图，再调用 `sxUploadAvatar` 生成人像分割透明PNG。
- 家长页上传区域保留“头像预览＋上传说明＋上传/更换按钮”，已上传结果直接显示人像分割后的透明 PNG 图层，不再使用圆形或虚线头像框。
- 新增共享规则 `通用赛事球员学员头像上传裁剪与人像分割组件规则.md`，覆盖输入格式、裁剪、手势、输出、人像分割、权限、安全、失败回退、数据字段、足球复用和必测用例；共享赛事规则索引升级为V1.3。
- 新增 `test:education-avatar`，验证无圆形裁剪框、方形九宫格、320×320输出和统一人像分割云函数；测试及UTF-8检查通过。
- 家长建档HTML/CSS/JS已正式部署，静态资源版本更新为 `20260901-2`，线上回读确认方形裁剪组件生效。
- 按建档页视觉反馈继续移除最终预览的圆形虚线容器：透明人像使用 `contain` 完整显示，未上传时仅显示圆角矩形占位；共享头像规则同步补充“建档确认页透明图层、业务列表可按需圆形展示”的边界。
- 家长建档HTML/CSS正式静态资源更新为 `20260901-3`；官网域名与CloudBase默认域名回读均为HTTP 200，并确认线上CSS不存在头像圆框规则。
- 修复透明头像上传后“孩子头像”占位层仍强制显示导致的叠层错位；已上传状态明确隐藏占位元素，透明人像继续按 `contain` 完整显示。
- 家长建档新增可选18位身份证号识别：前端校验格式、出生日期和校验位，自动回填出生日期、性别并动态提示年龄；服务端再次校验，完整身份证号不入库，仅保存脱敏号码和带业务命名空间前缀的SHA-256指纹用于重复档案校验。
- `sxEducationPublic`及建档HTML/CSS/JS已正式部署，静态资源版本更新为 `20260901-4`；线上回读确认身份证字段、自动识别脚本、占位层隐藏规则和新资源版本均已生效，云函数状态为部署完成。
- 修复微信进入家长建档页时首轮接口时序造成的“建档码无效”闪现：建档码首次校验失败改为按450/800/1200/1800毫秒静默退避重试，重试期间只显示“正在验证建档入口…”，仅在连续失败后才展示最终错误；服务号二维码轮询继续独立计数。
- 建档HTML/JS静态资源更新为 `20260901-5`并正式部署；官网回读确认新版资源、静默重试和验证中状态均已生效，相关语法及页面契约测试通过。
- 优化已关注家长的建档入口：服务号关注只需一次，建档页主操作改为“微信确认并开始建档”，通过 `snsapi_base` 静默识别服务号身份；“查看服务号二维码”降为仅供未关注家长使用的次级入口，二维码默认不展开。
- 微信服务号回调对30秒内相同家长、相同建档场景的重复扫码事件静默去重；已关注用户再次扫码时卡片标题明确显示“已关注，继续为孩子建立训练档案”，避免误解为再次关注。
- `sxTournamentNotification`、`sxEducationPublic`、`sxServiceAccountCallback`及建档HTML/CSS/JS已正式部署，静态资源版本更新为 `20260901-6`；三个云函数状态均为部署完成，官网回读确认新文案、主次入口和静默身份路由已生效。
- 根据实际扫码反馈移除微信内“微信身份确认”人工停留页：微信扫码进入且尚无建档会话时，页面隐藏身份卡片并自动发起静默OAuth；已关注家长直接进入建档表单，旧会话失效时自动清理并重新识别。非微信浏览器才保留备用说明入口。
- 建档HTML/JS静态资源更新为 `20260901-7`并正式部署；线上回读确认身份卡片默认隐藏、微信环境识别、自动OAuth和无手动点击跳转均已生效，页面契约测试通过。

# 2026-08-30 足球复用版赛程生成与A4输出规则

- 基于篮球PC赛事中心现行实现，补全 `docs/赛小蜂篮球赛程设置逻辑（足球借鉴版）.md`，形成足球端可直接实施的赛制、排赛、正式竞赛日程、A4打印、另存PDF和Excel导出规则。
- 固化正式日程八列字段、按竞赛组别独立成表、场序胜负来源、横竖版纸张尺寸、页边距、多页重复表头、Excel合并单元格和错误处理方法。
- 新增足球占场时长、最少休息、整场/子场互斥、加时与点球预留等项目参数，明确算法复用与品牌/规则替换边界。
- 验证：文档UTF-8读取正常；本次仅更新规则文档和开发记录，未修改PC业务代码、未部署、未推送远端仓库。
- 补充小组赛加淘汰赛的全员排位规则：明确16队四组四队时，前8名进入1—8名线，未出线球队进入9—16名线；两条8队名次线均按胜负递归分流，分别用12场决出唯一名次，连同24场小组赛共48场。
- 单独对比“小组赛 + 淘汰赛”和“单败淘汰”：前者按小组名次进入不同名次线，后者从抽签签位直接开赛；同时明确淘汰轮名称按进入该名次线的球队数计算，而不是按赛事总队数计算。
- 继续补充足球端可实施的赛制生成器口径：新增结构化来源、胜负去向、8队名次线复用模板、函数拆分，以及16队小组淘汰、普通单淘和单淘全员排位的场数与拓扑测试。

# 2026-09-01 裁判计分台电视大屏第一版

- 新增独立只读电视大屏 `scoreboard.html`：使用6位大屏码绑定正式比赛，展示主客队、超大比分、比赛时间、节次、24秒、球队犯规、暂停、球权和比赛状态；支持浏览器全屏、更换比赛、断线遮罩、自动重连和每秒云端校时。
- 保持既有裁判横屏控制台布局和操作集合不变；正式比赛启动时由 `sxMatchFlow` 生成唯一6位大屏码，裁判进入计分台后弹窗提示并可复制。大屏只读，不提供比分、时间或比赛状态写入口。
- 扩展正式比赛现场状态：裁判端继续以每秒一次的节流频率同步比分、节次和基准时间，并补充犯规、暂停、球权、24秒及节间/暂停倒计时；电视端在两次同步之间根据服务器时间戳本地走秒，避免逐帧写数据库。
- 新增 `sxScoreboardDisplay` 公共只读云函数和 `https://www.sxfbasketball.cn/api/scoreboard` 路由；接口只接受6位大屏码并返回裁剪后的公开比赛状态，不返回裁判身份、教练身份、球员名单或内部权限数据；路由启用总量和单IP限流。
- 已部署 `sxMatchFlow`、`sxScoreboardDisplay` 和大屏HTML/CSS/JS静态文件；线上回读确认页面与资源均为200，大屏接口对无效码返回预期404 JSON，路由上游为 `sxScoreboardDisplay`。
- 验证：相关JavaScript语法检查、Git差异检查、大屏数据契约和1920×1080浏览器视觉检查通过；截图位于 `outputs/电视大屏验收/电视大屏-1920x1080.png`，页面无纵向溢出。全仓 `npm run typecheck` 仍被既有 `config/index.ts` 的 `import.meta` 与TypeScript module配置不一致阻断，与本次改动无关。
- 发布边界：未调整版本号，未上传、提交审核或发布微信小程序，未推送GitHub/Gitee。大屏网页与云端只读接口已可访问；裁判端“大屏码”弹窗需随下一次2.1.0小程序代码上传后进入体验联调。
- 大屏视觉复核后将主客队比分改为与裁判计分板一致的七段数码管结构；暗线段保留低亮底色，亮线段使用高对比白色，支持一至三位比分自适应。
- 球权信息改为“左箭头｜居中节次｜右箭头”，当前持球方箭头橙色高亮，取消球队名称长文本，避免中间区域随队名长度跳动。更新后1920×1080专项测试、截图复核及生产HTML/CSS/JS回读均通过。
- 比赛时间进一步改为橙色四位七段数码管与独立双点冒号，整体尺寸和数字间距均小于两侧比分并集中在中轴；保持服务器校时和本地走秒逻辑不变。专项测试确认四位时间数字、两位比分、球权箭头和1080P无溢出，生产页面及资源回读均为200。
- 按现场远距观看反馈继续放大主比赛区：两侧比分、队徽、队名、比赛时间、节次箭头及犯规暂停统一增大；比分七段线条单独加粗，进攻时间缩小为辅助层级。正式大屏接口兼容球队 `logo`、`logoUrl`、`logoFileID`，并将云存储队徽转换为浏览器可用的临时HTTPS地址；无队徽时才显示首字占位。更新后的1080P截图无溢出，云函数、静态样式和脚本均已部署并回读通过。
- 两侧球队信息按用户反馈再次统一放大约20%：队徽、队名、七段比分、犯规与暂停同步增大，中间比赛时间、节次、球权和进攻时间保持原尺寸。1920×1080截图确认无滚动、裁切或中轴挤压，生产CSS回读确认四组放大参数均已生效。
- 两侧七段比分由白色改为与比赛时间一致的橘红色，暗线段同步使用低亮橘红轮廓并增加同色辉光；犯规、暂停和中间层级保持不变。1080P视觉复核与生产CSS回读通过。
- 用户确认当前小程序版本为2.1.4，本次电视大屏功能使用2.1.5测试版本。裁判横屏计分板顶部新增“电视大屏”常驻入口，仅在正式比赛已生成6位大屏码后显示；点击后打开只读连接面板，可复制大屏码或包含场次码的完整网址，不再在进入计分板时自动弹窗打断裁判。
- “我的”版本日志已更新为2.1.5，用户可见更新内容包括裁判大屏入口、电视端实时比赛状态、自动校时/重连和球队队徽。`package.json`、`package-lock.json`根版本及小程序展示版本均为2.1.5。
- 微信开发者工具CLI最终预览与上传成功：AppID `wx06d735da15276acd`，总包 `3,187,914` bytes，主包 `454,130` bytes，计分板分包 `261,623` bytes。上传说明为电视大屏入口和实时同步能力；当前仅上传开发测试代码，未提交审核、未发布正式版、未推送GitHub/Gitee。
- 快速比赛已补充临时大屏会话：从首页开始快速比赛后自动创建12小时有效的6位大屏码，裁判端每秒同步公开比赛状态，结束比赛后关闭会话；临时会话不写入正式赛事集合。新增 `sxQuickScoreboard`，写操作只认微信云函数上下文中的当前用户身份，公开网页继续仅按大屏码只读。
- 快速比赛大屏版已覆盖上传同一2.1.5开发测试版本：总包 `3,193,017` bytes，主包 `454,154` bytes，计分板分包 `264,318` bytes；未提交审核、未发布正式版、未推送Git远端。
- “我的”页底部版本号及版本更新日志已再次核对为2.1.5，2.1.4调整为历史版本；最终预览编译成功，总包 `3,192,982` bytes。随后覆盖上传遇到微信开发者工具 `code 10`，当时用户侧正在提交2.1.5开发版，未继续并发重试以免打断提交；此前成功上传的2.1.5包已包含版本底部更新。
- 版本边界纠正：用户确认当前对外版本仍为 `2.1.4`，尚未启用 `2.1.5`。本地 `package.json`、`package-lock.json` 根版本与“我的”页当前版本已恢复为 `2.1.4`；电视大屏相关开发内容并入 2.1.4 审核前收口，不创建 2.1.5 公开更新记录。
