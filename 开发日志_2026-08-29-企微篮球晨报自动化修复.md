# 开发日志｜企微篮球晨报自动化修复

> 日期：2026-08-29  
> 当前分支：`codex/education-2.0`  
> 远端代码：未提交、未推送

## 问题

- 服务器和09:00调度均正常，8月28日、29日自动生成也成功。
- 两天都因固定3条新闻中有1条超过严格24小时窗口，整份晨报进入 `review_required`，未创建企微任务。
- 用户感知为“定时任务没有运行”，实际是“运行后被来源时效门禁拦截”。

## 修复

- 新闻窗口改为：优先最近24小时，不足3条时最多回看72小时。
- 超过72小时的内容仍会拦截，不使用更早旧闻凑数。
- 更新核心默认值、服务器生产配置、环境示例、内容规则和部署说明。
- 使用新规则重新校验8月29日已生成的3条新闻并创建待发送任务。
- 新增失败不静默机制：内容门禁拦截、连续3次生成失败或企微建任务失败时，通过自建应用向群主发送内部告警。
- 配置自建应用 AgentId `1000007`，内部告警测试发送成功。

## 验证

- `node tools/test-wecom-group-briefing.js`：通过。
- `npm run check --prefix cloudfunctions/sxWecomGroupBriefing`：通过。
- 服务器容器：healthy；调度、自动建任务、自动提醒均为开启状态。
- 8月29日任务：`task_created`，唯一目标群1个。
- 群主提醒：成功触发1次。
- 回读确认提醒最小间隔已生效，证明今天提醒已经发出，没有重复催办。

## 边界

- 外部客户群仍需群主在企微点击一次确认发送。
- 当前工作区存在大量其他未提交改动，本次未切换分支、未提交、未推送。
