# 开发日志｜2026-07-31

## 官网每日 AEO/GEO 与 SEO 批次

### 当日判断

- 北京时间 2026-07-31 11:05 开始检查；自动化记忆、文章目录、内容中心、Sitemap、`llms.txt` 和开发日志均只完成到 7 月 30 日，因此今天尚无完整批次。
- 工作树存在后台、小程序、官网首页、公共样式和历史 SEO 文件的既有未提交改动。本任务未回滚、暂存或改动无关文件，只处理三篇官网文章、内容中心、Sitemap、`llms.txt`、收录与外链台账和本日志。

### 收录优先巡检

- 正式域名与篮球 CloudBase 系统域名在部署前各回读 Sitemap 中 28 个 URL，加 `robots.txt`、`llms.txt`，共 60 项，全部 HTTP 200。
- 公开搜索检查 `site:sxfbasketball.cn 赛小蜂篮球`、`"赛小蜂篮球" -site:sxfbasketball.cn`、`"赛小蜂" 篮球 官网`、`"sxfbasketball.cn" -site:sxfbasketball.cn`，未出现可核验的官网 URL；无关“小蜂”和篮球页面已排除。
- 当前环境没有可调用的百度搜索资源平台或 Bing Webmaster Tools 登录态接口，无法读取最新 URL 检查、Sitemap 处理、已发现 URL、抓取、索引量、展示、点击、查询词与平均位置。
- 百度保持“已手动提交，平台反馈待更新”；不重复提交。
- Bing 首页沿用 2026-07-29 “已成功编制索引”证据，只登记首页已收录；Bing Sitemap 沿用“正在处理、已发现 URL 0”，不重复提交。
- HTTP 200、Sitemap 成员和公开搜索空结果均未被当作收录证据。

### 三篇内容

1. **篮球比赛开赛前怎么检查计分台？设备、队伍、时间与人员清单**
   - 意图层级：核心长文。
   - 搜索任务：开赛前完成计分台比赛身份、球队名单、计时方式、人员分工和操作测试。
   - 正式 URL：https://sxfbasketball.cn/articles/posts/basketball-score-table-pregame-check.html
   - 质量评分：46/50（直接性 10、证据匹配 10、具体性 9、中文流畅 9、声音与结构 8）。
2. **篮球比赛计时错了怎么办？先停表、确认时间再恢复**
   - 意图层级：单一问题答疑。
   - 搜索任务：处理误启动、漏停和计时方向错误，不凭感觉补时间。
   - 正式 URL：https://sxfbasketball.cn/articles/posts/basketball-clock-error-handling.html
   - 质量评分：45/50（直接性 10、证据匹配 10、具体性 8、中文流畅 9、声音与结构 8）。
3. **篮球比赛临时中断后，计分台怎样安全恢复？**
   - 意图层级：具体场景文。
   - 搜索任务：中断时冻结状态，恢复前核对比赛、比分、时间、队伍事件和操作责任。
   - 正式 URL：https://sxfbasketball.cn/articles/posts/basketball-interruption-resume-scoring.html
   - 质量评分：46/50（直接性 10、证据匹配 10、具体性 9、中文流畅 9、声音与结构 8）。

三篇均包含直接答案、自指 canonical、Article JSON-LD、正文可见 FAQ 与匹配 FAQPage、首页上下文链接、自然内链、发布日期、作者/发布者和产品边界。内容未编造客户案例、排名、价格、政策、市场数据、自动识别能力或跨设备自动恢复承诺。

### 站点接入

- `articles/index.html` 新增三张文章卡片，编号 22—24。
- `sitemap.xml` 新增三条正式 URL，内容中心 `lastmod` 更新为 2026-07-31。
- `llms.txt` 新增三条公开资源并更新日期。
- `docs/AEO_GEO_INDEX_TRACKER.md` 记录百度、Bing、品牌词、Sitemap 与今日 URL 的证据边界。
- `docs/AEO_GEO_BACKLINK_TRACKER.md` 记录外链回读与今日唯一人工计划。

### 外链巡检与人工计划

- GitHub、CSDN 公开页面和目标官网均回读 200，页面仍包含正式域名；有效引用域保持 2 个。
- Gitee 公开仓库回读 200，但未发现 `sxfbasketball.cn`；不计入有效引用域。
- 新增有效外链 0、无链接品牌提及 0、失效外链 0。
- 今日唯一人工任务：在知乎手动回答“篮球比赛计时出错后应该怎么处理？”，只在内容相关位置加入今日计时错误文章链接；发布后保留公开 URL 供核验。本自动化未登录、建草稿、投稿或提交站长平台。

### 验证、部署与回读

- `npm run site:validate` 通过：24 篇文章、31 个 Sitemap URL。
- `npm run site:deploy` 通过 `tools/sxf-cloud.ps1` 进入 `sxf-basketball-d9gp6yt0rd1f7be4d` 并完成部署；未使用全局 CloudBase 登录、未切换环境、未读取或输出凭据。
- 独立回读 CloudBase 系统域名和 `https://sxfbasketball.cn/`：每个域名检查 31 个 Sitemap URL，加 `robots.txt`、`llms.txt`，各 33 项全部 HTTP 200。
- 两个域名的 24 篇文章 title 与本地源一致，canonical 全部指向唯一正式域名文章 URL。
- 两个内容中心均出现今日三篇；线上 Sitemap 均为 31 个 URL。
- 部署后的三个精确标题公开搜索未出现可核验的今日页面，新页面状态登记为“已上线，待复查”，不记为“已发现”或“已收录”。

### Git 与未解决问题

- 未创建 Git 分支，未提交，未推送 GitHub 或 Gitee。
- 待复查百度人工提交反馈、抓取与索引数据；待复查 Bing Sitemap 处理状态和已发现 URL 数。
- 品牌词公开搜索仍未出现可核验官网结果。
- HTTP 根首页和 HTTPS `/index.html` 到唯一 HTTPS 根首页的 301 收敛未在本任务中实施。

## “篮见”公众号兼职运营交付包

### 完成内容

- 在 `运营资料/篮见公众号运营包/` 建立完整执行资料，包含使用说明、WorkBuddy 快捷指令、企微承接与分发话术、8 周内容排期、首次上线验收清单。
- 制作《篮见公众号兼职运营手册》，覆盖账号定位、五类选题、每日工作流、四轮审稿、公众号草稿箱边界、企微承接、8 周考核和合作边界。
- 制作 8 周运营数据表，包含“8周看板、每日文章、企微线索、周复盘、配置与口径”5 张工作表，预排 2026-08-03 至 2026-09-25 的 40 个工作日，并配置公式、下拉项、目标线和趋势图。
- 创建 WorkBuddy 自定义 Skill `basketball-training-editor`，显示名为“篮球教培行业主编”；技能将选题、证据、大纲、正文、审稿、排版和草稿交付拆成必须人工确认的阶段。
- 将技能安装到当前 Windows 用户的 `C:\Users\15043\.workbuddy\skills\basketball-training-editor`，并在交付包内保留可导入压缩包。

### 验证

- WorkBuddy Skill 结构校验通过，交付源文件与本机安装文件的 SHA-256 逐项一致。
- 微信草稿辅助脚本通过 Node 语法检查和帮助命令检查，只包含草稿箱写入能力，不包含发布或群发接口。
- Word 手册共 10 页，已转换为 PDF 并逐页检查，无文字截断、表格越界或页面重叠。
- 数据表公式错误扫描为 0，5 张工作表均已渲染检查；修正看板“企微漏斗”区域列宽后复验通过。

### 当前边界

- 本次未注册或改名公众号，名称仍需兼职本人在微信公众平台检查。
- 未读取、写入或保存公众号 AppID、AppSecret、企业微信渠道码等敏感信息。
- 未调用公众号发布、群发或企微自动销售能力；草稿箱接入仍需兼职本人在自己的电脑上配置权限并完成小范围验证。
- 当前分支为 `codex/education-2.0`；本次未暂存、提交或推送 GitHub/Gitee，也未改动工作树中的无关业务文件。

## 赛事中心：赛事空间层级、全国地址与图片处理

### 完成内容

- 将“赛事空间”从“赛事管理”的二级标签调整为赛事中心最顶层入口；进入赛事中心默认先看到空间列表。
- 只有进入具体赛事空间后，才展示赛事工作台、赛事管理、报名与资格、抽签与分组、球队与球员、赛程与场次、现场执行、赛果与数据。
- 赛事空间内部固定页首新增“返回空间列表”，返回后收起具体赛事的导航和上下文信息。
- 创建赛事和赛事资料共用全国省、市、区县三级联动地址库。数据使用 `chinese_regions 0.5.5`，覆盖 34 个省级、363 个城市层级、3245 个区县层级。
- 赛事 Logo 上传增加 1:1 裁剪、缩放与位置调节、纯色背景自动扣除、容差调节、透明棋盘预览和透明 PNG 输出。
- 已有透明背景的 Logo 会识别四角透明像素并保留原透明层，避免错误扣除深色主体。
- 赛事主背景上传增加 16:9 裁剪、缩放与位置调节、压缩质量控制，确认后输出 1600×900 WEBP 压缩图。

### 变更文件

- `admin/tournament-center.html`
- `admin/tournament-center.js`
- `admin/tournament-center.css`
- `admin/assets/data/china-regions.js`
- `admin/assets/data/全国行政区划数据说明.md`
- `tools/构建全国行政区划数据.js`

### 验证

- `node --check admin/tournament-center.js` 通过。
- `node --check tools/构建全国行政区划数据.js` 通过。
- 全国地址交互验证：省级选项 34 个，上海默认联动至上海市、浦东新区。
- Logo 输出验证：512×512 PNG，测试图包含 110554 个全透明像素。
- 赛事主背景输出验证：1600×900 WEBP。
- 已在 1670×941 与 1440×900 检查空间列表、空间内工作台、固定页首侧栏、返回空间按钮、三级地址弹窗、Logo 裁剪和主背景裁剪。
- 验证截图保存在 `outputs/赛事中心验证/`。

### 当前边界

- 本轮为 PC 端可交互原型，图片处理在浏览器本地完成，尚未接入正式对象存储上传接口。
- 行政区划保存结构已保留区划编码；正式上线时需同时保存中文名称快照并建立数据更新机制。
- 未暂存、提交或推送 GitHub/Gitee。

## 创建赛事：透明 Logo 成品预览修正

- 将创建赛事弹窗中的 Logo 预览由横向上传条调整为 160×160 方形成品框。
- 预览区使用透明棋盘底，图片保持 `contain` 完整展示，并标记“透明 PNG 成品预览”。
- “重新上传”保留在方形预览框底部，创建按钮区域固定在弹窗底部，常见 PC 高度下无需寻找操作按钮。
- 验证输出为 512×512 PNG，页面预览框实际尺寸 158×158，未出现横向拉伸或裁切。
- 验证截图：`outputs/赛事中心验证/创建赛事-方形透明Logo预览-v4.png`。
- 补充修正未上传空状态：创建赛事 Logo 容器增加独立 `logo-square-upload` 样式，上传前后均强制保持 160×160 正方形，不再出现横向长条。
- 修正赛事创建后的页面路径：点击“完成创建”后继续停留在赛事空间列表，只新增赛事卡片；不再自动跳到赛事资料。
- “进入赛事空间”作为独立动作，点击后才切换当前赛事并进入赛事管理的赛事资料页。

## 赛事空间：创建赛事Logo与卡片持久化

### 完成内容

- 创建赛事弹窗新增必填赛事 Logo，沿用 1:1 裁剪、去底验证和 512×512 透明 PNG 输出流程。
- 上传 Logo 时保留已填写的赛事名称、日期、地区和首个组别；处理完成后返回创建弹窗继续确认。
- “创建并进入”现在会真实新增赛事空间数据、生成赛事卡片、选中新赛事并进入赛事资料页。
- 新赛事的名称、日期、地区、首个组别、Logo、状态和基础统计写入本地演示数据；刷新页面后仍然保留。
- 在赛事资料页重新上传 Logo 后，赛事空间卡片同步更新，不再只显示临时预览。
- 顶部赛事选择器与赛事卡片读取同一份赛事空间数据，切换和进入卡片都会更新当前赛事上下文。
- 赛事空间卡片改为固定窄卡片：常规宽度 230px，较窄视口为 220px，不再平均撑满整行。

### 验证

- `node --check admin/tournament-center.js` 通过。
- 自动创建“2027 蜂动城市篮球挑战赛”验证通过：创建后卡片数量由 3 增至 4，Logo 为透明 PNG，卡片宽度 230px。
- 页面刷新后新赛事卡片、Logo 和当前赛事 ID 均保留。
- 1670×941 验证截图：`outputs/赛事中心验证/创建赛事生效与窄卡片-v3.png`。

### 当前边界

- 当前为外部演示原型，赛事空间数据保存在浏览器本地；正式上线时需替换为赛事创建接口、对象存储上传和机构级权限校验。
- 未暂存、提交或推送 GitHub/Gitee。

## 赛事空间：透明 Logo、缩放适配与删除赛事

### 完成内容

- 赛事空间卡片 Logo 取消盾形裁剪、白色边框、圆角、底色和阴影，直接使用透明 PNG 原图并按 `contain` 完整展示。
- Logo 编辑器由“强制铺满正方形裁剪”调整为“完整适配正方形画布”；100% 默认显示完整原图，缩放范围扩展为 30%—300%，横向文字 Logo 可继续缩小。
- 去背景处理改为先在保持原始宽高比的临时画布中扣除纯色背景，再缩放到 512×512 透明画布，避免完整适配后因画布四角透明而跳过去背景。
- “赛事更多操作”新增“删除赛事”，提供独立二次确认、不可恢复提示和危险操作样式；演示数据至少保留一个赛事空间。

### 验证

- `node --check admin/tournament-center.js` 通过。
- 使用实际素材 `赛事LOGO.jpg` 验证：缩放下限为 30%，100% 时有效图像范围完整位于 512×512 画布内，名称未被裁断。
- 自动交互验证卡片 Logo 计算样式：边框为 0、圆角为 0、`clip-path: none`、背景透明、`object-fit: contain`、无滤镜。
- 自动交互验证“赛事更多操作 → 删除赛事 → 确认删除赛事”链路正常展示。

### 当前边界

- 本轮未执行正式删除接口，只删除当前浏览器保存的赛事演示数据；正式接入后台后需增加服务端权限校验、关联数据检查和审计记录。
- 未暂存、提交或推送 GitHub/Gitee。

## Logo 编辑器垂直位置修正

- 修正 Logo 处于完整适配状态、图片高度小于正方形画布时“垂直位置”滑杆不生效的问题。
- 水平与垂直位置现在同时支持两种状态：图片小于画布时可在剩余留白内移动，图片放大超过画布时可调整裁切位置。
- 正值对应向右、向下移动，负值对应向左、向上移动；居中值保持 0。
- `node --check admin/tournament-center.js` 验证通过。

## 赛事中心真实空白测试体验

### 完成内容

- 赛事中心测试数据升级为独立的 `v2` 本地存储，新用户首次进入时不再自动生成三项演示赛事；旧 `v1` 演示数据仍保留在浏览器中，未执行删除。
- 无赛事时强制回到“赛事空间”一级入口，只展示创建赛事入口，不允许直接进入没有赛事上下文的工作台和业务模块。
- 创建赛事表单取消预填赛事名称和日期；首个组别改为可选，默认“暂不创建组别”。
- 新赛事创建后保持在赛事空间列表，新卡片真实显示 `0 个组别 / 0 支球队 / 0 场比赛`；点击“进入赛事空间”后再进入赛事资料。
- 新赛事工作台改为空数据状态：今日场次、进行中、异常、数据任务全部为 0，并提供赛事资料、组别赛制、报名入口、场馆球场四项配置入口。
- 报名、抽签、球队、赛程、现场、赛果等业务模块在未产生真实数据前统一展示空状态，不再混入示例球队、球员、赛程、人员或赛果。
- 组别数据按赛事空间保存；新建、编辑组别后同步更新赛事卡片组别数量。
- 允许删除最后一个赛事空间；删除后恢复到首次创建状态。

### 验证

- `node --check admin/tournament-center.js` 通过。
- 自动交互跑通：空列表 → 创建赛事 → 上传并处理 Logo → 新增赛事卡片 → 进入赛事空间 → 打开空工作台。
- 创建后的测试赛事为 `0 个组别 / 0 支球队 / 0 场比赛`；工作台四项指标均为 0。
- 已在 `1670×941` 和 `1440×900` 检查空工作台布局，无横向错位和内容遮挡。
- 验证截图保存在 `outputs/赛事中心验证/真实空白赛事体验-创建起点-v1.png`、`真实空白赛事体验-空工作台-1670-v1.png`、`真实空白赛事体验-空工作台-1440-v1.png`。

### 当前边界

- 本轮仍为浏览器本地交互原型；赛事、组别和图片尚未写入正式后台接口或对象存储。
- 未暂存、提交或推送 GitHub/Gitee。

## 赛事中心全生命周期自测与状态闭环

### 完成内容

- 为每个赛事空间增加独立生命周期状态，串联赛事资料、组别、报名、球队入驻、资格审核、抽签、场馆、排赛、现场人员、比赛回传、赛果复核与结束赛事。
- 修复“新增组别后赛事仍永久停留在空白状态”的问题；各业务模块现在根据真实前置步骤开放，不再只依赖单一 `isBlank` 标记。
- 测试模式增加两项隔离模拟能力：模拟小程序接收 4 支合格球队、模拟计分台回传比赛结果；正常业务边界仍保持 PC 负责管理审核，小程序负责球队入驻与现场执行。
- 自动排赛按当前球队数生成场次数；4 支球队的单循环测试生成 6 场，不再固定显示旧演示数据中的 86 场。
- 工作台改为读取当前赛事的组别、球队、赛程、现场和赛果状态；新测试赛事不再混入旧演示赛事的 18 场、24 支球队等数字。
- “结束赛事”增加前置校验：未生成赛程、比赛未回传或赛果未复核时禁止结束，并明确列出缺失步骤。
- 完成赛事后状态更新为“已结束”、进度为 100%，赛事空间转为只读，修改类操作会被拦截。
- 新增可重复运行的端到端测试脚本 `tools/test-tournament-lifecycle.js`，使用独立浏览器数据目录，不影响用户当前浏览器和已有赛事数据。

### 全链路验证

- 空账号进入赛事空间列表。
- 创建赛事、上传并裁剪透明 Logo，创建后新增零数据赛事卡片。
- 验证未完成赛程和赛果时不能提前结束赛事。
- 进入赛事空间并保存赛事资料。
- 创建 `U12 测试组`，开放报名并生成入驻入口。
- 模拟小程序回传 4 支合格球队，完成自动抽签并保存分组。
- 保存场馆资源并自动生成 6 场赛程。
- 刷新页面后赛事阶段和赛程数量保持不丢失。
- 工作台读取当前赛事的 `4 支球队 / 6 场比赛`，未出现旧演示数字。
- 完成现场人员安排，模拟小程序计分台回传全部赛果。
- 主办方批量复核全部赛果，结束赛事。
- 最终赛事状态为 `已结束`，进度 `100%`，空间内显示只读状态。
- 页面控制台错误数量为 0。

### 验证命令与产物

- `node --check admin/tournament-center.js` 通过。
- `node --check tools/test-tournament-lifecycle.js` 通过。
- `node tools/test-tournament-lifecycle.js` 通过全部 14 项断言。
- 最终状态截图：`outputs/赛事中心验证/赛事全生命周期-结束状态-v1.png`。

### 当前边界

- 当前完整闭环仍运行在浏览器本地原型数据中；“模拟球队入驻”和“模拟比分回传”仅在 `?demo=1` 测试模式显示。
- 正式上线需将赛事状态、球队入驻、计分台回传、赛果复核和结束赛事分别替换为服务端接口，并增加机构权限、幂等校验与审计日志。
- 未暂存、提交或推送 GitHub/Gitee。

## 创建赛事：Logo 展示底色预设

- 创建赛事弹窗新增深色、白色、浅灰、暖白、品牌橙五种 Logo 展示底色。
- 底色选择与透明 Logo 文件分离，只控制赛事空间卡片的展示区域，不把底色重新写入透明 PNG。
- 创建弹窗采用左侧 Logo 成品预览、右侧底色选择的并排结构，常见 PC 高度下不会被底部操作栏遮挡。
- 所选底色随赛事数据保存，创建后及刷新页面后继续生效。
- 赛事资料页同步提供底色修改入口，方便主办方后续根据 Logo 的黑字、白字或主色调整可读性。
- 旧赛事未保存底色时默认沿用深色背景，不改变既有页面效果。
- `node --check admin/tournament-center.js` 通过；端到端脚本新增底色即时预览、保存到赛事卡片两项断言，完整测试 16 项全部通过，控制台错误为 0。
- 验证截图：`outputs/赛事中心验证/创建赛事-Logo预设底色-v1.png`、`outputs/赛事中心验证/赛事全生命周期-结束状态-v1.png`。
- 未暂存、提交或推送 GitHub/Gitee。

## 赛事空间：卡片编辑入口与竞赛组别中间层

- 赛事空间卡片新增“编辑”按钮，点击后直接进入对应赛事的赛事资料页；“进入赛事空间”继续作为赛事运营入口。
- 两个按钮改为 `64px + 自适应宽度`，文字固定单行，窄卡片中不再出现“进入赛事空间”换行。
- 创建赛事弹窗移除“首个组别”，创建动作现在只生成赛事空间，初始数据固定为 `0 个组别 / 0 支球队 / 0 场比赛`。
- `tournament-center.html` 为赛事中心 CSS/JS 增加本地资源版本号，避免 `file://` 页面继续命中旧缓存而显示已删除的“首个组别”字段。
- 点击“进入赛事空间”后默认先进入“竞赛组别”页面，不再先进入赛事资料。
- 竞赛组别页面明确“一项赛事包含多个组别”的层级关系；每个组别分别配置比赛形式、赛制、目标球队、名单范围、比赛时长和晋级规则。
- 新增赛事仍可从组别页创建任意数量组别，后续报名、抽签、排赛和赛果均基于赛事下的组别继续推进。
- 端到端测试新增“创建弹窗不存在组别字段”“编辑按钮进入对应赛事”“进入赛事先到组别页”三项断言，完整 18 项验证全部通过，控制台错误为 0。
- 验证截图：`outputs/赛事中心验证/赛事空间-竞赛组别入口-v1.png`、`outputs/赛事中心验证/赛事全生命周期-结束状态-v1.png`。
- 未暂存、提交或推送 GitHub/Gitee。
## 赛事资料标识与组别选择优化

- 赛事资料页移除无业务用途的“赛事主背景”上传入口及说明。
- 赛事标识改为摘要展示：沿用创建赛事时选择的 Logo 展示底色，上方显示透明赛事 Logo，下方显示赛事名称，仅保留“修改”入口。
- “修改赛事标识”弹窗可重新上传、裁剪 Logo，并调整赛事卡片展示底色。
- 新增竞赛组别时，“组别名称”由自由输入改为规范下拉菜单，提供 U6 至 U18、成人公开组和暑期 3V3 等常用选项。
- 资源版本更新为 `20260731-6`，避免本地页面继续读取旧缓存。
- 已完成赛事全生命周期自动验证，共 21 项断言通过，控制台无错误。
- 视觉验证：`outputs/赛事中心验证/赛事资料-标识摘要-v1.png`、`outputs/赛事中心验证/新增竞赛组别-规范下拉-v1.png`。
- 本次未暂存、未提交、未推送仓库。
## 竞赛组别出生日期资格联动

- 将原“参赛年龄或出生日期”自由输入项改为“最早出生日期”日期选择器。
- 组别与赛事年份自动联动：例如 2026 年赛事选择 U12，默认生成 `2014-01-01`，即此前出生的球员不能参赛。
- 主办方可根据具体赛事规程手动调整自动生成的日期，页面同步显示通俗资格说明。
- 成人公开组、3V3 等不带 U 年龄标识的组别默认不自动限制，可由主办方自行设置。
- 出生日期限制以 `birthCutoff` 保存到组别数据，供后续报名资格自动校验读取。
- 资源版本更新为 `20260731-7`；赛事全生命周期 23 项断言全部通过，控制台无错误。
- 视觉验证：`outputs/赛事中心验证/新增竞赛组别-自动出生日期限制-v1.png`。
- 本次未暂存、未提交、未推送仓库。

## 赛事报名海报生成与编辑器

### 已完成

- 先按 `$yuanxing` 流程建立并校验 `赛事管理-报名海报-v1_assets`，源海报仅用于信息层级拆解，未作为正式页面背景或裁切素材。
- 新增 5 套 `1080×1920` 无文字正式背景：红蓝赛事竞技、黑橙职业篮球、蓝白青少年、紫色霓虹 3V3、红金冠军荣誉。
- 赛事管理二级导航新增“报名海报”；报名设置补充收费方式、费用、奖项、报名权益、公开地点、联系人、联系电话和宣传副标题。
- 首次保存报名设置后自动创建默认海报；再次修改报名数据会标记海报待同步，并保留原有图层位置和样式。
- 新增本地 Fabric.js 7.4.0 图层编辑器，支持拖动、缩放、旋转、文字颜色/字号/对齐/加粗、复制、删除、锁定、隐藏、层级调整、中心吸附线、撤销与重做。
- 海报草稿和大型画布 JSON 存入 IndexedDB；赛事记录只保存海报 ID、主海报 ID 和报名设置摘要。
- 支持同一赛事多张海报草稿、复制、重命名、删除、设置唯一主海报、同步报名数据和导出准确 `1080×1920 PNG`。
- 自定义背景支持 JPG/PNG/WEBP 上传、固定 9:16 裁剪、缩放、水平/垂直位置调整和 WEBP 压缩。
- 演示报名码采用与正式接口一致的返回结构；二维码生成失败不删除草稿，并可单独重试。
- 新增小程序球队报名落地页 `pages/tournament-register/index`，承接海报场景参数，分别进入创建球队或认领已有球队流程；创建后的球队记录关联赛事 ID。
- 新增云函数 `sxCreateTournamentQrCode`，由云端调用微信小程序码接口并上传云存储，PC 端不接触 AppSecret 或访问令牌。
- Fabric.js 与二维码库均使用本地固定版本，不依赖远程 CDN；许可证随本地依赖文件保存。

### 验证

- `node --check admin/tournament-center.js`、`admin/tournament-poster-editor.js`、小程序报名页、球队创建页和 `sxCreateTournamentQrCode` 通过。
- `native-dist/app.json` 与新增页面 JSON 解析通过。
- 原赛事全生命周期 23 项断言继续全部通过，控制台错误为 0。
- 新增 `tools/test-tournament-poster.js`：验证 5 套模板、`1080×1920` 画布、默认草稿、12 个图层、报名二维码和 IndexedDB 关系，全部通过，控制台错误为 0。
- 在 `1670×941`、`1440×900` 下完成渲染检查，无页首/侧栏漂移、横向错位、按钮遮挡或编辑器重叠。
- 验证截图：`outputs/赛事中心验证/报名海报编辑器-1670x941-v1.png`、`outputs/赛事中心验证/报名海报编辑器-1440x900-v1.png`。
- `npm run typecheck` 未通过：现有 `config/index.ts` 使用 `import.meta`，但当前 TypeScript module 配置不允许；与本次新增原生 JS/HTML/WXML 代码无关。
- 安装依赖后的 npm 审计仍报告项目既有依赖链漏洞，未运行可能造成破坏性升级的 `npm audit fix --force`。

### 当前边界

- PC 演示阶段生成的是可扫描的模拟报名二维码；正式微信小程序码需要部署 `sxCreateTournamentQrCode`、配置对应小程序环境并由服务端接口接入 PC。
- 本次只完成代码和本地验证，未部署云函数、未上传小程序、未暂存、未提交、未推送 GitHub/Gitee。
