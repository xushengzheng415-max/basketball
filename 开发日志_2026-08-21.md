# 开发日志｜2026-08-21

## 机构后台新入口与组织开户

- 新增正式组织入口 `admin/organization-entry.html`：PC扫码确认、机构/学校/赛事主办方身份选择、差异化基础资料、默认工作面板选择与365天全功能体验期领取。
- 正式 `admin/index.html` 默认跳转至新入口；保留 `?legacy=1` 访问原机构后台页面。原有经营后台继续作为教务中心工作面使用，赛事中心仍保持独立页面。
- 新入口的赛事中心跳转会携带组织ID；赛事中心和演示草稿的本地键按组织ID隔离，不再在同一浏览器混用不同组织数据。
- 新增小程序 `pages/pc-auth/index`，在“我的”中加入“PC后台登录”，可扫描 `SXFPC|挑战ID` 二维码并调用 `sxPcAuth` 确认PC登录。

## 云端组织与权益

- 扩展 `sxCollaboration` 的PC认证领域：支持挑战状态查询、短期PC会话、组织资料读取、首次开户、主身份更新、默认工作面更新和365天体验期权益。
- `sx_organizations` 写入主身份、资料、默认工作面板、开户状态、体验开始/到期时间与权益状态；成员关系继续使用 `sx_organization_memberships`。
- `sxPcAuth` 增加HTTP访问路径 `/api/pc-auth` 的请求体兼容，PC端创建挑战与状态轮询走该路径；小程序确认仍使用同一事件函数和同一挑战记录。
- CloudBase Web安全域已包含正式域名；本次临时开启的匿名登录已在验证后关闭，未扩大匿名访问面。
- `sxLogin` 支持通过环境变量 `SXF_BASKETBALL_APPID` 覆盖默认小程序AppID；AppSecret继续只允许放在 `SXF_BASKETBALL_APPSECRET` 云函数环境变量中。

## 上线与验证

- 已部署 `sxCollaboration` 与 `sxPcAuth` 到篮球正式CloudBase环境。
- 已将完整 `admin/` 静态后台发布至 CloudBase `admin/` 路径；`https://www.sxfbasketball.cn/admin/` 保持正式入口，并会跳转到新身份引导页。
- 正式域名和CloudBase系统域名均已回读到新入口页面。
- 正式CloudBase域名下浏览器自动化通过：最新入口脚本、三类身份选择、真实云端二维码挑战生成。
- 正式 `/admin/` 地址跳转到新入口的浏览器自动化也已通过。
- `npm run test:collaboration`、组织入口脚本语法、小程序新页面脚本与 `app.json` 检查通过。
- 清理了未投入使用的Web SDK打包产物；正式PC端改为调用已验证的 `sxPcAuth` HTTP访问路径，减少后台静态资源体积。
- 已创建并回读飞书知识库公告：[赛小蜂篮球 2.1.0 版本更新公告](https://lxcmobp6gun.feishu.cn/wiki/E9mzwclb5i1coNkXhqJc0fKVn6g)。
- 已写入并回读飞书“版本更新记录”Base：记录 `recvsU7c0fg36I`，版本号 `2.1.0`，更新类型“功能新增”，影响范围“全端”，发布状态“已发布”，验证结果“验证通过”。
- 已写入并回读飞书“功能与体验更新”明细表：2.1.0 共3条，覆盖身份引导与365天体验期、赛事中心组织隔离、赛事与教务统一工作面。
- 已写入并回读飞书“测试验收记录”明细表：2.1.0 共4条，记录正式入口、PC二维码挑战、组织开户权益、小程序扫码页；其中真机扫码相关两项明确标记“待真机验证”。

## 微信小程序码替换

- 原PC端普通文本二维码已替换为微信官方小程序码链路：云端使用 `getwxacodeunlimit`，场景参数携带一次性PC挑战ID，并直接打开 `pages/pc-auth/index`。
- 小程序PC登录页支持从微信小程序码的 `scene` 参数进入，并要求用户主动点击“确认登录这台电脑”；普通扫码方式仍保留为兼容入口。
- 正式云端已部署小程序码生成逻辑并验证挑战创建成功；当前环境尚未配置 `SXF_BASKETBALL_APPSECRET`，页面会明确提示未配置，不再回退为普通二维码。
- 待在 `sxCollaboration` 云函数环境变量中设置 `SXF_BASKETBALL_APPID` 与 `SXF_BASKETBALL_APPSECRET` 后，重新刷新正式后台即可生成真实微信小程序码；Secret 不进入仓库、不写入飞书或开发日志。

## 待真机确认

- 使用已审核的赛小蜂篮球小程序，在“我的 → PC后台登录”扫描PC二维码；确认后验证PC自动进入组织资料页。
- 在CloudBase为 `sxLogin` 配置已审核应用的 `SXF_BASKETBALL_APPID` 与 `SXF_BASKETBALL_APPSECRET`（密钥不入仓库、不记录到日志）。
- 完成真机扫码确认后，再测试组织开户、365天体验期和赛事/教务默认工作面跳转。

## 通用赛制规则知识库

- 新增本地可复用规则源文件：`docs/通用赛事赛制与全排名生成规则.md`。
- 已创建并回读飞书知识库页：[通用赛事赛制与全排名生成规则](https://lxcmobp6gun.feishu.cn/wiki/Y2Xywccy2iGMCIkFFwqcTCisnTg)。内容覆盖小组赛+淘汰赛、单败淘汰的全员排位、8/16/32队递归名次线、轮空、场序胜负者引用、单循环与跨篮球/足球字段约定。
- 规则页写入136个知识块并完成回读；可作为足球项目赛事引擎的复用规范。
