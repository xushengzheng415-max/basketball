# 赛小蜂篮球开发记录 — 2026-07-27

## 一、今日完成

### 1. 机构赛事平台与三席协同控制台实施计划

- 根据产品讨论，正式确定一人、二人、三人现场记录模式。
- 确定三席名称与职责：
  - 比赛主控台。
  - 球员技术统计台。
  - MC 现场台。
- 明确一人模式保留原版完整控制界面。
- 明确二人模式只拆出球员技术统计，主控继续负责比分和 MC。
- 明确三人模式继续拆出 MC，形成三席独立操作。
- 补充球员命中、未中、命中率、篮板、助攻、抢断、盖帽、失误和犯规等完整技术统计规则。
- 补充扫码加入、机构预分配、角色回收、掉线接管和比赛中升降模式。
- 补充统一比赛事件流、状态快照、时钟控制权、幂等、冲突和断网恢复方案。
- 将机构后台、赛事工作人员任务、复杂赛事、服务号和平台运营纳入分阶段路线。
- 明确 M0 至 M6 的实施顺序、任务范围和验收标准。

### 2. 完成 M0 需求与数据冻结

- 完成一人、二人、三人模式页面流转和模式切换状态。
- 完成比赛主控员、球员技术统计员、MC 的逐项权限矩阵。
- 明确球队比分、球员统计、MC 音频的唯一权威写入端。
- 完成主控接管、角色回收、掉线和旧权限失效规则。
- 完成现场比赛、协同房间、房间成员和工作人员任务状态。
- 完成房间、比赛、比分、时钟、球队、球员统计、MC、撤销和更正事件字典。
- 完成命中、未中、命中率、犯规归属和数据一致性规则。
- 完成比赛房间、房间成员、角色邀请、事件、快照和在线状态的数据结构。
- 完成索引、机构隔离、关键写入和逻辑接口约束。
- 完成原版单人计分台回归清单。
- 确定首个技术试点使用机构内部 5V5 单场比赛。
- M0 验收通过，下一阶段为 M1 云端比赛房间与一人模式兼容。

### 3. 角色边界修正

- 确认主裁判不作为独立系统身份进入平台。
- 确认机构赛事管理员即 PC 后台管理员，负责机构配置、数据查看和赛事管理。
- 确认赛事管理员从馆内教练中分配比赛主控、球员技术统计和 MC 工作。
- 将规划中的裁判库、裁判任务、裁判签到和裁判报告统一调整为教练/赛事工作人员、赛事任务、到场确认和赛后赛事报告。

### 4. 产品定位与外请人员流程修正

- 明确 PC 端主产品是赛训经营增长平台；赛事中心为可按需启用的附加模块，不替代机构、校区、学员、课程、课消和经营增长等核心能力。
- 明确内部赛事工作人员从机构已有教练中直接分配，教练通过自己的小程序端接收并执行赛事任务。
- 明确外请裁判不录入机构教练系统；机构赛事管理员通过服务号发送邀请，外请人员关注并绑定轻量执行身份后，只查看和操作被指派比赛。
- 明确服务号用于外请邀请、绑定、通知和执法比赛数据查看；比赛中的高频计分、球员统计和 MC 操作仍从小程序控制台完成。
- 同步修订实施计划和 M0 数据规格：增加 `externalMatchWorkerBindings`、外请人员场次级授权、索引、接口和数据隔离规则。
- 明确计分板在手机和 PAD 上均只提供横屏控制台；后续不再保留或验收手机竖屏计分界面。
- 明确二人模式的主控只保留队伍级比赛控制；阵容与全部球员数据操作迁入独立球员技术统计台，统计台只读同步比分、比赛时间、节次和 24/14 秒。
- 明确三人模式中第三台设备独立使用 MC 现场台；主控底部的 MC 区域在被接管后锁定并置灰，只保留二次确认的紧急停止和接管能力。
- 记录文档命名规则：后续新增中文内容文档统一使用中文文件名；固定品牌名、协议名、代码文件名或技术标识除外。

### 5. 文档目录中文命名整理

- 将 `docs` 下可中文化的目录统一改为中文业务名称，包括外链推广、原型素材、原型版本、页面、图标、背景、按钮、报告和素材等。
- 保留 CSDN、MC 等固定平台名或通用缩写。
- 已同步原型实现规格、飞书准备脚本、软著材料脚本和外链跟踪文档中的目录引用。

### 6. 项目记忆清理

- 将唯一项目路径更正为 `E:\Documents\sxf-basketball`，删除旧 `C:\Users\Frank\Documents\` 路径和 Junction 桥接路径说明。
- 将 PC 产品定位、横屏计分、赛后赛事报告、三席协同、内部教练与外请人员边界写入 `AGENTS.md`，作为后续任务的项目记忆。

### 7. 小程序正式版更新提示

- 在 `native-dist/app.js` 接入微信小程序更新管理器。
- 新版本下载完成后，向用户显示“立即更新 / 稍后”确认；只有用户确认后才重启并应用新代码包。
- 不使用自动重启策略，避免打断现场计分等进行中的操作。
- 本次版本确定为 `2.0.3`，更新记录已写入 `docs/赛小蜂篮球2.0.3版本更新记录.md`；待发布时同步至飞书版本更新记录与更新公告。
- 用户已完成微信小程序 `2.0.3` 正式版发布；飞书同步待本机飞书写入凭据可用后补写并回读验证。

### 8. 微信开发者工具上传配置修正

- 上传报错 `800059` 指向 `pages/campus-manager/home/index.js`，已核对该文件及全部 104 个已注册页面文件均存在。
- 关闭本地开发者工具的“忽略未使用文件”优化，避免上传阶段错误排除 `campus-manager` 分包文件。
- `project.private.config.json` 为本地专用配置，不纳入仓库提交。

## 二、变更文件

- `docs/THREE_CONSOLE_COLLABORATION_IMPLEMENTATION_PLAN.md`
  - 新增机构赛事平台与三席协同控制台正式实施计划。
  - 标记 M0 已完成并关联正式规格。
- `docs/THREE_CONSOLE_M0_PRODUCT_DATA_SPEC.md`
  - 新增 M0 页面流转、权限、事件、数据和回归冻结规格。
- `DEVELOPMENT_LOG_2026-07-27.md`
  - 新增当日规划记录。

## 三、验证

- 已读取项目根目录 `AGENTS.md`。
- 已核对现有产品实施规范中的多角色、裁判比分板、MC、数据模型和最新比分板布局约束。
- 已核对现有页面入口：
  - `native-dist/pages/scorer/`
  - `native-dist/pages/scorer-board/`
  - `native-dist/pages/stats-scorer/`
  - `native-dist/pages/mc-system/`
  - `native-dist/pages/referee-report/`
- 已确认计划保持 `native-dist/` 为后续小程序实现来源。
- 已核对现有主计分台、球员统计预览页和 MC 音频页的实现边界。
- 已确认现有主计分台可作为 M1 单人模式兼容基础。
- 已确认 `stats-scorer` 和 `mc-system` 需要在 M2、M3 分别升级。
- 本次仅新增 Markdown 文档，不涉及 WXML、JS、云函数或部署，因此未运行小程序构建和类型检查。

## 四、当前分支与工作区

- 当前分支：`codex/education-2.0`。
- 工作区在本任务开始前已有其他未提交内容。
- 本次未修改、未回滚、未暂存任何既有无关改动。
- 当前分支并非后续三席协同实现的正式开发分支；开始 M0/M1 时应创建独立的 `codex/` 分支或工作树。
- 本次未提交、未推送 GitHub 或 Gitee。

## 五、未解决事项

- 尚未创建或关联三席协同控制台的 GitHub Issue。
- 尚未冻结云数据库集合字段、索引和安全规则。
- 尚未确定比赛事件云函数的最终接口边界和命名。
- 服务号账号关联及消息能力需在实施阶段依据届时微信官方规则复核。
- 复杂赛事首个试点机构、赛事类型和真实验收场景尚待确定。

## 五点五、2.0.3 发布同步结果

- 微信小程序：用户确认 `2.0.3` 已发布正式版。
- 仓库：已提交 `39472d6`（`chore(project): sync 2.0.3 release and Chinese docs`），并已同步至 GitHub `origin/codex/education-2.0` 与 Gitee `gitee/codex/education-2.0`。
- 飞书版本 Base 与知识库公告：本机飞书写入凭据未配置，`credential-status` 显示 `ready: false`；因此未写入、不虚报已同步。待配置凭据后，需要补写版本表记录并创建、回读 `赛小蜂篮球 2.0.3 版本更新公告`。

## 六、下一步

当前开发优先级调整为：先完成 PC 端赛事中心模块的产品设计与实现规划。三席协同控制台已独立记录在 `docs/三席协同现场控制台功能记录.md`，待 PC 端赛事基础能力完成后再进入实施。

后续进入 PC 端赛事中心：

1. 梳理赛训经营增长平台中赛事中心的 PC 信息架构与入口边界。
2. 冻结赛事、赛制、参赛队伍、场次、场地与工作人员安排的首期范围。
3. 基于现有 `admin/index.html` 原型确定首个可实施页面和数据接口。
4. 三席协同的云端比赛房间与小程序控制台在 PC 端基础赛事能力完成后排期实施。

## 七、官网 AEO / GEO 每日优化

### 2026-07-27 自动补跑

- 新增官网文章：
  - `articles/posts/basketball-match-day-operation.html`：核心长文，比赛日执行与场次交接意图。
  - `articles/posts/basketball-substitution-record.html`：单一问答，篮球比赛换人记录意图。
  - `articles/posts/basketball-postgame-result-check.html`：具体场景，训练赛赛后赛果核对意图。
- 已更新 `articles/index.html`、`sitemap.xml`、`llms.txt`、收录台账和外链台账。
- 发文前正式域名首页、内容中心、Sitemap、robots、llms 与既有 3 篇文章均回读 HTTP 200；公开搜索仅作辅助，未取得站长平台 URL 级收录证据，全部保持“待复查”。
- 外链巡检：CSDN 已知公开文章和目标官网页均回读 HTTP 200；已知链接保留 `nofollow`，状态保持有效。今日外部平台人工计划为公众号，待用户手动发布后回传公开 URL。
- 质量自检（直接度/证据匹配/具体性/中文流畅度/结构与声音，满分 50）：比赛日执行清单 45、换人记录 44、赛后赛果核对 44；均高于 43 分发布线。
- `npm run site:validate` 通过：12 篇文章、19 个 Sitemap URL。
- 已通过 `npm run site:deploy` 部署至篮球 CloudBase 环境 `sxf-basketball-d9gp6yt0rd1f7be4d`。CloudBase 系统域名与正式域名的首页、内容中心、Sitemap、robots、llms 及今日 3 篇文章均回读 HTTP 200；今日文章标题、自指 canonical、`index,follow` 和正文首页链接均与本地一致。
- 本次仅处理官网 SEO 白名单内容；未修改小程序、云函数、PC 后台、支付或会员内容；未提交、未推送 GitHub 或 Gitee。
