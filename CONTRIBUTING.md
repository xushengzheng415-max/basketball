# 赛小蜂篮球日常协作规范

## 角色分工

- 日常开发人员：接收客户 Bug 或主动测试发现问题，操作 Codex 完成修复、验证、提交和授权范围内的部署。
- 项目负责人：处理疑难故障、产品规则、账号权限、数据迁移、生产密钥和跨项目问题。
- Codex：读取仓库规则后修改代码、执行验证、提交同步，并在获得授权时执行对应部署。

## 日常 Bug 流程

1. 保留客户原话、截图或视频，补充发生页面和操作步骤。
2. 让 Codex 读取 `AGENTS.md`、检查 Git 状态并复现问题。
3. Codex 只修改相关文件并执行对应测试。
4. 开发人员亲自走一次客户原流程，确认结果正常。
5. Codex 提交并同步到 Gitee，正式版本必须能对应到提交号。
6. 部署前再次核对篮球项目和篮球 CloudBase 环境，只部署受影响内容。
7. 部署后打开正式页面或读取云函数状态，线上验证通过后再回复客户。

可以直接发送给 Codex：

```text
请处理当前收到的 Bug。先读取项目规则并检查 Git 状态，根据客户描述复现，只修改相关文件，不改变其他业务功能。完成后执行相关测试和构建；通过后提交并同步到 Gitee。部署前确认正式篮球项目和篮球 CloudBase 环境，部署后读回线上结果。最后报告原因、修改文件、测试、提交号、部署内容和线上验证。如涉及高风险事项，请停止并交给项目负责人。
```

## 简化分支规则

- `development`：日常修复和测试。
- `main`：已经确认并准备正式部署的稳定版本。
- Codex 可以代替开发人员执行分支、提交和同步操作，但开发人员必须看懂最终报告。
- 同一时间只允许一人处理同一个问题；交给负责人后，原开发人员暂停修改。
- 禁止强制推送或删除 `main`。

## 必须交给项目负责人

- Codex 多次修改仍无法稳定复现或修好。
- 客户提出的可能是新需求而不是 Bug。
- 涉及登录、账号合并、身份权限、收费或数据归属。
- 涉及数据库删除、迁移、批量修改或无法恢复的操作。
- 涉及生产密钥、微信后台、支付、域名、证书或 CloudBase 环境配置。
- 改动影响多个端、多个项目或大量真实用户。

交接时必须提供：客户原始描述、复现步骤、已尝试方案、当前分支、最后提交号、测试结果、部署状态和失败日志。

## 提交前最低检查

- `git status`
- `git diff --check`
- 小程序变更确认作用于 `native-dist/`
- 相关 JavaScript、JSON、WXML 检查
- `npm run typecheck` 或说明既有阻断
- 相关业务测试和构建
- 敏感信息扫描
- 对应开发日志或文档已更新
