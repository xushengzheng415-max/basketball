# 赛事赛程场次数同步开发日志

日期：2026-08-09

## 本次完成

- 修复赛事中心“赛程与场次”仍展示固定示例值“86 场”的问题。
- 新增基于当前赛事组别赛制的场次汇总：按小组赛、淘汰赛及附加名次赛计算计划总场次。
- 赛程生成预览、赛历摘要、场次列表与“生成赛程”动作均改为使用同一份赛制计算结果。
- 示例：8 支球队、2 组每组 4 队的小组单循环（12 场）+ 半决赛（2 场）+ 三四名决赛（1 场）+ 决赛（1 场），合计为 16 场。
- 更新赛事中心脚本版本参数，确保浏览器刷新后加载最新逻辑。

## 修改文件

- `admin/tournament-center.js`
- `admin/tournament-center.html`

## 验证

- `node --check admin/tournament-center.js`：通过。
- 已校验赛程预览、赛历、场次列表和生成动作均引用动态赛制汇总；固定的“86 场”预览文本已移除。
- 现有 `tools/test-tournament-groups.js` 在等待历史页面元素 `.group-management-list` 时超时，未进入本次场次计算断言；该测试阻塞已记录，未因此修改无关页面逻辑。

## 交付边界

- 未创建分支、未暂存、未提交、未推送。
- 未改动赛事以外的业务数据或页面。

## 补充修复：已建比赛与赛程呈现

- 自动排赛现在会保存 `scheduleRows`：每场比赛均包含场次编号、日期、时间、组别、对阵、场地和状态。
- 赛程日历和场次列表只读取已保存的 `scheduleRows`，不再在页面打开时用赛制计划临时拼接场次。
- 对已有的旧赛事草案，首次打开赛程时会将原先仅保存的总场次数兼容迁移为逐场比赛数据，再作为后续页面的唯一来源。
- 重构赛程日历：移除固定日期、固定场馆和不规则网格，改为按真实比赛日分组展示场次卡片，并在顶部显示已建比赛、比赛日和已用场地数。
- 自动排赛页中的赛事日期、每日时间、单场时长、缓冲时间和场馆/球场数改为读取当前赛事设置，不再使用固定示例值。

## 补充验证

- `node --check admin/tournament-center.js`：通过。
- 已验证代码中自动排赛动作写入逐场比赛数据，赛程日历和场次列表读取该数据源；旧的临时赛程读取路径已移除。

## 补充修复：阶段与轮次

- 已建比赛增加阶段、轮次和赛制阶段名称三个字段。
- 小组赛按各组的比赛轮次标注，例如“第 1 轮”“第 2 轮”；淘汰赛按“半决赛”“三、四名决赛”“决赛”等实际阶段标注。
- 统一日历的每张场次卡片与场次列表中的组别信息均同步展示“阶段 · 轮次 · 组别”。
- 旧的已建比赛在首次打开时会补齐上述字段并保存，避免历史赛程缺失阶段信息。

## 补充修复：组别、场地与时间对齐

- 统一日历场次卡调整为固定字段顺序：开赛时间、阶段/轮次、组别、场地与对阵、状态。
- 组别从阶段/轮次说明中独立为高亮列，避免多个标签混排导致组别难以识别。
- 场地名称固定显示在对阵上方，时间固定显示在卡片首列；同一比赛日的所有场次可按列直接对齐比较。
- 已执行脚本语法及场次卡字段/布局校验。

## 补充修复：统一日历与小组赛轮次

- 赛程日历改为真正的统一日历：时间为行、场地为列，每个交叉单元格只展示该时间、该场地的已建比赛。
- 小组赛采用轮转赛程生成。以 8 队、A/B 两组、每组 4 队为例：每轮共 4 场，A 组 2 场与 B 组 2 场会同时进入同一个时间段。
- 已建比赛明确保存分组名称（如 `U8 启蒙组 · A组`、`U8 启蒙组 · B组`），并补齐阶段、轮次、实际对阵和排赛时间。
- 旧版逐场数据通过 `scheduleVersion` 迁移为新版轮转赛程，避免出现同一时间段混入第 1 轮和第 2 轮的情况。
- 已执行脚本语法、统一日历网格和分组轮次生成校验。

## 补充修复：旧赛程迁移与分组颜色

- 将赛程数据版本升级至 `scheduleVersion: 3`，强制把上一版错误轮次的已建比赛重新按 A/B 组轮转规则生成。
- 修复迁移时旧组别字段覆盖新分组字段的问题，确保 `U8 启蒙组 · A组`、`U8 启蒙组 · B组` 会写入已建比赛。
- 小组赛标签调整为“`小组赛 · A组 · 第 N 轮`”或“`小组赛 · B组 · 第 N 轮`”。
- A 组使用蓝色标识，B 组使用紫色标识；淘汰赛保留橙色标识。

## 补充功能：统一日历拖拽调赛

- 已建比赛卡片支持拖拽，日历中的每一个“日期 × 时间 × 场地”单元格均可作为放置目标。
- 放置到空白单元格后，场次的日期、时间、场地和更新时间会立即写入同一份已建比赛数据并保存。
- 目标单元格已有比赛时会阻止放置并提示，避免同一场地同一时段重复排赛。
- 拖动中会提供抓取、半透明和目标高亮反馈。
- 已执行脚本语法及拖拽事件/样式校验。

## 补充功能：固定场地列与场次交换

- 统一日历改为读取赛事场馆设置中的全部启用球场，所有比赛日固定显示相同场地列，不再因为当天未使用某些场地而压缩或变形。
- 空白场地单元格保持可拖放，并显示“拖到此处”。
- 将场次拖到已有比赛的单元格时，系统会交换两场比赛的日期、时间和场地；拖到空白单元格时仍为单场调整。
- 已执行脚本语法以及固定场地列/场次交换数据逻辑校验。

## 补充功能：筛选、手动加场与当前位置保留

- 拖拽调整、场次交换、日历筛选及手动新增场次后，页面会恢复至用户操作前的滚动位置，不再跳回页面顶部。
- 日历顶部增加“场地”和“时间段”筛选，可聚焦查看指定资源与时段。
- 每个空白日历单元格增加“＋ 添加场次”；可填写阶段、轮次、组别、主队和客队，保存后直接写入已建比赛。
- 手动新增场次自动分配场次编号、日期、时间、场地、状态及当前赛程数据版本。
- 已执行脚本语法、控件接入与当前位置保留校验。

## 补充优化：手动添加场次下拉选择

- 手动添加场次中的阶段、轮次和组别改为从当前已建赛程的既有字段中选择。
- 主队、客队改为从已审核正式参赛球队中选择，不再允许手工录入名称。
- 比赛日期、时间、场地继承用户点击的空白日历单元格，保持只读。
- 增加主客队不能为空且不得相同的校验。

## 补充功能：场次表预览、导出与打印

- 将场次列表改为正式赛程表预览样式，包含赛事名称、赛事日期、场次数、制表日期、阶段/轮次、组别、双方球队、场地与状态。
- 增加“导出表格”：导出 UTF-8 CSV，可直接使用 Excel 打开和编辑。
- 增加“直接打印”：调用浏览器打印并提供 A4 横向的专用打印样式。
- 已执行脚本语法、导出动作与打印样式接入校验。

## 补充功能：场次表筛选

- 场次表预览新增阶段、轮次、组别、球队、场地、状态六项下拉筛选。
- 筛选后的表格会即时重排并更新场次数。
- 导出表格和直接打印均使用当前筛选结果，便于按组别、轮次或场地分发赛程。
- 提供“清除筛选”快速恢复完整赛程表。

## 补充功能：日历快捷资源维护与保存

- 赛程日历顶部新增“新增时间段”“新增场地”“保存赛程”快捷操作。
- 新增时间段后，该时间会在每个比赛日显示为可拖放、可手动加场的日历行。
- 新增场地会写入当前赛事的场馆设置，并在各比赛日固定展示为场地列。
- 保存赛程会记录保存时间、更新场次数，并使场次表预览、导出和打印读取同一份已保存数据。
- 从赛程子导航中移除“场次详情”标签，保留自动排赛、统一日历、场次列表与场馆球场四个主操作面。

## 补充优化：赛程日历主页面

- 赛程子导航收敛为“赛程日历、场次列表、场馆与球场”；原自动排赛入口会自动跳转到赛程日历。
- 日历顶部增加排赛设置区，集中展示计划场次、日期/时段、单场时长、调整条件、重新自动排赛和最近保存时间。
- 每张日历比赛卡加入双方队徽（无上传队徽时使用统一球队首字占位），并优化阶段、组别、对阵和色彩的视觉层级。
- 已执行脚本语法和日历主页面/队徽渲染校验。

## 补充功能：按比赛日维护时间段与清空赛程

- “新增时间段”改为同时选择比赛日期与开赛时间；时间段只出现在所选日期，支持为单独比赛日补充可排赛时段。
- 新增的日期会直接生成固定场地列和空白单元格，可继续通过“添加场次”或拖拽完成排赛。
- 日历页新增“清空赛程”二次确认：仅移除已建比赛与保存版本，保留球队、分组、场馆、比赛日、时间段和排赛条件，随后可重新自动排赛。
- 放大日历卡片的阶段、组别、对阵文字和队徽；队徽改为位于双方队名的最外侧，形成“队徽 + 主队 VS 客队 + 队徽”的阅读顺序。

## 补充验证

- `node --check admin/tournament-center.js`：通过。
- `git diff --check -- admin/tournament-center.js admin/tournament-center.css admin/tournament-center.html`：通过。
- 未创建分支、未暂存、未提交、未推送；保留工作区既有改动。

## 补充优化：清空赛程后的前置设置

- 清空确认后不再停留在空状态页，而是自动弹出“重新设置赛程”。
- 弹窗可设置首末比赛日、每日起止时间、单场时长和场次缓冲；提供“保存设置”及“保存并自动排赛”两条后续路径。
- 已执行 `node --check admin/tournament-center.js` 与相关文件 `git diff --check`，均通过。

## 补充优化：日历队徽与空时间段维护

- 赛程日历改为从同一赛事的报名球队数据读取 `logo`，主、客队徽分别显示在双方队名的最外侧；未上传队徽时才使用球队首字占位。
- 日历比赛卡片改为居中布局，阶段、组别和双方对阵在卡片中居中对齐。
- 无已建比赛的日期时间行新增“删除”入口；有比赛时不显示删除入口，并在逻辑层再次拦截删除，保护已排场次。

## 补充重构：赛程设置归属与全组别排赛

- 已从“赛事管理”子导航移除“场馆设置”，避免在赛事资料与赛程资源之间重复维护。
- 场地、球场、比赛日和可用时间统一收敛至 `赛程 / 设置` 隐藏三级页：它不作为赛程主导航标签，只能从赛程日历的“赛程设置”、赛事工作台步骤或清空赛程后的前置流程进入。
- 保存赛程设置后返回赛程日历，并标记资源已配置；自动排赛不再提示回到赛事管理。
- 设置范围强制为“全部组别”，赛程日历文案、工作台步骤和自动排赛校验均明确使用全赛事共享的资源配置；日历持续按每场比赛自身的组别显示并排布。
- 已执行 `node --check admin/tournament-center.js` 与相关文件 `git diff --check`，均通过。

## 补充功能：半场 A/B 资源与场馆入口

- 赛程设置中将球场类型设为“半场”后，自动排赛、资源摘要和日历列会将该物理球场扩展为 `A 半场`、`B 半场` 两个可同时使用的独立场地；标准全场仍只占用一列。
- 赛程设置页标题区新增“新增场馆”“编辑当前场馆”入口；编辑场馆只调整名称和地址，球场数量及全场/半场类型仍由球场配置维护。
- 已执行 `node --check admin/tournament-center.js` 与相关文件 `git diff --check`，均通过。

## 补充优化：按上午/下午开赛时间自动推算场次

- 赛程设置的时间配置改为“上午首场开赛、下午首场开赛、最晚结束时间、单场时长、场次缓冲时间”，不再要求维护笼统的每日最早开始时间。
- 系统将上午首场至下午首场、下午首场至最晚结束分别作为两个排赛时段，自动推算并实时展示每天全部可安排的开赛时间。
- 自动排赛改为读取推算出的时间段；赛程日历和排赛摘要同步展示上午/下午开赛设置与最晚结束时间。
- 已执行 `node --check admin/tournament-center.js` 与相关文件 `git diff --check`，均通过。

## 补充优化：三段式与手动时间段

- 时间设置拆分为上午首场/中午最晚结束、下午首场/下午最晚结束；两段分别独立推算，不再跨越中午休息。
- 新增“晚上时段”开关。开启后可填写晚上首场与最晚结束，晚间可用场次会加入自动排赛；关闭时不会排入夜场。
- 新增“手动时间段”，可命名、填写起止时间、删除；适用于临时增加的开赛窗口，并与上午、下午、晚上一起参与可排场次推算。
- 已执行 `node --check admin/tournament-center.js` 与相关文件 `git diff --check`，均通过。

## 补充优化：跨时段排赛保护与当前日期校正

- 赛程时间段被作为硬约束：单场比赛不得跨越中午、下午、夜间或手动时段的结束线；自动排赛仅生成能在对应时段内完整结束的开赛时间。
- 手动添加场次若例如在 11:30 开赛会超过 12:00 的中午结束线，系统不再直接保存，而是提示“调整时间设置”或移到下一可用时段；淘汰赛会额外提示移到下午可能压缩晋级队伍恢复时间，优先建议延长时段、缩短场间缓冲或提前首场开赛。
- 日历拖拽或交换场次同样受时间段约束保护，目标时间或交换后的原时间不合规时会阻止变更并引导至赛程设置。
- 编辑赛程资源时，比赛开始日期自动以今天为下限；过期的开始日期不会继续出现在编辑表单中，结束日期也会自动校正为不早于开始日期。保存后同步写入当前赛事的比赛日期范围。

## 补充修复：结束日期选择器

- 为赛程设置中的开始、结束日期增加显式日期面板唤起；解决部分桌面 `file://` 页面中点击结束日期后原生选择器没有展开的问题。
- 结束日期仍受开始日期约束，不能选择早于当前开始日期的日期。

## 补充修复：截止日期编辑与完整时段设置

- 修复结束日期选择后又被赛事原截止日期强制覆盖的问题：现在结束日期以用户当前选择为准，只在该值无效时才使用赛事截止日兜底。
- 移除日期范围下方的冗余说明文案。
- 赛程设置页取消资源设置区的固定高度和裁切；上午、下午、夜间、手动时段及例外日期均可完整显示并随页面滚动查看。

## 文档归档：赛小蜂篮球知识库分类整理

- 在项目根目录新增 `01_Raw/02_赛小蜂篮球`，按项目总览、产品规划、赛事中心、现场协作、原型设计、技术架构、官网运营、版本记录、软著合规和操作指南分类归档。
- 已汇总项目说明、`docs` 文档、原型资料、运营资料、开发记录、软著材料和使用指南；保留原路径以避免既有页面、脚本和工具引用失效。
